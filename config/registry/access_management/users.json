[
  {
    "tool_id": "access_management.change_user_password",
    "description": "Change a user's password.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "Internal user ID (``_id``) to update."
        },
        "password": {
          "type": "string",
          "description": "New password for the user. Must not be empty."
        }
      },
      "required": [
        "user_id",
        "password"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Reset the password for the user with ID 'user123'.",
        "arguments": {
          "user_id": "user123",
          "password": "NewSecurePassword123!"
        },
        "notes": "This call updates the password for the user with ID 'user123' to a new secure value. Use this when a user requests a password reset or if their account security is compromised."
      },
      {
        "user_query": "Change the password for the admin user with ID 'admin001' to a new value.",
        "arguments": {
          "user_id": "admin001",
          "password": "AdminPass2023!"
        },
        "notes": "This call changes the password for the admin user with ID 'admin001'. This is useful for enforcing password updates for administrative accounts."
      },
      {
        "user_query": "Update the password for the user with ID 'teamlead45' to a stronger one.",
        "arguments": {
          "user_id": "teamlead45",
          "password": "Str0ngP@ssw0rd!"
        },
        "notes": "This call sets a stronger password for the user with ID 'teamlead45'. Use this to improve account security for key team members."
      }
    ]
  },
  {
    "tool_id": "access_management.create_user",
    "description": "Create a new user in Sisense.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_data": {
          "type": "object",
          "properties": {
            "email": {
              "type": "string"
            },
            "role": {
              "type": "string"
            },
            "userName": {
              "type": "string"
            },
            "firstName": {
              "type": "string"
            },
            "lastName": {
              "type": "string"
            },
            "groups": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "password": {
              "type": "string"
            },
            "preferences": {
              "type": "object"
            }
          },
          "required": [
            "email",
            "role"
          ],
          "additionalProperties": true,
          "description": "User details, using canonical Sisense payload field names: - ``email`` : str — the user's email address. **Required.** - ``role`` : str — role name to assign, in either vocabulary (``\"viewer\"`` or ``\"consumer\"``, ``\"sysAdmin\"`` or ``\"super\"``, ``\"dashboardDesigner\"``/``\"designer\"`` or ``\"contributor\"``, or any other role the instance defines); resolved to ``roleId``. **Required.** - ``userName`` : str — the user's login name (optional). - ``firstName`` : str — the user's first name (optional). - ``lastName`` : str — the user's last name (optional). - ``groups`` : list[str] — group names to assign, resolved to IDs (optional). - ``password`` : str — initial password; if omitted, the user receives an email to set one (optional). - ``preferences`` : dict — user preference settings (optional)."
        }
      },
      "required": [
        "user_data"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Add a new user with the Viewer role and assign them to the Sales and Marketing groups.",
        "arguments": {
          "user_data": {
            "email": "jane.doe@example.com",
            "firstName": "Jane",
            "lastName": "Doe",
            "role": "VIEWER",
            "groups": [
              "Sales",
              "Marketing"
            ],
            "preferences": {
              "language": "en",
              "timezone": "America/New_York"
            }
          }
        },
        "notes": "This call creates a new user named Jane Doe with the Viewer role, assigns them to the Sales and Marketing groups, and sets their preferences for language and timezone."
      },
      {
        "user_query": "Create a new Designer user and assign them to the Data Analysts group.",
        "arguments": {
          "user_data": {
            "email": "john.smith@example.com",
            "firstName": "John",
            "lastName": "Smith",
            "role": "DESIGNER",
            "groups": [
              "Data Analysts"
            ],
            "preferences": {
              "language": "en",
              "timezone": "Europe/London"
            }
          }
        },
        "notes": "This call creates a new user named John Smith with the Designer role, assigns them to the Data Analysts group, and configures their preferences for language and timezone."
      },
      {
        "user_query": "Add a new admin user with no group assignments.",
        "arguments": {
          "user_data": {
            "email": "admin.user@example.com",
            "firstName": "Admin",
            "lastName": "User",
            "role": "ADMIN",
            "groups": [],
            "preferences": {
              "language": "en",
              "timezone": "UTC"
            }
          }
        },
        "notes": "This call creates a new user named Admin User with the Admin role, does not assign them to any groups, and sets their preferences for language and timezone."
      }
    ]
  },
  {
    "tool_id": "access_management.create_users_bulk",
    "description": "Create multiple users in a single bulk request.",
    "parameters": {
      "type": "object",
      "properties": {
        "users": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "User definitions to create. Each dictionary should use canonical Sisense user fields (at minimum ``email``, ``firstName``, and ``roleId``)."
        }
      },
      "required": [
        "users"
      ]
    },
    "mutates": true
  },
  {
    "tool_id": "access_management.delete_user",
    "description": "Delete a user by their email (username).",
    "parameters": {
      "type": "object",
      "properties": {
        "user_name": {
          "type": "string",
          "description": "The email or username of the user to be deleted.",
          "format": "email"
        }
      },
      "required": [
        "user_name"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Remove the user with the email john.doe@example.com from the system.",
        "arguments": {
          "user_name": "john.doe@example.com"
        },
        "notes": "This call deletes the user account associated with the email john.doe@example.com. Use this when a user is no longer part of the organization or should no longer have access."
      },
      {
        "user_query": "Delete the account for jane.smith@company.com.",
        "arguments": {
          "user_name": "jane.smith@company.com"
        },
        "notes": "This operation removes the user jane.smith@company.com from the system. Use this to revoke access for a specific user permanently."
      },
      {
        "user_query": "I need to delete the user with the email admin@org.com.",
        "arguments": {
          "user_name": "admin@org.com"
        },
        "notes": "This deletes the user admin@org.com from the system. Ensure that this user is not critical for administrative tasks before proceeding."
      }
    ]
  },
  {
    "tool_id": "access_management.get_my_user",
    "description": "Retrieve the currently logged-in user for the API token.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Who am I logged in as right now?",
        "arguments": {},
        "notes": "This call retrieves the details of the currently authenticated user based on the API token. Useful for verifying the identity of the logged-in user."
      },
      {
        "user_query": "Can you fetch the user details for the current API token?",
        "arguments": {},
        "notes": "This call returns the user object associated with the API token, including information like email, username, and internal ID. Ideal for debugging or auditing purposes."
      },
      {
        "user_query": "What is the email address of the user associated with this token?",
        "arguments": {},
        "notes": "This retrieves the logged-in user's information, which can be used to confirm the email address or other identifying details of the API token owner."
      }
    ]
  },
  {
    "tool_id": "access_management.get_roles",
    "description": "Retrieve all Sisense roles.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What roles are available in our Sisense instance?",
        "arguments": {},
        "notes": "This call retrieves all roles defined in the Sisense instance. Useful for understanding role configurations or preparing for role-based access control setup."
      },
      {
        "user_query": "Can you list all the roles so I can map them to user groups in our system?",
        "arguments": {},
        "notes": "This call fetches the complete list of roles, which can be used to map Sisense roles to external user groups during integration or migration workflows."
      },
      {
        "user_query": "I need to see all roles to verify if a new role needs to be created.",
        "arguments": {},
        "notes": "Use this call to review the existing roles and determine if additional roles are necessary for new user access requirements."
      }
    ]
  },
  {
    "tool_id": "access_management.get_user",
    "description": "Retrieve one named user by email address, in the canonical user row shape.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_email": {
          "type": "string",
          "description": "Email address of the user to retrieve. **Required** — this method always answers \"one named user\"; use ``get_users_all`` for every user.",
          "format": "email"
        }
      },
      "required": [
        "user_email"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Find details for the user with email john.doe@example.com.",
        "arguments": {
          "user_email": "john.doe@example.com"
        },
        "notes": "This call retrieves detailed information about John Doe, including their groups and roles."
      },
      {
        "user_query": "Get user information for jane.smith@company.com.",
        "arguments": {
          "user_email": "jane.smith@company.com"
        },
        "notes": "Use this to fetch Jane Smith's user details, including expanded group and role data."
      },
      {
        "user_query": "Retrieve the profile for admin@organization.org.",
        "arguments": {
          "user_email": "admin@organization.org"
        },
        "notes": "This call is useful for verifying the admin user's access and role assignments."
      }
    ]
  },
  {
    "tool_id": "access_management.get_users_all",
    "description": "Retrieve every user, one canonical user row each.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Show me all the users in the system along with their roles and group memberships.",
        "arguments": {},
        "notes": "This call retrieves a list of all users in the system, including their associated roles and group memberships. Useful for auditing or managing user access."
      },
      {
        "user_query": "I need to see which groups and roles are assigned to each user.",
        "arguments": {},
        "notes": "This call provides detailed information about each user's group and role assignments. It excludes the 'Everyone' group if the user belongs to other groups."
      },
      {
        "user_query": "Can I get a full list of users with their roles and groups for reporting purposes?",
        "arguments": {},
        "notes": "This call generates a comprehensive list of users, including their roles and group memberships, which can be used for reporting or compliance checks."
      }
    ]
  },
  {
    "tool_id": "access_management.update_user",
    "description": "Update an existing Sisense user identified by their email address.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_email": {
          "type": "string",
          "description": "Email address of the user to update (used to locate the user).",
          "format": "email"
        },
        "user_data": {
          "type": "object",
          "properties": {
            "email": {
              "type": "string"
            },
            "userName": {
              "type": "string"
            },
            "firstName": {
              "type": "string"
            },
            "lastName": {
              "type": "string"
            },
            "role": {
              "type": "string"
            },
            "groups": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "password": {
              "type": "string"
            },
            "preferences": {
              "type": "object"
            }
          },
          "required": [],
          "additionalProperties": true,
          "description": "Dictionary of fields to update. Only include fields you want to change. Supported fields ---------------- - email : str Update the user's email address. - userName : str Update the user's username/login name. - firstName : str Update the user's first name. - lastName : str Update the user's last name. - role : str Role name in either vocabulary — raw (``\"consumer\"``, ``\"super\"``, ``\"contributor\"``), the UI name (``\"viewer\"``, ``\"sysAdmin\"``, ``\"dashboardDesigner\"``), or any other role the instance defines (``dataDesigner``, ``dataAdmin``, custom roles). Matched ignoring case, spaces and punctuation. This is resolved to ``roleId`` before sending the API request. - groups : list[str] List of group names to apply. Group names are resolved to group IDs before sending the API request. If ``groups`` is explicitly provided as an empty list (``[]``), group memberships are cleared (tenant defaults may still apply)."
        }
      },
      "required": [
        "user_email",
        "user_data"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Update the email and role of a user with the email john.doe@example.com.",
        "arguments": {
          "user_email": "john.doe@example.com",
          "user_data": {
            "email": "john.doe.new@example.com",
            "role": "designer"
          }
        },
        "notes": "This call updates the user's email address and assigns them the 'designer' role."
      },
      {
        "user_query": "Change the username and clear all group memberships for jane.smith@example.com.",
        "arguments": {
          "user_email": "jane.smith@example.com",
          "user_data": {
            "userName": "jane.smith.new",
            "groups": []
          }
        },
        "notes": "This call updates the user's username and removes all group memberships."
      },
      {
        "user_query": "Update the first and last name of a user with the email admin.user@example.com.",
        "arguments": {
          "user_email": "admin.user@example.com",
          "user_data": {
            "firstName": "Admin",
            "lastName": "User"
          }
        },
        "notes": "This call updates the user's first and last name while leaving other fields unchanged."
      }
    ]
  }
]