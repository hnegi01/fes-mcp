[
  {
    "tool_id": "dashboard.can_be_owned",
    "description": "Check whether a dashboard can be owned by the current user.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to check."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Can I take ownership of the dashboard with ID '12345abcde'?",
        "arguments": {
          "dashboard_id": "12345abcde"
        },
        "notes": "This checks if the dashboard with the specified ID can be owned by the current user. Useful for determining ownership eligibility before attempting to claim it."
      },
      {
        "user_query": "Am I allowed to own the dashboard with ID '67890fghij'?",
        "arguments": {
          "dashboard_id": "67890fghij"
        },
        "notes": "This verifies whether the user has the ability to own the dashboard with the given ID. Typically used in workflows involving dashboard ownership changes."
      },
      {
        "user_query": "Is it possible for me to own the dashboard with ID 'abc123xyz'?",
        "arguments": {
          "dashboard_id": "abc123xyz"
        },
        "notes": "This call determines if the current user can take ownership of the specified dashboard. Useful for validating permissions before proceeding with ownership transfer."
      }
    ]
  },
  {
    "tool_id": "dashboard.export_dashboard",
    "description": "Export a dashboard definition using the Sisense admin export endpoint.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to export."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "How can I export the definition of the sales dashboard?",
        "arguments": {
          "dashboard_id": "5f7d3b2e4c8b9a0012345678"
        },
        "notes": "This call exports the definition of the dashboard with the ID '5f7d3b2e4c8b9a0012345678', which might represent a sales dashboard. Use this to retrieve the dashboard's structure and configuration."
      },
      {
        "user_query": "I need to back up the marketing dashboard configuration. How do I do that?",
        "arguments": {
          "dashboard_id": "6a8e4f9d5b7c2a0098765432"
        },
        "notes": "This call retrieves the definition of the dashboard with the ID '6a8e4f9d5b7c2a0098765432', which could be a marketing dashboard. This is useful for creating a backup of the dashboard's settings and layout."
      },
      {
        "user_query": "Can I get the layout and widgets of the executive overview dashboard?",
        "arguments": {
          "dashboard_id": "7b9f5c1d6e8a3b0043210987"
        },
        "notes": "This call exports the definition of the dashboard with the ID '7b9f5c1d6e8a3b0043210987', potentially representing an executive overview dashboard. Use this to access details like widgets, layout, and filters."
      }
    ]
  },
  {
    "tool_id": "dashboard.get_all_dashboards",
    "description": "Retrieve all dashboards from the Sisense server.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "How can I list all dashboards available on the Sisense server?",
        "arguments": {},
        "notes": "This call retrieves all dashboards accessible to the user, including their metadata."
      },
      {
        "user_query": "I need to fetch all dashboards to review their configurations.",
        "arguments": {},
        "notes": "Use this to get a complete list of dashboards for auditing or configuration review purposes."
      },
      {
        "user_query": "Can I get a list of all dashboards owned by me?",
        "arguments": {},
        "notes": "This retrieves all dashboards, including those owned by the user, using the admin endpoint."
      }
    ]
  },
  {
    "tool_id": "dashboard.get_dashboard_by_id",
    "description": "Retrieve a specific dashboard by its ID.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard to retrieve."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "How can I retrieve the dashboard with ID '5f7d3a2e4b8c9a0012345678'?",
        "arguments": {
          "dashboard_id": "5f7d3a2e4b8c9a0012345678"
        },
        "notes": "This call retrieves the dashboard with the specified ID. Use this when you need details about a specific dashboard."
      },
      {
        "user_query": "I need to fetch the dashboard with the ID '63a1b2c3d4e5f67890123456'. How do I do that?",
        "arguments": {
          "dashboard_id": "63a1b2c3d4e5f67890123456"
        },
        "notes": "This retrieves the dashboard corresponding to the given ID. Useful for accessing dashboard metadata or verifying its existence."
      },
      {
        "user_query": "Can you get me the dashboard with ID '7e8f9a0b1c2d3e4f56789012'?",
        "arguments": {
          "dashboard_id": "7e8f9a0b1c2d3e4f56789012"
        },
        "notes": "This call fetches the dashboard identified by the provided ID. Ideal for retrieving specific dashboards for review or updates."
      }
    ]
  },
  {
    "tool_id": "dashboard.get_dashboard_by_name",
    "description": "Retrieve a specific dashboard by its name.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_name": {
          "type": "string",
          "description": "Title of the dashboard to retrieve."
        }
      },
      "required": [
        "dashboard_name"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Find the dashboard named 'Sales Performance Overview'.",
        "arguments": {
          "dashboard_name": "Sales Performance Overview"
        },
        "notes": "This retrieves the dashboard titled 'Sales Performance Overview', typically used to analyze sales KPIs and trends."
      },
      {
        "user_query": "Get the dashboard called 'Marketing Campaign Analysis'.",
        "arguments": {
          "dashboard_name": "Marketing Campaign Analysis"
        },
        "notes": "Use this to fetch the 'Marketing Campaign Analysis' dashboard, which might contain insights on campaign performance and ROI."
      },
      {
        "user_query": "Retrieve the dashboard with the name 'Executive Summary'.",
        "arguments": {
          "dashboard_name": "Executive Summary"
        },
        "notes": "This call is useful for accessing the 'Executive Summary' dashboard, often used for high-level business reporting."
      }
    ]
  },
  {
    "tool_id": "dashboard.get_dashboard_widgets",
    "description": "Retrieve widget definitions from an admin export of the dashboard.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_ref": {
          "type": "string",
          "description": "Dashboard ``oid`` or title."
        }
      },
      "required": [
        "dashboard_ref"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What widgets are included in the dashboard titled 'Sales Overview'?",
        "arguments": {
          "dashboard_ref": "Sales Overview"
        },
        "notes": "This retrieves all widget definitions from the 'Sales Overview' dashboard using its title as a reference."
      },
      {
        "user_query": "Can you list the widgets in the dashboard with ID '63a9f0ea7bb9805e7e5e1f7c'?",
        "arguments": {
          "dashboard_ref": "63a9f0ea7bb9805e7e5e1f7c"
        },
        "notes": "This fetches widget definitions from the dashboard identified by its unique 24-character ID."
      },
      {
        "user_query": "I need to check the widgets in the 'Marketing Performance' dashboard. Can you help?",
        "arguments": {
          "dashboard_ref": "Marketing Performance"
        },
        "notes": "This retrieves widget definitions from the 'Marketing Performance' dashboard using its title as the reference."
      }
    ]
  },
  {
    "tool_id": "dashboard.get_dashboards",
    "description": "Retrieve dashboards visible to the authenticated user.",
    "parameters": {
      "type": "object",
      "properties": {
        "fields": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Subset of fields to include in the response (for example ``[\"oid\", \"title\", \"owner\"]``). When omitted, all fields are returned."
        }
      },
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Show me the dashboards I own with their titles and IDs.",
        "arguments": {
          "fields": [
            "oid",
            "title"
          ]
        },
        "notes": "This call retrieves only the 'oid' and 'title' fields for dashboards visible to the authenticated user. Use this to get a concise list of dashboard identifiers and names."
      },
      {
        "user_query": "I need to see all dashboards shared with me, including their owners and titles.",
        "arguments": {
          "fields": [
            "title",
            "owner"
          ]
        },
        "notes": "This call fetches the 'title' and 'owner' fields for dashboards accessible to the user. Useful for understanding who owns the dashboards shared with you."
      },
      {
        "user_query": "Get all available details for dashboards I can access.",
        "arguments": {
          "fields": []
        },
        "notes": "This call retrieves all fields for dashboards visible to the authenticated user. Use this when you need comprehensive dashboard details without filtering specific fields."
      }
    ]
  },
  {
    "tool_id": "dashboard.move_dashboard_to_folder",
    "description": "Move a dashboard into a folder.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to move."
        },
        "folder_id": {
          "type": "string",
          "description": "The target folder ``oid`` (``parentFolder`` value)."
        }
      },
      "required": [
        "dashboard_id",
        "folder_id"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How can I move the 'Sales Overview' dashboard into the 'Q4 Reports' folder?",
        "arguments": {
          "dashboard_id": "dashboard_12345",
          "folder_id": "folder_67890"
        },
        "notes": "This moves the 'Sales Overview' dashboard into the 'Q4 Reports' folder. Use this when reorganizing dashboards into specific folders."
      },
      {
        "user_query": "I need to transfer the 'Marketing Performance' dashboard to the 'Archived Dashboards' folder. How do I do that?",
        "arguments": {
          "dashboard_id": "dashboard_54321",
          "folder_id": "folder_09876"
        },
        "notes": "This transfers the 'Marketing Performance' dashboard to the 'Archived Dashboards' folder. Useful for archiving older dashboards."
      },
      {
        "user_query": "Can I move the 'Executive Summary' dashboard to the 'Leadership Reports' folder?",
        "arguments": {
          "dashboard_id": "dashboard_11223",
          "folder_id": "folder_44556"
        },
        "notes": "This action relocates the 'Executive Summary' dashboard into the 'Leadership Reports' folder. Ideal for organizing dashboards by audience."
      }
    ]
  },
  {
    "tool_id": "dashboard.publish_dashboard",
    "description": "Publish (republish) a dashboard.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to publish."
        },
        "admin_access": {
          "type": "boolean",
          "description": "When ``True`` (default), request with ``adminAccess=true``."
        },
        "force": {
          "type": "boolean",
          "description": "When ``True``, request with ``force=true``. Default is ``False``."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Publish the dashboard with ID 'dashboard_12345'.",
        "arguments": {
          "dashboard_id": "dashboard_12345",
          "admin_access": true,
          "force": false
        },
        "notes": "This call republishes the specified dashboard with admin access enabled. Use this when the caller has access but is not the owner."
      },
      {
        "user_query": "Force publish the dashboard 'sales_dashboard_2023' after script updates.",
        "arguments": {
          "dashboard_id": "sales_dashboard_2023",
          "admin_access": true,
          "force": true
        },
        "notes": "This call forces the republishing of the dashboard, typically used after updates to scripts or configurations."
      },
      {
        "user_query": "Republish the dashboard 'marketing_dashboard' without admin access.",
        "arguments": {
          "dashboard_id": "marketing_dashboard",
          "admin_access": false,
          "force": false
        },
        "notes": "This call republishes the dashboard without admin access. Use this when the caller is the owner or already has sufficient permissions."
      }
    ]
  },
  {
    "tool_id": "dashboard.rename_dashboard",
    "description": "Rename a dashboard.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard to rename."
        },
        "title": {
          "type": "string",
          "description": "The new dashboard title."
        }
      },
      "required": [
        "dashboard_id",
        "title"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How do I rename the sales dashboard to 'Q3 Sales Performance'?",
        "arguments": {
          "dashboard_id": "62f1a2b3c4d5e6f7890abc12",
          "title": "Q3 Sales Performance"
        },
        "notes": "This renames the dashboard with the specified ID to 'Q3 Sales Performance'. Use this when updating the title to reflect a new reporting period or focus."
      },
      {
        "user_query": "Can I change the name of the 'Marketing Overview' dashboard to '2023 Marketing Strategy'?",
        "arguments": {
          "dashboard_id": "5e4d3c2b1a0f987654321abc",
          "title": "2023 Marketing Strategy"
        },
        "notes": "This updates the title of the 'Marketing Overview' dashboard to '2023 Marketing Strategy'. Useful for aligning dashboard names with updated organizational goals."
      },
      {
        "user_query": "I need to rename the 'Executive Summary' dashboard to 'Monthly Executive Summary'. How can I do that?",
        "arguments": {
          "dashboard_id": "abc123def456ghi789jkl012",
          "title": "Monthly Executive Summary"
        },
        "notes": "This renames the 'Executive Summary' dashboard to 'Monthly Executive Summary'. Ideal for clarifying the dashboard's purpose or frequency."
      }
    ]
  },
  {
    "tool_id": "dashboard.resolve_dashboard_reference",
    "description": "Resolve a dashboard reference (ID or name) to a concrete dashboard ID and title.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_ref": {
          "type": "string",
          "description": "Dashboard reference to resolve. This can be either an ID or a name."
        }
      },
      "required": [
        "dashboard_ref"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What is the ID and title of the dashboard named 'Sales Overview'?",
        "arguments": {
          "dashboard_ref": "Sales Overview"
        },
        "notes": "This resolves the dashboard name 'Sales Overview' to its corresponding ID and title. Use this when you know the dashboard name but need its ID."
      },
      {
        "user_query": "Can you find the dashboard with the ID '5f8d04b2c9a123456789abcd'?",
        "arguments": {
          "dashboard_ref": "5f8d04b2c9a123456789abcd"
        },
        "notes": "This resolves the dashboard ID '5f8d04b2c9a123456789abcd' to its corresponding title. Use this when you have the ID but want to confirm its title."
      },
      {
        "user_query": "I need the details for the dashboard called 'Marketing Performance'.",
        "arguments": {
          "dashboard_ref": "Marketing Performance"
        },
        "notes": "This resolves the dashboard name 'Marketing Performance' to its ID and title. Use this when you know the dashboard name but need its metadata."
      }
    ]
  }
]