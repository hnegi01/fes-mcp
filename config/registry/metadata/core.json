[
  {
    "tool_id": "metadata.add_datasource_measure",
    "description": "Create a saved formula measure in Sisense metadata.",
    "parameters": {
      "type": "object",
      "properties": {
        "measure": {
          "type": "object",
          "description": "Measure object in Sisense metadata format (for example datasource, table, column, expression, and related fields)."
        }
      },
      "required": [
        "measure"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Add a new measure to calculate total sales for the 'sales_data' table.",
        "arguments": {
          "measure": {
            "datasource": "sales_db",
            "table": "sales_data",
            "column": "total_sales",
            "expression": "SUM(sales_amount)",
            "fields": {
              "aggregation": "sum",
              "type": "measure"
            }
          }
        },
        "notes": "This call creates a measure that calculates the total sales by summing the 'sales_amount' column in the 'sales_data' table."
      },
      {
        "user_query": "Create a measure to calculate average order value for the 'orders' table.",
        "arguments": {
          "measure": {
            "datasource": "ecommerce_db",
            "table": "orders",
            "column": "avg_order_value",
            "expression": "AVG(order_total)",
            "fields": {
              "aggregation": "average",
              "type": "measure"
            }
          }
        },
        "notes": "This call adds a measure to calculate the average order value using the 'order_total' column in the 'orders' table."
      },
      {
        "user_query": "Define a measure to count the number of customers in the 'customer_data' table.",
        "arguments": {
          "measure": {
            "datasource": "crm_db",
            "table": "customer_data",
            "column": "customer_count",
            "expression": "COUNT(customer_id)",
            "fields": {
              "aggregation": "count",
              "type": "measure"
            }
          }
        },
        "notes": "This call creates a measure to count the total number of customers based on the 'customer_id' column in the 'customer_data' table."
      }
    ]
  },
  {
    "tool_id": "metadata.get_datasource_dimensions",
    "description": "Retrieve saved filter dimensions for a datasource.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource": {
          "type": "string",
          "description": "Datasource identifier (for example datamodel title)."
        },
        "ds_full_name": {
          "type": "string",
          "description": "Full datasource name (for example ``localhost/MyModel``)."
        }
      },
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What dimensions are available for the 'SalesData' datasource?",
        "arguments": {
          "datasource": "SalesData"
        },
        "notes": "This call retrieves the saved filter dimensions for the 'SalesData' datasource. Use this when you need to understand the available dimensions for filtering or analysis."
      },
      {
        "user_query": "I need the dimensions for the datasource hosted at 'localhost/FinanceModel'.",
        "arguments": {
          "ds_full_name": "localhost/FinanceModel"
        },
        "notes": "This call fetches dimensions for the datasource identified by its full name 'localhost/FinanceModel'. Use this when the full name is required to uniquely identify the datasource."
      },
      {
        "user_query": "Can you provide dimensions for the 'MarketingAnalytics' datasource hosted at 'localhost/MarketingModel'?",
        "arguments": {
          "datasource": "MarketingAnalytics",
          "ds_full_name": "localhost/MarketingModel"
        },
        "notes": "This call retrieves dimensions for the 'MarketingAnalytics' datasource while also specifying its full name 'localhost/MarketingModel'. Use this when both identifiers are available and needed for clarity."
      }
    ]
  },
  {
    "tool_id": "metadata.get_datasource_measures",
    "description": "Retrieve saved formula measures for a datasource.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource": {
          "type": "string",
          "description": "Datasource identifier (for example datamodel title)."
        },
        "ds_full_name": {
          "type": "string",
          "description": "Full datasource name (for example ``localhost/MyModel``)."
        }
      },
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What measures are available in the Sales datasource?",
        "arguments": {
          "datasource": "Sales",
          "ds_full_name": null
        },
        "notes": "This call retrieves all saved formula measures for the 'Sales' datasource using its identifier."
      },
      {
        "user_query": "Can you show me the measures for the datasource hosted at localhost/MyModel?",
        "arguments": {
          "datasource": null,
          "ds_full_name": "localhost/MyModel"
        },
        "notes": "This call fetches measures for the datasource using its full name 'localhost/MyModel'."
      },
      {
        "user_query": "I need to check the measures for the Marketing datasource hosted at localhost/MarketingModel.",
        "arguments": {
          "datasource": "Marketing",
          "ds_full_name": "localhost/MarketingModel"
        },
        "notes": "This call retrieves measures for the 'Marketing' datasource using both its identifier and full name."
      }
    ]
  },
  {
    "tool_id": "metadata.get_datasources",
    "description": "Retrieve all datasources visible to the authenticated user.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What datasources can I access in my Sisense environment?",
        "arguments": {},
        "notes": "This call retrieves a list of all datasources visible to the authenticated user. Use it to get an overview of available data connections."
      },
      {
        "user_query": "Show me all the datasources I can use to build dashboards.",
        "arguments": {},
        "notes": "This call is useful for identifying datasources you have permission to access, which can then be used for creating or updating dashboards."
      },
      {
        "user_query": "List all available datasources for my account.",
        "arguments": {},
        "notes": "This call provides a complete list of datasources accessible to the current user, helping to ensure you are aware of all available data connections."
      }
    ]
  },
  {
    "tool_id": "metadata.post_metadata_query",
    "description": "Execute a metadata query against Sisense.",
    "parameters": {
      "type": "object",
      "properties": {
        "query_payload": {
          "type": "object",
          "description": "Metadata query body as required by the Sisense API."
        }
      },
      "required": [
        "query_payload"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "Retrieve metadata for the 'sales_dashboard' object.",
        "arguments": {
          "query_payload": {
            "objectType": "dashboard",
            "objectId": "sales_dashboard"
          }
        },
        "notes": "This call retrieves metadata for the specified dashboard object, identified by its ID."
      },
      {
        "user_query": "Fetch metadata for all widgets in the 'finance_dashboard'.",
        "arguments": {
          "query_payload": {
            "objectType": "widget",
            "parentObjectId": "finance_dashboard"
          }
        },
        "notes": "This call retrieves metadata for all widgets associated with the specified dashboard."
      },
      {
        "user_query": "Get metadata for the 'customer_data_model' data model.",
        "arguments": {
          "query_payload": {
            "objectType": "data_model",
            "objectId": "customer_data_model"
          }
        },
        "notes": "This call retrieves metadata for the specified data model, useful for understanding its structure and properties."
      }
    ]
  }
]