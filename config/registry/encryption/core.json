[
  {
    "tool_id": "encryption.decrypt",
    "description": "Decrypt a value using the Sisense encryption service.",
    "parameters": {
      "type": "object",
      "properties": {
        "payload": {
          "type": "object",
          "description": "Decryption request body (typically includes a ``value`` field with the ciphertext to decrypt)."
        }
      },
      "required": [
        "payload"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How can I decrypt the encrypted database password stored in my datamodel?",
        "arguments": {
          "payload": {
            "value": "ENCRYPTED_PASSWORD_12345"
          }
        },
        "notes": "This call decrypts the encrypted database password stored in the datamodel, allowing the user to retrieve the plaintext password for further use."
      },
      {
        "user_query": "I need to decrypt an API key that was encrypted before saving it to the system. How can I do that?",
        "arguments": {
          "payload": {
            "value": "ENCRYPTED_API_KEY_67890"
          }
        },
        "notes": "This call decrypts an encrypted API key, enabling the user to retrieve the original API key for integration purposes."
      },
      {
        "user_query": "How do I decrypt a token stored in the configuration file for my Sisense plugin?",
        "arguments": {
          "payload": {
            "value": "ENCRYPTED_TOKEN_ABCDE"
          }
        },
        "notes": "This call decrypts an encrypted token from a configuration file, allowing the user to access the original token for plugin authentication."
      }
    ]
  },
  {
    "tool_id": "encryption.encrypt",
    "description": "Encrypt a value using the Sisense encryption service.",
    "parameters": {
      "type": "object",
      "properties": {
        "payload": {
          "type": "object",
          "description": "Encryption request body (typically includes a ``value`` field with the plaintext to encrypt)."
        }
      },
      "required": [
        "payload"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How can I encrypt a database password before saving it in Sisense?",
        "arguments": {
          "payload": {
            "value": "my_secure_password_123"
          }
        },
        "notes": "This call encrypts the provided database password so it can be securely stored in Sisense configuration."
      },
      {
        "user_query": "I need to encrypt an API key for use in a cross-server data model import. How do I do that?",
        "arguments": {
          "payload": {
            "value": "api_key_abc123xyz"
          }
        },
        "notes": "This call encrypts the API key, ensuring it is safe to use during cross-server data model imports."
      },
      {
        "user_query": "What is the process to encrypt sensitive connection parameters for a new data source?",
        "arguments": {
          "payload": {
            "value": "connection_string=Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;"
          }
        },
        "notes": "This call encrypts a sensitive connection string, preparing it for secure storage or transmission."
      }
    ]
  }
]