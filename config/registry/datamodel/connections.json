[
  {
    "tool_id": "datamodel.create_connections",
    "description": "Create a new connection using the provided payload.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_payload": {
          "type": "object",
          "description": "The configuration payload for the connection. Canonical fields include ``provider``, ``name``, ``description``, ``parameters``, ``enabled``, ``createdByUser``, and ``supportedModelTypes``."
        }
      },
      "required": [
        "connection_payload"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How can I create a new connection for a PostgreSQL database?",
        "arguments": {
          "connection_payload": {
            "provider": "postgresql",
            "name": "SalesDB Connection",
            "description": "Connection to the Sales database for analytics.",
            "parameters": {
              "host": "sales-db.example.com",
              "port": 5432,
              "database": "sales",
              "username": "analytics_user",
              "password": "securepassword123"
            },
            "enabled": true,
            "createdByUser": "admin_user",
            "supportedModelTypes": [
              "live",
              "elasticube"
            ]
          }
        },
        "notes": "This creates a connection to a PostgreSQL database named 'sales' hosted on 'sales-db.example.com'. Use this when setting up a new data source for analytics."
      },
      {
        "user_query": "I need to add a connection for a Google BigQuery project. How can I do that?",
        "arguments": {
          "connection_payload": {
            "provider": "bigquery",
            "name": "Marketing Analytics",
            "description": "BigQuery connection for marketing data.",
            "parameters": {
              "projectId": "marketing-project-123",
              "credentials": {
                "type": "service_account",
                "client_email": "service-account@marketing-project-123.iam.gserviceaccount.com",
                "private_key": "-----BEGIN PRIVATE KEY-----\\nMIIEv...\\n-----END PRIVATE KEY-----\\n"
              }
            },
            "enabled": true,
            "createdByUser": "data_engineer",
            "supportedModelTypes": [
              "live"
            ]
          }
        },
        "notes": "This sets up a connection to a Google BigQuery project for live data analysis. Use this for integrating marketing data stored in BigQuery."
      },
      {
        "user_query": "Can I create a connection to an Amazon Redshift cluster?",
        "arguments": {
          "connection_payload": {
            "provider": "redshift",
            "name": "Redshift Cluster Connection",
            "description": "Connection to the main Redshift cluster for company data.",
            "parameters": {
              "host": "redshift-cluster.example.com",
              "port": 5439,
              "database": "company_data",
              "username": "redshift_user",
              "password": "anothersecurepassword456"
            },
            "enabled": false,
            "createdByUser": "sysadmin",
            "supportedModelTypes": [
              "elasticube"
            ]
          }
        },
        "notes": "This creates a connection to an Amazon Redshift cluster but keeps it disabled initially. Use this when preparing a connection for future use or testing."
      }
    ]
  },
  {
    "tool_id": "datamodel.generate_connections_payload",
    "description": "Generate a connection payload for a given data source type.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource_type": {
          "type": "string",
          "description": "Type of data source. One of ``\"Athena\"``, ``\"RedShift\"``, ``\"BigQuery\"``, ``\"DataBricks\"`` (case-insensitive)."
        },
        "connection_params": {
          "type": "object",
          "description": "Connection details. Supported keys depend on ``datasource_type``: - Athena: ``name`` (required), ``region`` (required), ``s3_output_location`` (required), ``aws_access_key`` (required), ``aws_secret_key`` (required), ``description``, ``schema``, ``additional_parameters``. - DataBricks: ``name`` (required), ``connection_string`` (required), ``token`` (required), ``description``, ``use_dynamic_schema``, ``schema``. - BigQuery: ``name`` (required), ``service_account_key_path`` (required), ``description``, ``use_service_account``, ``use_proxy_server``, ``use_dynamic_schema``, ``record_field_flattening_level``, ``unnest_arrays``, ``allow_large_results``, ``use_storage_api``, ``additional_parameters``, ``database``. - RedShift: ``server`` (required), ``username`` (required), ``password`` (required), ``name``, ``description``, ``default_database``, ``additional_parameters``."
        }
      },
      "required": [
        "datasource_type",
        "connection_params"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How do I create a connection payload for an Athena data source?",
        "arguments": {
          "datasource_type": "Athena",
          "connection_params": {
            "name": "Athena_Connection_1",
            "region": "us-west-2",
            "s3_output_location": "s3://athena-query-results/",
            "aws_access_key": "AKIAEXAMPLE",
            "aws_secret_key": "SECRETEXAMPLE",
            "description": "Connection to Athena for analytics",
            "schema": "default",
            "additional_parameters": {
              "workgroup": "primary"
            }
          }
        },
        "notes": "This call generates a connection payload for an Athena data source, specifying the required AWS credentials and S3 output location."
      },
      {
        "user_query": "I need to set up a connection payload for a BigQuery data source. How can I do that?",
        "arguments": {
          "datasource_type": "BigQuery",
          "connection_params": {
            "name": "BigQuery_Analytics",
            "service_account_key_path": "/path/to/service_account.json",
            "description": "BigQuery connection for marketing data",
            "use_service_account": true,
            "use_dynamic_schema": false,
            "record_field_flattening_level": "none",
            "unnest_arrays": true,
            "allow_large_results": true,
            "database": "marketing_db"
          }
        },
        "notes": "This call creates a connection payload for a BigQuery data source, using a service account key and enabling array unnesting for query results."
      },
      {
        "user_query": "Can you help me generate a connection payload for a RedShift data source?",
        "arguments": {
          "datasource_type": "RedShift",
          "connection_params": {
            "server": "redshift-cluster-1.example.com",
            "username": "admin",
            "password": "securepassword123",
            "name": "RedShift_Cluster_Connection",
            "description": "Connection to RedShift cluster for sales data",
            "default_database": "sales_db",
            "additional_parameters": {
              "ssl": true
            }
          }
        },
        "notes": "This call generates a connection payload for a RedShift data source, including the server address, credentials, and default database."
      }
    ]
  },
  {
    "tool_id": "datamodel.get_connection",
    "description": "Retrieve connections matching a name.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_name": {
          "type": "string",
          "description": "Name of the connection to filter by."
        }
      },
      "required": [
        "connection_name"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "Find the connection details for the 'Salesforce_Prod' data source.",
        "arguments": {
          "connection_name": "Salesforce_Prod"
        },
        "notes": "This call retrieves the connection object for the Salesforce production environment. Use this to confirm the connection details or validate its existence."
      },
      {
        "user_query": "What connection is configured for the 'GoogleAnalytics_Main' data source?",
        "arguments": {
          "connection_name": "GoogleAnalytics_Main"
        },
        "notes": "This call checks if a connection named 'GoogleAnalytics_Main' exists and retrieves its details. Useful for troubleshooting or auditing connections."
      },
      {
        "user_query": "Get the connection information for 'Redshift_Cluster_A'.",
        "arguments": {
          "connection_name": "Redshift_Cluster_A"
        },
        "notes": "This call fetches the connection object for the specified Redshift cluster. Use this to verify the connection's configuration or status."
      }
    ]
  },
  {
    "tool_id": "datamodel.get_connections",
    "description": "Retrieve all connections.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "How can I see all the connections available in my Sisense environment?",
        "arguments": {},
        "notes": "This call retrieves a list of all connections configured in the Sisense instance. Use it to review available data sources."
      },
      {
        "user_query": "I need to check which data connections are currently set up in the system.",
        "arguments": {},
        "notes": "This call provides a complete list of data connections, useful for auditing or verifying existing configurations."
      },
      {
        "user_query": "Can I get a list of all the data connections in Sisense?",
        "arguments": {},
        "notes": "This call returns all configured connections, helping users understand the data sources linked to their Sisense instance."
      }
    ]
  },
  {
    "tool_id": "datamodel.get_table_schema",
    "description": "Retrieve the schema of a table within a connection's data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_name": {
          "type": "string",
          "description": "Name of the connection."
        },
        "database_name": {
          "type": "string",
          "description": "Name of the database (sent as ``Database``)."
        },
        "schema_name": {
          "type": "string",
          "description": "Name of the schema (sent as ``schema``)."
        },
        "table_name": {
          "type": "string",
          "description": "Name of the table (sent as ``table``)."
        }
      },
      "required": [
        "connection_name",
        "database_name",
        "schema_name",
        "table_name"
      ]
    },
    "mutates": false,
    "examples": [
      {
        "user_query": "What is the schema of the 'sales_data' table in the 'analytics_db' database?",
        "arguments": {
          "connection_name": "primary_data_source",
          "database_name": "analytics_db",
          "schema_name": "public",
          "table_name": "sales_data"
        },
        "notes": "This call retrieves the schema details for the 'sales_data' table in the 'analytics_db' database using the 'primary_data_source' connection."
      },
      {
        "user_query": "Can you show me the schema for the 'customer_info' table in the 'crm_db' database?",
        "arguments": {
          "connection_name": "crm_connection",
          "database_name": "crm_db",
          "schema_name": "customer_data",
          "table_name": "customer_info"
        },
        "notes": "This call fetches the schema for the 'customer_info' table located in the 'customer_data' schema of the 'crm_db' database using the 'crm_connection' connection."
      },
      {
        "user_query": "I need the schema for the 'inventory' table in the 'warehouse_db' database.",
        "arguments": {
          "connection_name": "warehouse_connection",
          "database_name": "warehouse_db",
          "schema_name": "inventory_schema",
          "table_name": "inventory"
        },
        "notes": "This call retrieves the schema details for the 'inventory' table in the 'inventory_schema' schema of the 'warehouse_db' database using the 'warehouse_connection' connection."
      }
    ]
  },
  {
    "tool_id": "datamodel.update_connection",
    "description": "Update an existing connection.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_id": {
          "type": "string",
          "description": "Connection ``oid`` to update."
        },
        "connection_data": {
          "type": "object",
          "description": "Fields to update (for example ``name``, ``parameters``, ``provider``). Supported keys depend on the Sisense connection type."
        }
      },
      "required": [
        "connection_id",
        "connection_data"
      ]
    },
    "mutates": true,
    "examples": [
      {
        "user_query": "How can I update the name and parameters of an existing connection?",
        "arguments": {
          "connection_id": "conn_12345",
          "connection_data": {
            "name": "Updated Connection Name",
            "parameters": {
              "host": "new-database-host.example.com",
              "port": 5432
            }
          }
        },
        "notes": "This call updates the name and connection parameters for the specified connection. Use this when migrating to a new database host."
      },
      {
        "user_query": "I need to change the provider of a connection. How do I do that?",
        "arguments": {
          "connection_id": "conn_67890",
          "connection_data": {
            "provider": "new_provider"
          }
        },
        "notes": "This call updates the provider for the specified connection. Use this when switching to a different connection provider."
      },
      {
        "user_query": "Can I update multiple fields of a connection at once?",
        "arguments": {
          "connection_id": "conn_54321",
          "connection_data": {
            "name": "New Connection Name",
            "parameters": {
              "host": "updated-host.example.com",
              "port": 3306
            },
            "provider": "mysql"
          }
        },
        "notes": "This call updates multiple fields (name, parameters, and provider) for the specified connection. Use this for comprehensive updates during a migration or reconfiguration."
      }
    ]
  }
]