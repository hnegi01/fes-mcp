[
  {
    "tool_id": "access_management.change_folder_and_dashboard_ownership",
    "module": "access_management",
    "sub_module": "access_management.ownership",
    "class": "AccessManagement",
    "method": "change_folder_and_dashboard_ownership",
    "description": "Change the ownership of folders and optionally dashboards.",
    "full_doc": "Change the ownership of folders and optionally dashboards.\n\nChanges the ownership of a target folder and the entire tree structure\nsurrounding it, including subfolders, sibling folders, and parent\nfolders. Optionally, it also changes the ownership of dashboards\nassociated with these folders.\n\nParameters\n----------\nexecuting_user : str\n    The user running the operation. This is necessary for API access\n    checks. (format: email)\nfolder_name : str\n    The target folder whose ownership needs to be changed.\nnew_owner_name : str\n    The new owner to whom the folder (and optionally dashboards)\n    ownership will be transferred. (format: email)\noriginal_owner_rule : str, optional\n    The ownership rule to set for the original owner after changing\n    ownership (``\"edit\"`` or ``\"view\"``). Default is ``\"edit\"``.\nchange_dashboard_ownership : bool, optional\n    Whether to also change the ownership of dashboards in the folder\n    tree. Default is ``True``.\n\nReturns\n-------\ndict[str, Any] | None\n    A dictionary with ``total_folders_changed`` and\n    ``total_dashboards_changed`` counts on success, ``{\"error\": \"...\"}``\n    if the executing user or new owner cannot be resolved, or ``None``\n    when there are no folders or dashboards to change.",
    "parameters": {
      "type": "object",
      "properties": {
        "executing_user": {
          "type": "string",
          "description": "The user running the operation. This is necessary for API access checks.",
          "format": "email"
        },
        "folder_name": {
          "type": "string",
          "description": "The target folder whose ownership needs to be changed."
        },
        "new_owner_name": {
          "type": "string",
          "description": "The new owner to whom the folder (and optionally dashboards) ownership will be transferred.",
          "format": "email"
        },
        "original_owner_rule": {
          "type": "string",
          "description": "The ownership rule to set for the original owner after changing ownership (``\"edit\"`` or ``\"view\"``). Default is ``\"edit\"``."
        },
        "change_dashboard_ownership": {
          "type": "boolean",
          "description": "Whether to also change the ownership of dashboards in the folder tree. Default is ``True``."
        }
      },
      "required": [
        "executing_user",
        "folder_name",
        "new_owner_name"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.change_user_password",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "change_user_password",
    "description": "Change a user's password.",
    "full_doc": "Change a user's password.\n\nSends ``PATCH /api/users/{user_id}`` with only ``password`` in the\nrequest body. Other user fields are not modified.\n\nParameters\n----------\nuser_id : str\n    Internal user ID (``_id``) to update.\npassword : str\n    New password for the user. Must not be empty.\n\nReturns\n-------\ndict[str, Any]\n    The updated user object on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "Internal user ID (``_id``) to update."
        },
        "password": {
          "type": "string",
          "description": "New password for the user. Must not be empty."
        }
      },
      "required": [
        "user_id",
        "password"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.create_groups_bulk",
    "module": "access_management",
    "sub_module": "access_management.groups",
    "class": "AccessManagement",
    "method": "create_groups_bulk",
    "description": "Create multiple groups in a single bulk request.",
    "full_doc": "Create multiple groups in a single bulk request.\n\nSends the provided group definitions to the bulk group creation\nendpoint.\n\nParameters\n----------\ngroups : list[dict[str, Any]]\n    Group definitions to create. Each dictionary should use\n    canonical Sisense group fields, at minimum ``name``.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    The list of created group objects on success, or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "groups": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Group definitions to create. Each dictionary should use canonical Sisense group fields, at minimum ``name``."
        }
      },
      "required": [
        "groups"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "groups",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.create_schedule_build",
    "module": "access_management",
    "sub_module": "access_management.admin",
    "class": "AccessManagement",
    "method": "create_schedule_build",
    "description": "Create a schedule build for a DataModel.",
    "full_doc": "Create a schedule build for a DataModel.\n\nSupports both cron-based schedules (e.g. every Monday at 9:00 UTC) and\ninterval-based schedules (e.g. every 2 days, 1 hour, 30 minutes). An\ninterval-based schedule is created when any of the ``interval_*``\nparameters are provided; otherwise a cron-based schedule is created from\n``days``, ``hour``, and ``minute``.\n\nParameters\n----------\ndatamodel_name : str\n    The name of the DataModel.\nbuild_type : str, optional\n    Type of the build, one of ``\"ACCUMULATE\"``, ``\"FULL\"``, or\n    ``\"SCHEMA_CHANGES\"``. Defaults to ``\"ACCUMULATE\"``.\ndays : list[str] | None, optional\n    Keyword-only. List of days for a cron schedule, e.g.\n    ``[\"SUN\", \"MON\", \"TUE\", \"WED\", \"THU\", \"FRI\", \"SAT\"]`` or ``[\"*\"]``\n    for all days.\nhour : int | None, optional\n    Keyword-only. Hour in 24-hour format (UTC) for a cron schedule.\nminute : int | None, optional\n    Keyword-only. Minute of the hour (UTC) for a cron schedule.\ninterval_days : int | None, optional\n    Keyword-only. Interval in days for an interval-based schedule.\ninterval_hours : int | None, optional\n    Keyword-only. Interval in hours for an interval-based schedule.\ninterval_minutes : int | None, optional\n    Keyword-only. Interval in minutes for an interval-based schedule.\n\nReturns\n-------\ndict[str, Any]\n    The API response on success, or ``{\"error\": \"...\"}`` on failure or\n    invalid schedule configuration.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "The name of the DataModel."
        },
        "build_type": {
          "type": "string",
          "description": "Type of the build, one of ``\"ACCUMULATE\"``, ``\"FULL\"``, or ``\"SCHEMA_CHANGES\"``. Defaults to ``\"ACCUMULATE\"``."
        },
        "days": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Keyword-only. List of days for a cron schedule, e.g. ``[\"SUN\", \"MON\", \"TUE\", \"WED\", \"THU\", \"FRI\", \"SAT\"]`` or ``[\"*\"]`` for all days."
        },
        "hour": {
          "type": "integer",
          "description": "Keyword-only. Hour in 24-hour format (UTC) for a cron schedule."
        },
        "minute": {
          "type": "integer",
          "description": "Keyword-only. Minute of the hour (UTC) for a cron schedule."
        },
        "interval_days": {
          "type": "integer",
          "description": "Keyword-only. Interval in days for an interval-based schedule."
        },
        "interval_hours": {
          "type": "integer",
          "description": "Keyword-only. Interval in hours for an interval-based schedule."
        },
        "interval_minutes": {
          "type": "integer",
          "description": "Keyword-only. Interval in minutes for an interval-based schedule."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.create_user",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "create_user",
    "description": "Create a new user in Sisense.",
    "full_doc": "Create a new user in Sisense.\n\nValidates that the required fields are present, then resolves the role\nname and group names to their corresponding IDs and sends a POST\nrequest to create the user. Group names in ``groups`` are resolved to\ngroup IDs.\n\nThe ``role`` field accepts either vocabulary — the raw Sisense name\n(``\"consumer\"``, ``\"super\"``, ``\"contributor\"``) or the name the UI\nshows (``\"viewer\"``, ``\"sysAdmin\"``, ``\"dashboardDesigner\"``) — and is\nmatched ignoring case, spaces and punctuation, so ``\"sys admin\"`` and\n``\"sysAdmin\"`` are equivalent. Roles the instance defines beyond these\n(for example ``dataDesigner``, ``dataAdmin``, ``admin`` or custom\nroles) are matched by their own name and are never treated as synonyms\nfor a similar-sounding role. An unmatched name returns a failure dict\nlisting the roles the instance actually has.\n\nParameters\n----------\nuser_data : CreateUserPayload\n    User details, using canonical Sisense payload field names:\n\n    - ``email`` : str — the user's email address. **Required.**\n    - ``role`` : str — role name to assign, in either vocabulary\n      (``\"viewer\"`` or ``\"consumer\"``, ``\"sysAdmin\"`` or ``\"super\"``,\n      ``\"dashboardDesigner\"``/``\"designer\"`` or ``\"contributor\"``, or\n      any other role the instance defines); resolved to ``roleId``.\n      **Required.**\n    - ``userName`` : str — the user's login name (optional).\n    - ``firstName`` : str — the user's first name (optional).\n    - ``lastName`` : str — the user's last name (optional).\n    - ``groups`` : list[str] — group names to assign, resolved to IDs\n      (optional).\n    - ``password`` : str — initial password; if omitted, the user\n      receives an email to set one (optional).\n    - ``preferences`` : dict — user preference settings (optional).\n\nReturns\n-------\ndict[str, Any]\n    The created user object returned by the API if successful, or a\n    dictionary with an ``error`` key if the operation fails. Missing\n    required fields are rejected up front, before any API call.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_data": {
          "type": "object",
          "properties": {
            "email": {
              "type": "string"
            },
            "role": {
              "type": "string"
            },
            "userName": {
              "type": "string"
            },
            "firstName": {
              "type": "string"
            },
            "lastName": {
              "type": "string"
            },
            "groups": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "password": {
              "type": "string"
            },
            "preferences": {
              "type": "object"
            }
          },
          "required": [
            "email",
            "role"
          ],
          "additionalProperties": true,
          "description": "User details, using canonical Sisense payload field names: - ``email`` : str — the user's email address. **Required.** - ``role`` : str — role name to assign, in either vocabulary (``\"viewer\"`` or ``\"consumer\"``, ``\"sysAdmin\"`` or ``\"super\"``, ``\"dashboardDesigner\"``/``\"designer\"`` or ``\"contributor\"``, or any other role the instance defines); resolved to ``roleId``. **Required.** - ``userName`` : str — the user's login name (optional). - ``firstName`` : str — the user's first name (optional). - ``lastName`` : str — the user's last name (optional). - ``groups`` : list[str] — group names to assign, resolved to IDs (optional). - ``password`` : str — initial password; if omitted, the user receives an email to set one (optional). - ``preferences`` : dict — user preference settings (optional)."
        }
      },
      "required": [
        "user_data"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.create_users_bulk",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "create_users_bulk",
    "description": "Create multiple users in a single bulk request.",
    "full_doc": "Create multiple users in a single bulk request.\n\nSends the provided user definitions to the bulk user creation\nendpoint. Each entry must already carry a resolved ``roleId`` and\n``groups`` (list of group IDs) — no name-to-ID resolution is\nperformed by this method.\n\nParameters\n----------\nusers : list[dict[str, Any]]\n    User definitions to create. Each dictionary should use canonical\n    Sisense user fields (at minimum ``email``, ``firstName``, and\n    ``roleId``).\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    The list of created user objects on success, or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "users": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "User definitions to create. Each dictionary should use canonical Sisense user fields (at minimum ``email``, ``firstName``, and ``roleId``)."
        }
      },
      "required": [
        "users"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.delete_group",
    "module": "access_management",
    "sub_module": "access_management.groups",
    "class": "AccessManagement",
    "method": "delete_group",
    "description": "Delete a group by ID.",
    "full_doc": "Delete a group by ID.\n\nSends a DELETE request to remove the group from the Sisense server.\n\nParameters\n----------\ngroup_id : str\n    The ID of the group to delete.\n\nReturns\n-------\ndict[str, Any]\n    A success message dict if successful, or ``{\"error\": \"...\"}``\n    on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "group_id": {
          "type": "string",
          "description": "The ID of the group to delete."
        }
      },
      "required": [
        "group_id"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "groups",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.delete_user",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "delete_user",
    "description": "Delete a user by their email (username).",
    "full_doc": "Delete a user by their email (username).\n\nResolves the user by email/username and sends a DELETE request to remove\nthe account.\n\nParameters\n----------\nuser_name : str\n    The email or username of the user to be deleted. (format: email)\n\nReturns\n-------\ndict[str, Any]\n    A success message dict from the API if successful, or an\n    ``{\"error\": \"...\"}`` dict on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_name": {
          "type": "string",
          "description": "The email or username of the user to be deleted.",
          "format": "email"
        }
      },
      "required": [
        "user_name"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_all_dashboard_shares",
    "module": "access_management",
    "sub_module": "access_management.admin",
    "class": "AccessManagement",
    "method": "get_all_dashboard_shares",
    "description": "Retrieve all dashboard shares, including user and group details for each shared dashboard.",
    "full_doc": "Retrieve all dashboard shares, including user and group details for each shared dashboard.\n\nUses pagination to retrieve all dashboards and their share information,\nand collects the corresponding user and group details for each share.\n\nReturns\n-------\nlist[dict[str, Any]]\n    A list of dictionaries containing the dashboard title, share type\n    (``user`` or ``group``), and share name (email or group name) — one\n    row per share, so the row count equals the number of shares.\n    Dashboards with no shares contribute no rows. An empty list is\n    returned if users or groups cannot be fetched.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "access_management",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_datamodel_columns",
    "module": "access_management",
    "sub_module": "access_management.columns",
    "class": "AccessManagement",
    "method": "get_datamodel_columns",
    "description": "Retrieve columns from a DataModel by collecting them from its datasets and tables.",
    "full_doc": "Retrieve columns from a DataModel by collecting them from its datasets and tables.\n\nResolves the DataModel ID by title, then walks its datasets and tables\nto gather every column.\n\nParameters\n----------\ndatamodel_name : str\n    The name of the DataModel from which to extract columns.\n\nReturns\n-------\nlist[dict[str, Any]]\n    A list of dictionaries, each containing ``datamodel_id``,\n    ``datamodel_name``, ``table``, and ``column``. An empty list is\n    returned if the DataModel cannot be found or has no columns.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "The name of the DataModel from which to extract columns."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "access_management",
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_groups",
    "module": "access_management",
    "sub_module": "access_management.groups",
    "class": "AccessManagement",
    "method": "get_groups",
    "description": "Retrieve groups — one named group, or all of them.",
    "full_doc": "Retrieve groups — one named group, or all of them.\n\nFetches the groups defined on the Sisense server. With ``name`` the\nAPI filters server-side to that group; without it, every group is\nreturned. One row per group.\n\nParameters\n----------\nname : str or None, optional\n    Group name to filter by. Omit for all groups.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of raw group objects as returned by the API (each with\n    ``_id``, ``name``, ``defaultRole``, and related fields). Without\n    ``name``, an empty list means the server genuinely has no groups.\n    With ``name``, the filter is an exact-match lookup — an unknown\n    name returns ``{\"ok\": False, \"error\": \"...\"}`` naming it, never\n    an empty list. Returns the standard error dict on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Group name to filter by. Omit for all groups."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "access_management",
      "groups",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_my_user",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "get_my_user",
    "description": "Retrieve the currently logged-in user for the API token.",
    "full_doc": "Retrieve the currently logged-in user for the API token.\n\nSends ``GET /api/users/loggedin``. Use this to resolve migration user\nidentity (email, username, internal ID) for the authenticated admin\ntoken.\n\nReturns\n-------\ndict[str, Any]\n    The logged-in user object from the API, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "access_management",
      "users",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_roles",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "get_roles",
    "description": "Retrieve all Sisense roles.",
    "full_doc": "Retrieve all Sisense roles.\n\nSends ``GET /api/roles``. Returns the raw role list used to build role\nname-to-ID maps (for example in multi-tenant migration workflows).\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of role objects on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "access_management",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_tenants",
    "module": "access_management",
    "sub_module": "access_management.tenants",
    "class": "AccessManagement",
    "method": "get_tenants",
    "description": "Retrieve all tenants.",
    "full_doc": "Retrieve all tenants.\n\nFetches the full list of tenants defined on the Sisense server. Only\nmeaningful on multi-tenant deployments.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of raw tenant objects, or ``{\"error\": \"...\"}`` if\n    retrieval fails (for example, on a single-tenant deployment\n    where the tenants endpoint is unavailable).",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "access_management",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_unused_columns_bulk",
    "module": "access_management",
    "sub_module": "access_management.columns",
    "class": "AccessManagement",
    "method": "get_unused_columns_bulk",
    "description": "Run unused-column analysis for one or more data models and return a",
    "full_doc": "Run unused-column analysis for one or more data models and return a\ncombined per-model outcome.\n\nParameters\n----------\ndatamodels : str or list of str\n    One or more data model references to analyze. **Required.** Each\n    reference can be:\n      - a data model ID, or\n      - a data model title (name).\n    At runtime this parameter is tolerant of a single string and will\n    normalize it to a one-element list.\n\nReturns\n-------\ndict[str, Any]\n    Always a dict with ``\"results\"`` and ``\"errors\"``:\n      - ``\"results\"``: flat list of column rows across all processed\n        data models, each row shaped as ``get_datamodel_columns`` rows\n        plus a ``\"used\"`` boolean. A model that resolves and genuinely\n        has no columns in use contributes rows with ``used: False``.\n      - ``\"errors\"``: list of ``{\"ref\": ..., \"error\": ...}`` entries,\n        one per reference that could not be resolved or processed —\n        empty when every reference succeeded.\n    When **none** of the given references can be processed (or the\n    input is invalid), the dict additionally carries ``\"ok\": False``\n    and a top-level ``\"error\"`` summary, so standard failure detection\n    (``payload.get(\"ok\") is False``) still fires.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more data model references to analyze. **Required.** Each reference can be: - a data model ID, or - a data model title (name). At runtime this parameter is tolerant of a single string and will normalize it to a one-element list."
        }
      },
      "required": [
        "datamodels"
      ]
    },
    "mutates": false,
    "tags": [
      "access_management",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_user",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "get_user",
    "description": "Retrieve one named user by email address, in the canonical user row shape.",
    "full_doc": "Retrieve one named user by email address, in the canonical user row shape.\n\nFetches users with expanded ``groups`` and ``role`` data and returns the\nrecord matching the provided email address.\n\nChanged in 2.0: ``ROLE_NAME`` held the display name in 1.x (that value\nis now ``ROLE_DISPLAY_NAME``); ``GROUPS`` still holds the group names\nand is joined by the new ``GROUP_IDS``. See ``docs/upgrading.md``.\n\nParameters\n----------\nuser_email : str\n    Email address of the user to retrieve. **Required** — this method\n    always answers \"one named user\"; use ``get_users_all`` for every\n    user. (format: email)\n\nReturns\n-------\ndict[str, Any]\n    The canonical user row: ``USER_ID``, ``USER_NAME``, ``EMAIL``,\n    ``FIRST_NAME``, ``LAST_NAME``, ``IS_ACTIVE``, ``ROLE_ID``,\n    ``ROLE_NAME`` (the raw Sisense value, e.g. ``\"consumer\"``),\n    ``ROLE_DISPLAY_NAME`` (the name the Sisense UI shows, e.g.\n    ``\"viewer\"``), ``GROUP_IDS``, and ``GROUPS`` (unfiltered —\n    includes ``Everyone``). Returns ``{\"error\": \"...\"}`` when the user\n    is not found or the API call fails.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_email": {
          "type": "string",
          "description": "Email address of the user to retrieve. **Required** — this method always answers \"one named user\"; use ``get_users_all`` for every user.",
          "format": "email"
        }
      },
      "required": [
        "user_email"
      ]
    },
    "mutates": false,
    "tags": [
      "access_management",
      "users",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.get_users_all",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "get_users_all",
    "description": "Retrieve every user, one canonical user row each.",
    "full_doc": "Retrieve every user, one canonical user row each.\n\nReports exactly what Sisense stores: group memberships are unfiltered\n(``Everyone`` is included — consumers that want to hide a universal\ngroup can drop it; a consumer that never received it cannot put it\nback), and ``ROLE_NAME`` carries the raw Sisense value with the\nUI-facing name in ``ROLE_DISPLAY_NAME``.\n\nChanged in 2.0: ``ROLE_NAME`` held the display name in 1.x (that value\nis now ``ROLE_DISPLAY_NAME``), ``GROUPS`` is joined by the new\n``GROUP_IDS``, and ``Everyone`` is no longer filtered out of\n``GROUPS``. See ``docs/upgrading.md``.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    One row per user, each with ``USER_ID``, ``USER_NAME``, ``EMAIL``,\n    ``FIRST_NAME``, ``LAST_NAME``, ``IS_ACTIVE``, ``ROLE_ID``,\n    ``ROLE_NAME`` (raw, e.g. ``\"consumer\"``), ``ROLE_DISPLAY_NAME``\n    (UI name, e.g. ``\"viewer\"``), ``GROUP_IDS``, and ``GROUPS``\n    (unfiltered). Returns ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "access_management",
      "users",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.update_user",
    "module": "access_management",
    "sub_module": "access_management.users",
    "class": "AccessManagement",
    "method": "update_user",
    "description": "Update an existing Sisense user identified by their email address.",
    "full_doc": "Update an existing Sisense user identified by their email address.\n\nThis method finds the user by email and performs a partial update (PATCH).\nAll update fields MUST be provided inside the ``user_data`` dictionary. Do not\npass update fields at the top level.\n\nParameters\n----------\nuser_email : str\n    Email address of the user to update (used to locate the user). (format: email)\nuser_data : UpdateUserPayload\n    Dictionary of fields to update. Only include fields you want to change.\n\n    Supported fields\n    ----------------\n    - email : str\n        Update the user's email address.\n    - userName : str\n        Update the user's username/login name.\n    - firstName : str\n        Update the user's first name.\n    - lastName : str\n        Update the user's last name.\n    - role : str\n        Role name in either vocabulary — raw (``\"consumer\"``, ``\"super\"``,\n        ``\"contributor\"``), the UI name (``\"viewer\"``, ``\"sysAdmin\"``,\n        ``\"dashboardDesigner\"``), or any other role the instance defines\n        (``dataDesigner``, ``dataAdmin``, custom roles). Matched ignoring\n        case, spaces and punctuation. This is resolved to ``roleId`` before\n        sending the API request.\n    - groups : list[str]\n        List of group names to apply. Group names are resolved to group IDs before\n        sending the API request. If ``groups`` is explicitly provided as an empty\n        list (``[]``), group memberships are cleared (tenant defaults may still apply).\n\nReturns\n-------\ndict[str, Any]\n    The updated user payload when successful. If the operation fails, returns a\n    dictionary with an ``error`` key.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_email": {
          "type": "string",
          "description": "Email address of the user to update (used to locate the user).",
          "format": "email"
        },
        "user_data": {
          "type": "object",
          "properties": {
            "email": {
              "type": "string"
            },
            "userName": {
              "type": "string"
            },
            "firstName": {
              "type": "string"
            },
            "lastName": {
              "type": "string"
            },
            "role": {
              "type": "string"
            },
            "groups": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "password": {
              "type": "string"
            },
            "preferences": {
              "type": "object"
            }
          },
          "required": [],
          "additionalProperties": true,
          "description": "Dictionary of fields to update. Only include fields you want to change. Supported fields ---------------- - email : str Update the user's email address. - userName : str Update the user's username/login name. - firstName : str Update the user's first name. - lastName : str Update the user's last name. - role : str Role name in either vocabulary — raw (``\"consumer\"``, ``\"super\"``, ``\"contributor\"``), the UI name (``\"viewer\"``, ``\"sysAdmin\"``, ``\"dashboardDesigner\"``), or any other role the instance defines (``dataDesigner``, ``dataAdmin``, custom roles). Matched ignoring case, spaces and punctuation. This is resolved to ``roleId`` before sending the API request. - groups : list[str] List of group names to apply. Group names are resolved to group IDs before sending the API request. If ``groups`` is explicitly provided as an empty list (``[]``), group memberships are cleared (tenant defaults may still apply)."
        }
      },
      "required": [
        "user_email",
        "user_data"
      ]
    },
    "mutates": true,
    "tags": [
      "access_management",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "access_management.users_per_group",
    "module": "access_management",
    "sub_module": "access_management.groups",
    "class": "AccessManagement",
    "method": "users_per_group",
    "description": "Retrieve group memberships — one group's members, or every membership.",
    "full_doc": "Retrieve group memberships — one group's members, or every membership.\n\nReturns one flat row per (group, user) membership. With ``group_name``\nthe rows are that group's members; without it, every membership on the\ninstance is returned (ask ``get_groups`` for the per-group view).\n``Everyone`` and ``All users in system`` are omitted from the\nall-groups view: Sisense fills both with every user, so they restate\n``get_users_all`` rather than describing group structure. Naming\neither one still returns its members.\n\nThe rule across the SDK: **targeted questions give complete answers;\nonly the all-groups view filters.** So ``users_per_group(\"Everyone\")``\nand ``get_user(email)[\"GROUPS\"]`` both report every group a user is\nin, while ``users_per_group()`` omits the two universal ones. Do not\nderive one person's groups from the all-groups view — it will be\nmissing those two. Use :meth:`get_user` for that.\n\nMembership is read from the **group** side\n(``GET /api/v1/groups?expand=users``), which is the same source the\nSisense UI shows. Sisense resolves the auto-generated groups\n(``Admins``, ``All users in system``) on the group side only — their\nmembers do not appear in any user's own ``groups`` field — so reading\nfrom the user side would report them as empty while the UI shows\nmembers.\n\nParameters\n----------\ngroup_name : str or None, optional\n    The name of the group whose members to list. Omit for all\n    memberships. A name that matches no group returns\n    ``{\"error\": \"...\"}`` naming it — never a silent empty list.\n    Naming ``Everyone`` or ``All users in system`` returns their\n    members — an explicit request is always honored, even though the\n    all-groups view omits them.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    One row per (group, user) membership, each with ``GROUP_ID``,\n    ``GROUP_NAME``, ``USER_ID``, ``USER_NAME``, ``EMAIL``,\n    ``FIRST_NAME``, ``LAST_NAME``, ``IS_ACTIVE``, ``ROLE_ID``,\n    ``ROLE_NAME`` and ``ROLE_DISPLAY_NAME`` (both the name the Sisense\n    UI shows), and ``ROLE_RAW_NAME`` (the raw Sisense value). A group\n    with no members contributes no rows, so the row count always equals\n    the real membership count.\n    Returns ``{\"error\": \"...\"}`` on failure or unknown ``group_name``.",
    "parameters": {
      "type": "object",
      "properties": {
        "group_name": {
          "type": "string",
          "description": "The name of the group whose members to list. Omit for all memberships. A name that matches no group returns ``{\"error\": \"...\"}`` naming it — never a silent empty list. Naming ``Everyone`` or ``All users in system`` returns their members — an explicit request is always honored, even though the all-groups view omits them."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "access_management",
      "users",
      "groups",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "blox.delete_blox_action",
    "module": "blox",
    "sub_module": "blox.core",
    "class": "Blox",
    "method": "delete_blox_action",
    "description": "Delete a custom Blox action from the Sisense instance.",
    "full_doc": "Delete a custom Blox action from the Sisense instance.\n\nSends the action type to ``POST /api/v1/blox/deleteCustomAction``.\nThis endpoint is supported on Linux deployments only.\n\nParameters\n----------\naction_type : str\n    The ``type`` identifier of the Blox action to delete.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True}`` on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "action_type": {
          "type": "string",
          "description": "The ``type`` identifier of the Blox action to delete."
        }
      },
      "required": [
        "action_type"
      ]
    },
    "mutates": true,
    "tags": [
      "blox",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "blox.get_blox_actions",
    "module": "blox",
    "sub_module": "blox.core",
    "class": "Blox",
    "method": "get_blox_actions",
    "description": "Retrieve all custom Blox actions from the Sisense instance.",
    "full_doc": "Retrieve all custom Blox actions from the Sisense instance.\n\nThe endpoint used varies by the ``operating_system`` set on the\n``SisenseClient``:\n\n- ``\"linux\"``: ``GET /api/v1/blox/getCustomActions``\n- ``\"windows\"``: ``GET /api/v1/getCustomActions/actions``\n\nReturns\n-------\nlist[dict[str, Any]]\n    A list of Blox action objects, or ``[{\"error\": \"...\"}]`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "blox",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "blox.get_blox_widget_style",
    "module": "blox",
    "sub_module": "blox.widgets",
    "class": "Blox",
    "method": "get_blox_widget_style",
    "description": "Retrieve the style objects of a BloX widget.",
    "full_doc": "Retrieve the style objects of a BloX widget.\n\nFetches the widget and returns its ``style.currentCard`` and\n``style.currentConfig`` objects. ``currentCard`` holds the BloX card\ndefinition (body, actions, the ``style`` CSS string, and so on) and\n``currentConfig`` holds the widget configuration (``fontFamily``,\n``fontSizes``, and so on). Returns ``{\"error\": \"...\"}`` if the widget\nis not a BloX type.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard that contains the widget.\nwidget_id : str\n    The ``oid`` of the BloX widget.\nadmin_access : bool, optional\n    When ``True`` (default), appends ``?adminAccess=true`` to the request,\n    allowing access to dashboards the API token user does not own.\n\nReturns\n-------\ndict[str, Any]\n    A dictionary with:\n\n    - ``currentCard`` (dict): The widget's ``style.currentCard`` object.\n    - ``currentConfig`` (dict): The widget's ``style.currentConfig`` object.\n\n    Returns ``{\"error\": \"...\"}`` on failure or if the widget type is not ``\"BloX\"``.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard that contains the widget."
        },
        "widget_id": {
          "type": "string",
          "description": "The ``oid`` of the BloX widget."
        },
        "admin_access": {
          "type": "boolean",
          "description": "When ``True`` (default), appends ``?adminAccess=true`` to the request, allowing access to dashboards the API token user does not own."
        }
      },
      "required": [
        "dashboard_id",
        "widget_id"
      ]
    },
    "mutates": false,
    "tags": [
      "blox",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "blox.save_blox_action",
    "module": "blox",
    "sub_module": "blox.core",
    "class": "Blox",
    "method": "save_blox_action",
    "description": "Save a custom Blox action on the Sisense instance.",
    "full_doc": "Save a custom Blox action on the Sisense instance.\n\nSends the action payload to ``POST /api/v1/blox/saveCustomAction``.\nThis endpoint is supported on Linux deployments only.\n\nParameters\n----------\naction : dict[str, Any]\n    A Blox action object.\n\nReturns\n-------\ndict[str, Any]\n    The API response body on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "object",
          "description": "A Blox action object."
        }
      },
      "required": [
        "action"
      ]
    },
    "mutates": true,
    "tags": [
      "blox",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "blox.update_blox_widget_style",
    "module": "blox",
    "sub_module": "blox.widgets",
    "class": "Blox",
    "method": "update_blox_widget_style",
    "description": "Update the style objects of a BloX widget.",
    "full_doc": "Update the style objects of a BloX widget.\n\nReads the current widget, replaces its ``style.currentCard`` and/or\n``style.currentConfig`` objects with the provided values, and writes\nthe result back via ``PUT /api/dashboards/{dashboard_id}/widgets/{widget_id}``.\nServer-managed fields are stripped before the write.\n\nThe typical flow is read-modify-write: fetch the objects with\n:meth:`get_blox_widget_style`, change the fields you need (for example\nthe ``style`` CSS string on the card, or ``fontFamily`` on the config),\nand pass the modified objects back here. Each provided object replaces\nthe existing one wholesale; omitted objects are left unchanged.\n\nWhen ``executing_user_id`` is provided, ownership of the dashboard is\ntemporarily transferred to that user before the write, then restored\nin a ``finally`` block regardless of whether the write succeeds. Pass\nthe Sisense user ID (not email); resolve it with\n``AccessManagement.get_my_user()`` for the API token user, or\n``AccessManagement.get_user(email)`` for any other user.\n\nWhen neither ``current_card`` nor ``current_config`` is provided the\nmethod returns immediately with the current style objects and makes\nno write.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard that contains the widget.\nwidget_id : str\n    The ``oid`` of the BloX widget to update.\ncurrent_card : dict[str, Any] | None, optional\n    Replacement for the ``style.currentCard`` object. Omit to leave\n    the current value unchanged.\ncurrent_config : dict[str, Any] | None, optional\n    Replacement for the ``style.currentConfig`` object. Omit to leave\n    the current value unchanged.\nexecuting_user_id : str | None, optional\n    Sisense user ID of the user who should own the dashboard during the\n    write. Required when the API token user is not the dashboard owner.\n\nReturns\n-------\ndict[str, Any]\n    The style objects after the update:\n\n    - ``currentCard`` (dict): The value of ``style.currentCard`` after the write.\n    - ``currentConfig`` (dict): The value of ``style.currentConfig`` after the write.\n\n    Returns ``{\"error\": \"...\"}`` on failure or if the widget is not a BloX type.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard that contains the widget."
        },
        "widget_id": {
          "type": "string",
          "description": "The ``oid`` of the BloX widget to update."
        },
        "current_card": {
          "type": "object",
          "description": "Replacement for the ``style.currentCard`` object. Omit to leave the current value unchanged."
        },
        "current_config": {
          "type": "object",
          "description": "Replacement for the ``style.currentConfig`` object. Omit to leave the current value unchanged."
        },
        "executing_user_id": {
          "type": "string",
          "description": "Sisense user ID of the user who should own the dashboard during the write. Required when the API token user is not the dashboard owner."
        }
      },
      "required": [
        "dashboard_id",
        "widget_id"
      ]
    },
    "mutates": true,
    "tags": [
      "blox",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.create_notebook",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "create_notebook",
    "description": "Create a new notebook.",
    "full_doc": "Create a new notebook.\n\nSends ``POST /api/v1/notebooks``. By default includes the ``Internal``\nheader required for programmatic notebook creation.\n\nParameters\n----------\nnotebook_data : NotebookCreatePayload\n    Notebook creation payload. ``notebookType`` and ``displayName``\n    are required; additional Sisense notebook manifest fields may be\n    included and are passed through unchanged.\nuse_internal_header : bool, optional\n    When ``True`` (default), send ``Internal: true`` header.\n\nReturns\n-------\ndict[str, Any]\n    Created notebook object on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "notebook_data": {
          "type": "object",
          "properties": {
            "notebookType": {
              "type": "string"
            },
            "displayName": {
              "type": "string"
            },
            "description": {
              "type": "string"
            }
          },
          "required": [
            "displayName",
            "notebookType"
          ],
          "additionalProperties": true,
          "description": "Notebook creation payload. ``notebookType`` and ``displayName`` are required; additional Sisense notebook manifest fields may be included and are passed through unchanged."
        },
        "use_internal_header": {
          "type": "boolean",
          "description": "When ``True`` (default), send ``Internal: true`` header."
        }
      },
      "required": [
        "notebook_data"
      ]
    },
    "mutates": true,
    "tags": [
      "custom_code",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.delete_notebook",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "delete_notebook",
    "description": "Delete a notebook by ID.",
    "full_doc": "Delete a notebook by ID.\n\nSends ``DELETE /api/v1/notebooks/{notebook_id}``.\n\nParameters\n----------\nnotebook_id : str\n    Notebook UUID or identifier.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True}`` on success (HTTP 204), or ``{\"error\": \"...\"}``\n    on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "notebook_id": {
          "type": "string",
          "description": "Notebook UUID or identifier."
        }
      },
      "required": [
        "notebook_id"
      ]
    },
    "mutates": true,
    "tags": [
      "custom_code",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.export_notebook",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "export_notebook",
    "description": "Export a notebook definition.",
    "full_doc": "Export a notebook definition.\n\nSends ``GET /api/v1/notebooks/{notebook_id}/export``.\n\nParameters\n----------\nnotebook_id : str\n    Notebook UUID or identifier.\n\nReturns\n-------\ndict[str, Any]\n    Exported notebook payload on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "notebook_id": {
          "type": "string",
          "description": "Notebook UUID or identifier."
        }
      },
      "required": [
        "notebook_id"
      ]
    },
    "mutates": false,
    "tags": [
      "custom_code",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.get_notebooks",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "get_notebooks",
    "description": "Retrieve notebooks from Sisense.",
    "full_doc": "Retrieve notebooks from Sisense.\n\nSends ``GET /api/v1/notebooks`` with optional query parameters.\n\nParameters\n----------\nnotebook_type : str, optional\n    When provided, sets ``notebookType`` query parameter (for example\n    ``CustomCodeTransformation``).\nparams : dict[str, Any], optional\n    Additional query parameters merged with ``notebookType`` when set.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    Notebook list from the API, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "notebook_type": {
          "type": "string",
          "description": "When provided, sets ``notebookType`` query parameter (for example ``CustomCodeTransformation``)."
        },
        "params": {
          "type": "object",
          "description": "Additional query parameters merged with ``notebookType`` when set."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "custom_code",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.list_notebook_folder_contents",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "list_notebook_folder_contents",
    "description": "List contents of a custom-code notebook folder.",
    "full_doc": "List contents of a custom-code notebook folder.\n\nSends ``GET /api/resources/notebooks/custom_code_notebooks/notebooks/{folder_id}/``.\n\nParameters\n----------\nfolder_id : str\n    Folder identifier.\n\nReturns\n-------\ndict[str, Any] | list[Any]\n    Folder contents from the API, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "folder_id": {
          "type": "string",
          "description": "Folder identifier."
        }
      },
      "required": [
        "folder_id"
      ]
    },
    "mutates": false,
    "tags": [
      "custom_code",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.rename_notebook_file",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "rename_notebook_file",
    "description": "Rename or update a notebook resource file.",
    "full_doc": "Rename or update a notebook resource file.\n\nSends ``PATCH /api/resources/{resource_path}``.\n\nParameters\n----------\nresource_path : str\n    Resource path relative to ``/api/resources/`` (for example\n    ``notebooks/custom_code_notebooks/.../file.ipynb``).\npayload : dict[str, Any]\n    PATCH body with fields to update (for example new name/path).\n\nReturns\n-------\ndict[str, Any]\n    API response on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "resource_path": {
          "type": "string",
          "description": "Resource path relative to ``/api/resources/`` (for example ``notebooks/custom_code_notebooks/.../file.ipynb``)."
        },
        "payload": {
          "type": "object",
          "description": "PATCH body with fields to update (for example new name/path)."
        }
      },
      "required": [
        "resource_path",
        "payload"
      ]
    },
    "mutates": true,
    "tags": [
      "custom_code",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.rename_notebook_folder",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "rename_notebook_folder",
    "description": "Rename a custom-code notebook folder.",
    "full_doc": "Rename a custom-code notebook folder.\n\nSends ``PATCH /api/resources/notebooks/custom_code_notebooks/notebooks/{old_id}/``.\n\nParameters\n----------\nold_id : str\n    Current folder identifier.\npayload : dict[str, Any]\n    PATCH body (for example new folder name or target id).\n\nReturns\n-------\ndict[str, Any]\n    API response on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "old_id": {
          "type": "string",
          "description": "Current folder identifier."
        },
        "payload": {
          "type": "object",
          "description": "PATCH body (for example new folder name or target id)."
        }
      },
      "required": [
        "old_id",
        "payload"
      ]
    },
    "mutates": true,
    "tags": [
      "custom_code",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "custom_code.update_notebook",
    "module": "custom_code",
    "sub_module": "custom_code.core",
    "class": "CustomCode",
    "method": "update_notebook",
    "description": "Update an existing notebook.",
    "full_doc": "Update an existing notebook.\n\nSends ``PATCH /api/v1/notebooks/{notebook_id}``. Only fields present in\n``notebook_data`` are sent; omitted fields are not modified. By default\nincludes the ``Internal`` header.\n\nParameters\n----------\nnotebook_id : str\n    Notebook UUID or identifier. (format: uuid)\nnotebook_data : NotebookUpdatePayload\n    Fields to update — only include fields you want to change.\nuse_internal_header : bool, optional\n    When ``True`` (default), send ``Internal: true`` header.\n\nReturns\n-------\ndict[str, Any]\n    Updated notebook object on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "notebook_id": {
          "type": "string",
          "description": "Notebook UUID or identifier.",
          "format": "uuid"
        },
        "notebook_data": {
          "type": "object",
          "properties": {
            "notebookType": {
              "type": "string"
            },
            "displayName": {
              "type": "string"
            },
            "description": {
              "type": "string"
            }
          },
          "required": [],
          "additionalProperties": true,
          "description": "Fields to update — only include fields you want to change."
        },
        "use_internal_header": {
          "type": "boolean",
          "description": "When ``True`` (default), send ``Internal: true`` header."
        }
      },
      "required": [
        "notebook_id",
        "notebook_data"
      ]
    },
    "mutates": true,
    "tags": [
      "custom_code",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.add_dashboard_script",
    "module": "dashboard",
    "sub_module": "dashboard.scripts",
    "class": "Dashboard",
    "method": "add_dashboard_script",
    "description": "Add or overwrite a script on a dashboard.",
    "full_doc": "Add or overwrite a script on a dashboard.\n\nAdds or overwrites a script on a dashboard, temporarily changing\nownership to the executing user and restoring it (along with the\noriginal shares) afterwards when required, as only the dashboard owner\ncan modify scripts.\n\nParameters\n----------\ndashboard_id : str\n    The ID of the dashboard where the script will be added.\nscript : str | dict[str, Any]\n    The JavaScript script to add, provided as either a properly\n    formatted JSON string, a raw multi-line script string, or a\n    pre-built payload dictionary.\nexecuting_user : str | None, optional\n    The username of the API user, used to temporarily change the owner\n    of the dashboard, as only the owner can add scripts. If not\n    provided, assumes the dashboard owner is the same as the API user.\n    (format: email)\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True, \"message\": \"...\"}`` on success, or the standard\n    ``{\"ok\": False, \"error\": \"...\", ...}`` dict on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ID of the dashboard where the script will be added."
        },
        "script": {
          "type": "object",
          "description": "The JavaScript script to add, provided as either a properly formatted JSON string, a raw multi-line script string, or a pre-built payload dictionary."
        },
        "executing_user": {
          "type": "string",
          "description": "The username of the API user, used to temporarily change the owner of the dashboard, as only the owner can add scripts. If not provided, assumes the dashboard owner is the same as the API user.",
          "format": "email"
        }
      },
      "required": [
        "dashboard_id",
        "script"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.add_dashboard_shares",
    "module": "dashboard",
    "sub_module": "dashboard.shares",
    "class": "Dashboard",
    "method": "add_dashboard_shares",
    "description": "Add or update shares for a dashboard for the given users and groups.",
    "full_doc": "Add or update shares for a dashboard for the given users and groups.\n\nResolves each share's ``name`` to its user or group ``shareId``, compares\nagainst the dashboard's existing shares, and posts only new shares and\nshares whose ``rule`` changed. Existing shares that are unaffected are\npreserved in the payload.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard to which the shares will be applied.\nshares : list[dict[str, Any]]\n    A list of share entries. Each entry must contain:\n\n    - ``name`` (str): The username (email) or group name.\n    - ``type`` (str): Either ``\"user\"`` or ``\"group\"``.\n    - ``rule`` (str): The access level (for example ``\"view\"`` or\n      ``\"edit\"``).\n\n    The ``name`` is resolved to ``shareId`` internally; entries that\n    cannot be resolved are skipped.\n\nReturns\n-------\ndict[str, Any]\n    On success: ``{\"success\": True, \"message\": ..., \"new_shares\": <n>,\n    \"updated_shares\": <n>}`` (``new_shares``/``updated_shares`` are the\n    counts actually written; both 0 when every requested share already\n    existed with the same rule). On failure: the standard\n    ``{\"ok\": False, \"error\": \"...\", ...}`` dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard to which the shares will be applied."
        },
        "shares": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "A list of share entries. Each entry must contain: - ``name`` (str): The username (email) or group name. - ``type`` (str): Either ``\"user\"`` or ``\"group\"``. - ``rule`` (str): The access level (for example ``\"view\"`` or ``\"edit\"``). The ``name`` is resolved to ``shareId`` internally; entries that cannot be resolved are skipped."
        }
      },
      "required": [
        "dashboard_id",
        "shares"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.add_widget_script",
    "module": "dashboard",
    "sub_module": "dashboard.scripts",
    "class": "Dashboard",
    "method": "add_widget_script",
    "description": "Add or overwrite a script for a specific widget within a dashboard.",
    "full_doc": "Add or overwrite a script for a specific widget within a dashboard.\n\nAdds or overwrites a script for a specific widget, republishing the\ndashboard to apply the changes. If required, it temporarily changes the\ndashboard ownership to the executing user and restores it (along with\nthe original shares) afterwards, as only the owner can modify widget\nscripts.\n\nParameters\n----------\ndashboard_id : str\n    The ID of the dashboard containing the widget.\nwidget_id : str\n    The ID of the widget where the script will be added.\nscript : str | dict[str, Any]\n    The JavaScript script to add, provided as either a properly\n    formatted JSON string, a raw multi-line script string, or a\n    pre-built payload dictionary.\nexecuting_user : str | None, optional\n    The username of the API user, used to temporarily change the owner\n    of the dashboard, as only the owner can add scripts. If not\n    provided, assumes the dashboard owner is the same as the API user.\n    (format: email)\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True, \"message\": \"...\"}`` on success, or the standard\n    ``{\"ok\": False, \"error\": \"...\", ...}`` dict on failure (including\n    when the script was added but republishing the dashboard failed).",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ID of the dashboard containing the widget."
        },
        "widget_id": {
          "type": "string",
          "description": "The ID of the widget where the script will be added."
        },
        "script": {
          "type": "object",
          "description": "The JavaScript script to add, provided as either a properly formatted JSON string, a raw multi-line script string, or a pre-built payload dictionary."
        },
        "executing_user": {
          "type": "string",
          "description": "The username of the API user, used to temporarily change the owner of the dashboard, as only the owner can add scripts. If not provided, assumes the dashboard owner is the same as the API user.",
          "format": "email"
        }
      },
      "required": [
        "dashboard_id",
        "widget_id",
        "script"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.can_be_owned",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "can_be_owned",
    "description": "Check whether a dashboard can be owned by the current user.",
    "full_doc": "Check whether a dashboard can be owned by the current user.\n\nSends ``GET /api/v1/dashboards/{dashboard_id}/can_be_owned``.\n\nParameters\n----------\ndashboard_id : str\n    The dashboard ``oid`` to check.\n\nReturns\n-------\ndict[str, Any]\n    The API response on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to check."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.change_dashboard_owner",
    "module": "dashboard",
    "sub_module": "dashboard.shares",
    "class": "Dashboard",
    "method": "change_dashboard_owner",
    "description": "Transfer ownership of a dashboard to a different user.",
    "full_doc": "Transfer ownership of a dashboard to a different user.\n\nSends ``POST /api/v1/dashboards/{dashboard_id}/change_owner``.\nThe previous owner is demoted to a share entry with the rule\nspecified by ``original_owner_rule``.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard whose owner will be changed.\nnew_owner_id : str\n    The Sisense user ID (``_id``) of the user who will become the new owner.\nadmin_access : bool, optional\n    When ``True`` (default), appends ``?adminAccess=true`` to the request.\n    Required when the API token user is not the current dashboard owner.\n    Pass ``False`` when restoring ownership back to the original owner\n    (the caller is already the temporary owner at that point).\noriginal_owner_rule : str, optional\n    The share rule assigned to the outgoing owner after the transfer.\n    Defaults to ``\"edit\"``.\n\nReturns\n-------\ndict[str, Any]\n    The API response body on success, or ``{\"success\": True}`` when the\n    API responds 200 with an empty body. ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard whose owner will be changed."
        },
        "new_owner_id": {
          "type": "string",
          "description": "The Sisense user ID (``_id``) of the user who will become the new owner."
        },
        "admin_access": {
          "type": "boolean",
          "description": "When ``True`` (default), appends ``?adminAccess=true`` to the request. Required when the API token user is not the current dashboard owner. Pass ``False`` when restoring ownership back to the original owner (the caller is already the temporary owner at that point)."
        },
        "original_owner_rule": {
          "type": "string",
          "description": "The share rule assigned to the outgoing owner after the transfer. Defaults to ``\"edit\"``."
        }
      },
      "required": [
        "dashboard_id",
        "new_owner_id"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.export_dashboard",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "export_dashboard",
    "description": "Export a dashboard definition using the Sisense admin export endpoint.",
    "full_doc": "Export a dashboard definition using the Sisense admin export endpoint.\n\nSends a GET request to ``/api/v1/dashboards/export`` with\n``dashboardIds`` and ``adminAccess=true``. The response is a JSON array;\nthis method returns the first dashboard object, which includes fields such\nas ``title``, ``oid``, ``script``, ``widgets``, ``layout``, and ``filters``.\nOther features (for example ``get_dashboard_script``) use this payload\ninternally.\n\nParameters\n----------\ndashboard_id : str\n    The dashboard ``oid`` to export.\n\nReturns\n-------\ndict[str, Any]\n    The exported dashboard object on success, or ``{\"error\": \"<message>\"}``\n    when the HTTP call fails, the body is not valid JSON, or the payload\n    is not a non-empty list as expected.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to export."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.find_widgets_by_type",
    "module": "dashboard",
    "sub_module": "dashboard.widgets",
    "class": "Dashboard",
    "method": "find_widgets_by_type",
    "description": "Find all widgets matching a given type across one or more dashboards.",
    "full_doc": "Find all widgets matching a given type across one or more dashboards.\n\nIterates over the specified dashboards (or all dashboards when\n``dashboards`` is ``None``) and returns every widget whose ``type``\nfield matches ``widget_type``.\n\nParameters\n----------\nwidget_type : str\n    The widget type to match (for example ``\"BloX\"``, ``\"chart\"``,\n    ``\"pivot\"``). Comparison is case-sensitive.\ndashboards : list[str] | str | None, optional\n    One or more dashboard IDs or titles to search. A bare string is\n    treated as a single-item list. When ``None`` (default), all\n    dashboards on the instance are searched.\nadmin_access : bool, optional\n    When ``True`` (default), enumerates all dashboards on the instance\n    via the admin endpoint and fetches widgets using ``adminAccess=true``,\n    so dashboards owned by other users are included. When ``False``,\n    only dashboards visible to the API token user are scanned.\nmax_results : int | None, optional\n    Stop after collecting this many matching widgets. ``None`` (default)\n    means no limit.\n\nReturns\n-------\nlist[dict[str, Any]]\n    A list of match records. Each record contains:\n\n    - ``dashboard_id`` (str): The ``oid`` of the containing dashboard.\n    - ``dashboard_title`` (str): The title of the containing dashboard.\n    - ``widget_id`` (str): The ``oid`` of the matching widget.\n    - ``widget_title`` (str): The title of the matching widget.\n    - ``widget_type`` (str): The type of the matching widget.\n\n    Returns an empty list when no matches are found or all lookups fail.",
    "parameters": {
      "type": "object",
      "properties": {
        "widget_type": {
          "type": "string",
          "description": "The widget type to match (for example ``\"BloX\"``, ``\"chart\"``, ``\"pivot\"``). Comparison is case-sensitive."
        },
        "dashboards": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more dashboard IDs or titles to search. A bare string is treated as a single-item list. When ``None`` (default), all dashboards on the instance are searched."
        },
        "admin_access": {
          "type": "boolean",
          "description": "When ``True`` (default), enumerates all dashboards on the instance via the admin endpoint and fetches widgets using ``adminAccess=true``, so dashboards owned by other users are included. When ``False``, only dashboards visible to the API token user are scanned."
        },
        "max_results": {
          "type": "integer",
          "description": "Stop after collecting this many matching widgets. ``None`` (default) means no limit."
        }
      },
      "required": [
        "widget_type"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_all_dashboards",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "get_all_dashboards",
    "description": "Retrieve all dashboards from the Sisense server.",
    "full_doc": "Retrieve all dashboards from the Sisense server.\n\nSends ``GET /api/v1/dashboards/admin?dashboardType=owner`` using the\nadmin endpoint, which requires elevated access.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of dashboard objects on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboard_by_id",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "get_dashboard_by_id",
    "description": "Retrieve a specific dashboard by its ID.",
    "full_doc": "Retrieve a specific dashboard by its ID.\n\nSends ``GET /api/v1/dashboards/admin?dashboardType=owner&id={dashboard_id}``\nagainst the admin endpoint.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard to retrieve.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    The matching dashboard objects on success, or ``{\"error\": \"...\"}``\n    when the request fails or no dashboard is found.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard to retrieve."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboard_by_name",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "get_dashboard_by_name",
    "description": "Retrieve a specific dashboard by its name.",
    "full_doc": "Retrieve a specific dashboard by its name.\n\nSends ``GET /api/v1/dashboards/admin?dashboardType=owner&name={dashboard_name}``\nagainst the admin endpoint.\n\nParameters\n----------\ndashboard_name : str\n    Title of the dashboard to retrieve.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    The matching dashboard objects on success, or ``{\"error\": \"...\"}``\n    when the request fails or no dashboard is found.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_name": {
          "type": "string",
          "description": "Title of the dashboard to retrieve."
        }
      },
      "required": [
        "dashboard_name"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboard_columns",
    "module": "dashboard",
    "sub_module": "dashboard.columns",
    "class": "Dashboard",
    "method": "get_dashboard_columns",
    "description": "Retrieve columns referenced by a dashboard, including widget and filter columns.",
    "full_doc": "Retrieve columns referenced by a dashboard, including widget and filter columns.\n\nResolves the dashboard by title with ``get_dashboard_by_name``, exports\nits full metadata, then extracts column references from both filters and\nwidgets. The final list is deduplicated by ``table`` and ``column``.\n\nParameters\n----------\ndashboard_name : str\n    Title of the dashboard to retrieve columns from.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of distinct column entries. Each entry contains\n    ``dashboard_name``, ``source`` (``\"filter\"`` or ``\"widget\"``),\n    ``widget_id``, ``table``, and ``column`` — an empty list means the\n    dashboard genuinely references no columns. On failure (dashboard\n    not found, or its metadata cannot be retrieved or parsed), returns\n    the standard ``{\"ok\": False, \"error\": \"...\", ...}`` dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_name": {
          "type": "string",
          "description": "Title of the dashboard to retrieve columns from."
        }
      },
      "required": [
        "dashboard_name"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboard_script",
    "module": "dashboard",
    "sub_module": "dashboard.scripts",
    "class": "Dashboard",
    "method": "get_dashboard_script",
    "description": "Build a formatted dashboard script helper object.",
    "full_doc": "Build a formatted dashboard script helper object.\n\nRetrieves a dashboard export payload and wraps its script content in a\n:class:`SisenseScript` helper that can render plain text, markdown, or file output.\n\nParameters\n----------\ndashboard_id : str\n    The dashboard identifier to export.\n\nReturns\n-------\nSisenseScript | dict[str, str]\n    A :class:`SisenseScript` instance when the dashboard is retrieved and\n    has a script. ``{\"error\": \"...\"}`` when the export fails (including\n    ``status_code`` for HTTP failures such as missing access) or when the\n    dashboard has no script — a normal state, reported explicitly.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard identifier to export."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboard_share",
    "module": "dashboard",
    "sub_module": "dashboard.shares",
    "class": "Dashboard",
    "method": "get_dashboard_share",
    "description": "Retrieve share details (users and groups) for a dashboard by title.",
    "full_doc": "Retrieve share details (users and groups) for a dashboard by title.\n\nResolves the dashboard by title, then maps each share's ``shareId`` to a\nreadable name using the users and groups lists: user shares resolve to\nthe user's ``email`` and group shares to the group ``name``.\n\nParameters\n----------\ndashboard_name : str\n    Title of the dashboard to retrieve share information for.\n\nReturns\n-------\nlist[dict[str, Any]]\n    A list of share entries, each containing ``type`` (``\"user\"`` or\n    ``\"group\"``) and ``name`` (email or group name). An empty list means\n    the dashboard genuinely has no shares. On failure (dashboard not\n    found, users/groups lookup failed), returns the standard\n    ``{\"ok\": False, \"error\": \"...\", ...}`` dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_name": {
          "type": "string",
          "description": "Title of the dashboard to retrieve share information for."
        }
      },
      "required": [
        "dashboard_name"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboard_shares_v1",
    "module": "dashboard",
    "sub_module": "dashboard.shares",
    "class": "Dashboard",
    "method": "get_dashboard_shares_v1",
    "description": "Retrieve share details for a dashboard using the v1 shares endpoint.",
    "full_doc": "Retrieve share details for a dashboard using the v1 shares endpoint.\n\nSends ``GET /api/v1/dashboards/{dashboard_id}/shares``. This returns the\nraw Sisense shares payload (for example ``sharesTo`` and ``owner``),\nunlike ``get_dashboard_share`` which resolves names from the admin list.\n\nParameters\n----------\ndashboard_id : str\n    The dashboard ``oid``.\nadmin_access : bool, optional\n    When ``True`` (default), request with ``adminAccess=true``. Some\n    Sisense versions reject the ``adminAccess`` query parameter with\n    HTTP 422 (strict query-schema validation); the request is then\n    retried automatically without it.\n\nReturns\n-------\ndict[str, Any] | list[dict[str, Any]]\n    The shares response from the API, or ``{\"error\": \"...\"}`` on\n    failure. The payload shape varies by Sisense version (a dict with\n    ``sharesTo``/``owner``, or a list of share entries).",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid``."
        },
        "admin_access": {
          "type": "boolean",
          "description": "When ``True`` (default), request with ``adminAccess=true``. Some Sisense versions reject the ``adminAccess`` query parameter with HTTP 422 (strict query-schema validation); the request is then retried automatically without it."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboard_widgets",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "get_dashboard_widgets",
    "description": "Retrieve widget definitions from an admin export of the dashboard.",
    "full_doc": "Retrieve widget definitions from an admin export of the dashboard.\n\nUses ``export_dashboard`` (``GET /api/v1/dashboards/export`` with\n``dashboardIds`` and ``adminAccess=true``), then reads the ``widgets``\nfield from the first exported dashboard object—the same payload shape\nused by ``get_dashboard_script`` and ``get_widget_script``. If\n``widgets`` is missing or empty, an empty list is returned.\n\n``dashboard_ref`` is resolved with ``resolve_dashboard_reference`` so it\nmay be either a 24-character dashboard ``oid`` or a dashboard title.\n\nParameters\n----------\ndashboard_ref : str\n    Dashboard ``oid`` or title.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of widget objects on success (possibly empty). On failure,\n    ``{\"error\": \"...\"}`` when the reference cannot be resolved or\n    ``export_dashboard`` fails. If ``widgets`` is present but neither a\n    list nor a mapping of widget objects, returns an error dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_ref": {
          "type": "string",
          "description": "Dashboard ``oid`` or title."
        }
      },
      "required": [
        "dashboard_ref"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_dashboards",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "get_dashboards",
    "description": "Retrieve dashboards visible to the authenticated user.",
    "full_doc": "Retrieve dashboards visible to the authenticated user.\n\nSends ``GET /api/v1/dashboards``, which returns dashboards the current\nuser owns or has been shared to — as opposed to\n``get_all_dashboards``, which uses the admin endpoint and requires\nelevated access.\n\nParameters\n----------\nfields : list[str], optional\n    Subset of fields to include in the response (for example\n    ``[\"oid\", \"title\", \"owner\"]``). When omitted, all fields are\n    returned.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of dashboard objects on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "fields": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Subset of fields to include in the response (for example ``[\"oid\", \"title\", \"owner\"]``). When omitted, all fields are returned."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_widget_by_id",
    "module": "dashboard",
    "sub_module": "dashboard.widgets",
    "class": "Dashboard",
    "method": "get_widget_by_id",
    "description": "Retrieve a single widget by its dashboard and widget IDs.",
    "full_doc": "Retrieve a single widget by its dashboard and widget IDs.\n\nSends ``GET /api/v1/dashboards/{dashboard_id}/widgets/{widget_id}``.\nReturns the full widget object as returned by the Sisense API.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard that contains the widget.\nwidget_id : str\n    The ``oid`` of the widget to retrieve.\nadmin_access : bool, optional\n    When ``True`` (default), appends ``?adminAccess=true`` to the request,\n    allowing access to dashboards the API token user does not own. Some\n    Sisense versions reject the ``adminAccess`` query parameter with\n    HTTP 422 (strict query-schema validation); the request is then\n    retried automatically without it.\n\nReturns\n-------\ndict[str, Any]\n    The widget object returned by the API, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard that contains the widget."
        },
        "widget_id": {
          "type": "string",
          "description": "The ``oid`` of the widget to retrieve."
        },
        "admin_access": {
          "type": "boolean",
          "description": "When ``True`` (default), appends ``?adminAccess=true`` to the request, allowing access to dashboards the API token user does not own. Some Sisense versions reject the ``adminAccess`` query parameter with HTTP 422 (strict query-schema validation); the request is then retried automatically without it."
        }
      },
      "required": [
        "dashboard_id",
        "widget_id"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.get_widget_script",
    "module": "dashboard",
    "sub_module": "dashboard.scripts",
    "class": "Dashboard",
    "method": "get_widget_script",
    "description": "Build a formatted widget script helper object.",
    "full_doc": "Build a formatted widget script helper object.\n\nRetrieves a dashboard export payload, selects a widget by index/key, and\nwraps its script content in a :class:`SisenseScript` helper for downstream rendering.\n\nParameters\n----------\ndashboard_id : str\n    The dashboard identifier to export.\nwidget_id : str\n    The widget identifier or lookup key used in the exported widget mapping.\n\nReturns\n-------\nSisenseScript | dict[str, str]\n    A :class:`SisenseScript` instance when the widget is found and has a\n    script. ``{\"error\": \"...\"}`` when the export fails (including\n    ``status_code`` for HTTP failures such as missing access), the widget\n    is not found, or the widget has no script — a normal state, reported\n    explicitly.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard identifier to export."
        },
        "widget_id": {
          "type": "string",
          "description": "The widget identifier or lookup key used in the exported widget mapping."
        }
      },
      "required": [
        "dashboard_id",
        "widget_id"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.import_dashboards_bulk",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "import_dashboards_bulk",
    "description": "Import one or more dashboards via the bulk import endpoint.",
    "full_doc": "Import one or more dashboards via the bulk import endpoint.\n\nSends ``POST /api/v1/dashboards/import/bulk`` with ``action`` as a\nquery parameter and ``dashboards`` as the request body. Each\ndashboard object is typically the payload returned by\n``export_dashboard`` on another environment. The server matches\ndashboards by their ``oid``: when a dashboard with the same ``oid``\nalready exists, ``action`` controls whether it is left unchanged,\nreplaced, or a new copy is created alongside it.\n\nParameters\n----------\ndashboards : list[dict[str, Any]]\n    Dashboard objects to import.\naction : str, optional\n    Conflict behavior for dashboards whose ``oid`` already exists.\n    One of ``\"skip\"``, ``\"overwrite\"``, or ``\"duplicate\"``. Default\n    is ``\"skip\"``.\n\nReturns\n-------\ndict[str, Any]\n    The API response body — including ``succeded`` and ``failed``\n    lists describing the outcome for each dashboard — or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboards": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Dashboard objects to import."
        },
        "action": {
          "type": "string",
          "description": "Conflict behavior for dashboards whose ``oid`` already exists. One of ``\"skip\"``, ``\"overwrite\"``, or ``\"duplicate\"``. Default is ``\"skip\"``."
        }
      },
      "required": [
        "dashboards"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.move_dashboard_to_folder",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "move_dashboard_to_folder",
    "description": "Move a dashboard into a folder.",
    "full_doc": "Move a dashboard into a folder.\n\nSends ``PATCH /api/dashboards/{dashboard_id}`` with only ``parentFolder``\nin the request body. Other dashboard fields are not modified.\n\nParameters\n----------\ndashboard_id : str\n    The dashboard ``oid`` to move.\nfolder_id : str\n    The target folder ``oid`` (``parentFolder`` value).\n\nReturns\n-------\ndict[str, Any]\n    The updated dashboard object from the API, or ``{\"success\": True}``\n    when the API responds 200 with an empty body. ``{\"error\": \"...\"}``\n    on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to move."
        },
        "folder_id": {
          "type": "string",
          "description": "The target folder ``oid`` (``parentFolder`` value)."
        }
      },
      "required": [
        "dashboard_id",
        "folder_id"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.publish_dashboard",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "publish_dashboard",
    "description": "Publish (republish) a dashboard.",
    "full_doc": "Publish (republish) a dashboard.\n\nSends ``POST /api/v1/dashboards/{dashboard_id}/publish``. By default\n``adminAccess=true`` is appended so an admin token can republish when the\ncaller already has access but is not the owner. Pass ``force=True`` to\nappend ``force=true`` (used after script updates).\n\nParameters\n----------\ndashboard_id : str\n    The dashboard ``oid`` to publish.\nadmin_access : bool, optional\n    When ``True`` (default), request with ``adminAccess=true``.\nforce : bool, optional\n    When ``True``, request with ``force=true``. Default is ``False``.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True}`` or the JSON response body on success. On failure,\n    ``{\"error\": \"...\"}``.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The dashboard ``oid`` to publish."
        },
        "admin_access": {
          "type": "boolean",
          "description": "When ``True`` (default), request with ``adminAccess=true``."
        },
        "force": {
          "type": "boolean",
          "description": "When ``True``, request with ``force=true``. Default is ``False``."
        }
      },
      "required": [
        "dashboard_id"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.rename_dashboard",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "rename_dashboard",
    "description": "Rename a dashboard.",
    "full_doc": "Rename a dashboard.\n\nSends ``PATCH /api/dashboards/{dashboard_id}`` with only ``title`` in\nthe request body. Other dashboard fields are not modified.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard to rename.\ntitle : str\n    The new dashboard title.\n\nReturns\n-------\ndict[str, Any]\n    The updated dashboard object on success, or ``{\"success\": True}``\n    when the API responds 200 with an empty body. ``{\"error\": \"...\"}``\n    on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard to rename."
        },
        "title": {
          "type": "string",
          "description": "The new dashboard title."
        }
      },
      "required": [
        "dashboard_id",
        "title"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.resolve_dashboard_reference",
    "module": "dashboard",
    "sub_module": "dashboard.core",
    "class": "Dashboard",
    "method": "resolve_dashboard_reference",
    "description": "Resolve a dashboard reference (ID or name) to a concrete dashboard ID and title.",
    "full_doc": "Resolve a dashboard reference (ID or name) to a concrete dashboard ID and title.\n\nThis helper accepts a single string that may be either:\n- a Sisense dashboard ID (24-character ID), or\n- a dashboard title (name).\n\nIt first attempts to treat the reference as an ID using\n`get_dashboard_by_id`. If that fails or the reference does not look\nlike an ID, it falls back to `get_dashboard_by_name`. The underlying\nmethods are reused as-is.\n\nParameters\n----------\ndashboard_ref : str\n    Dashboard reference to resolve. This can be either an ID or a name.\n\nReturns\n-------\ndict\n    A dictionary with the following keys:\n    - success (bool): True if the reference was resolved to a dashboard.\n    - status_code (int): 200 if resolved successfully, 404 if not found,\n      or 500 if an unexpected error occurred.\n    - dashboard_id (str or None): Resolved dashboard ID (oid) if found,\n      otherwise None.\n    - dashboard_title (str or None): Resolved dashboard title if found,\n      otherwise None.\n    - error (str or None): Error message if success is False, otherwise None.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_ref": {
          "type": "string",
          "description": "Dashboard reference to resolve. This can be either an ID or a name."
        }
      },
      "required": [
        "dashboard_ref"
      ]
    },
    "mutates": false,
    "tags": [
      "dashboard",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "dashboard.update_widget",
    "module": "dashboard",
    "sub_module": "dashboard.widgets",
    "class": "Dashboard",
    "method": "update_widget",
    "description": "Write updated widget data back to Sisense.",
    "full_doc": "Write updated widget data back to Sisense.\n\nSends ``PUT /api/dashboards/{dashboard_id}/widgets/{widget_id}``.\nServer-managed fields (``oid``, ``_id``, ``owner``, ``userId``,\n``created``, ``lastUpdated``, ``instanceType``, ``dashboardid``) are\nstripped from ``widget_data`` before the request is sent.\n\nThe caller is responsible for obtaining the current widget via\n:meth:`get_widget_by_id`, modifying the desired fields, and passing\nthe result here.\n\nOnly the dashboard owner can write widgets. Pair with\n:meth:`change_dashboard_owner` if the API token user is not the owner.\n\nParameters\n----------\ndashboard_id : str\n    The ``oid`` of the dashboard that contains the widget.\nwidget_id : str\n    The ``oid`` of the widget to update.\nwidget_data : dict[str, Any]\n    The full widget payload with the desired changes applied. Server-managed\n    fields are removed automatically before the PUT request.\n\nReturns\n-------\ndict[str, Any]\n    The API response body on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_id": {
          "type": "string",
          "description": "The ``oid`` of the dashboard that contains the widget."
        },
        "widget_id": {
          "type": "string",
          "description": "The ``oid`` of the widget to update."
        },
        "widget_data": {
          "type": "object",
          "description": "The full widget payload with the desired changes applied. Server-managed fields are removed automatically before the PUT request."
        }
      },
      "required": [
        "dashboard_id",
        "widget_id",
        "widget_data"
      ]
    },
    "mutates": true,
    "tags": [
      "dashboard",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.add_datamodel_shares",
    "module": "datamodel",
    "sub_module": "datamodel.shares",
    "class": "DataModel",
    "method": "add_datamodel_shares",
    "description": "Add share entries (users and groups) to a data model.",
    "full_doc": "Add share entries (users and groups) to a data model.\n\nResolves each share's user email or group name to its identifier, merges\nthe new shares with the existing ones, and submits the combined share list.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model to add shares to.\nshares : list[dict[str, Any]]\n    List of share definitions to add. Each dictionary should include:\n    ``name`` (user email or group name), ``type`` (``\"user\"`` or\n    ``\"group\"``), and ``permission`` (one of ``\"EDIT\"``, ``\"READ\"``,\n    ``\"USE\"``).\n\nReturns\n-------\ndict[str, Any]\n    On success: ``{\"success\": True, \"message\": \"...\", \"new_shares\":\n    <n>, \"updated_shares\": <n>, \"skipped\": [...]}``. ``skipped``\n    lists every requested share that was not submitted, as\n    ``{\"name\", \"type\", \"reason\"}`` entries (unknown user/group,\n    inactive user, invalid share type) — an empty list means every\n    requested share was submitted. On failure: the standard\n    ``{\"ok\": False, \"error\": \"...\", ...}`` dict; when none of the\n    given shares can be resolved, no changes are written and the\n    failure dict carries the same ``\"skipped\"`` list.\n\nNotes\n-----\nBoth model types key share entries by ``partyId``, but use different\nendpoints and verbs: EXTRACT permissions are written via\n``PUT /api/elasticubes/localhost/{title}/permissions`` (by title),\nLIVE via ``PATCH /api/v1/elasticubes/live/{oid}/permissions`` (by\noid). A share for a party that already has one updates that party's\npermission instead of duplicating the entry (EXTRACT path). Shares\nfor inactive users are skipped and reported in ``skipped`` — Sisense\naccepts the write but silently drops such entries, so submitting\nthem would report success for a share that never lands.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model to add shares to."
        },
        "shares": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "List of share definitions to add. Each dictionary should include: ``name`` (user email or group name), ``type`` (``\"user\"`` or ``\"group\"``), and ``permission`` (one of ``\"EDIT\"``, ``\"READ\"``, ``\"USE\"``)."
        }
      },
      "required": [
        "datamodel_name",
        "shares"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.create_connections",
    "module": "datamodel",
    "sub_module": "datamodel.connections",
    "class": "DataModel",
    "method": "create_connections",
    "description": "Create a new connection using the provided payload.",
    "full_doc": "Create a new connection using the provided payload.\n\nSends ``POST /api/v2/connections`` with the given payload, which is\ntypically produced by ``generate_connections_payload``.\n\nParameters\n----------\nconnection_payload : ConnectionPayload\n    The configuration payload for the connection. ``provider``,\n    ``name``, and ``parameters`` are required; optional fields include\n    ``description``, ``enabled``, ``createdByUser``, and\n    ``supportedModelTypes``.\n\nReturns\n-------\ndict[str, Any] | None\n    JSON response with the created connection details on success\n    (HTTP 201), or the standard ``{\"ok\": False, \"error\": \"...\", ...}``\n    dict on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_payload": {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "parameters": {
              "type": "object"
            },
            "enabled": {
              "type": "boolean"
            },
            "createdByUser": {
              "type": "boolean"
            },
            "description": {
              "type": "string"
            },
            "supportedModelTypes": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "name",
            "parameters",
            "provider"
          ],
          "additionalProperties": true,
          "description": "The configuration payload for the connection. ``provider``, ``name``, and ``parameters`` are required; optional fields include ``description``, ``enabled``, ``createdByUser``, and ``supportedModelTypes``."
        }
      },
      "required": [
        "connection_payload"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.create_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.build",
    "class": "DataModel",
    "method": "create_datamodel",
    "description": "Create a new data model in Sisense.",
    "full_doc": "Create a new data model in Sisense.\n\nNormalizes and validates the model type, checks that no data model with\nthe same title already exists (the API otherwise fails with an opaque\nHTTP 500), then sends a request to create the data model and returns\nits assigned ID.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model.\ndatamodel_type : Literal[\"extract\", \"live\"]\n    Type of the data model: ``\"extract\"`` (Elasticube) or ``\"live\"``.\n\nReturns\n-------\ndict[str, Any]\n    Dictionary with the data model ID under ``\"datamodel_id\"`` on success,\n    or ``{\"error\": \"...\"}`` on failure — including a clear\n    \"already exists\" error when the title is taken.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model."
        },
        "datamodel_type": {
          "type": "string",
          "enum": [
            "extract",
            "live"
          ],
          "description": "Either 'extract' (Elasticube/EC) or 'live'. If user says 'elasticube' or 'ec', normalize to 'extract'.",
          "x-aliases": {
            "extract": [
              "ec",
              "elasticube",
              "elastic cube",
              "cube",
              "elastic-cube"
            ],
            "live": [
              "realtime",
              "real-time",
              "live model"
            ]
          }
        }
      },
      "required": [
        "datamodel_name",
        "datamodel_type"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.create_dataset",
    "module": "datamodel",
    "sub_module": "datamodel.build",
    "class": "DataModel",
    "method": "create_dataset",
    "description": "Create a new dataset in the specified data model.",
    "full_doc": "Create a new dataset in the specified data model.\n\nResolves the data model and connection by name, then sends a request to\ncreate a dataset. When ``dataset_name`` is omitted, the schema name is used.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model where the dataset will be created.\nconnection_name : str\n    Name of the connection to use.\ndatabase_name : str\n    Name of the data source database.\nschema_name : str\n    Name of the data source schema.\ndataset_name : str | None, optional\n    Name of the dataset. Defaults to the schema name if not provided.\n\nReturns\n-------\ndict[str, Any]\n    The full dataset object on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model where the dataset will be created."
        },
        "connection_name": {
          "type": "string",
          "description": "Name of the connection to use."
        },
        "database_name": {
          "type": "string",
          "description": "Name of the data source database."
        },
        "schema_name": {
          "type": "string",
          "description": "Name of the data source schema."
        },
        "dataset_name": {
          "type": "string",
          "description": "Name of the dataset. Defaults to the schema name if not provided."
        }
      },
      "required": [
        "datamodel_name",
        "connection_name",
        "database_name",
        "schema_name"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.create_table",
    "module": "datamodel",
    "sub_module": "datamodel.build",
    "class": "DataModel",
    "method": "create_table",
    "description": "Create a new table in the specified data model.",
    "full_doc": "Create a new table in the specified data model.\n\nResolves the data model and dataset (inferring database, schema, and\nconnection when not provided), fetches the table schema, and creates the\ntable. For EXTRACT models, applies an optional build behavior configuration.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model where the table will be created.\ntable_name : str\n    Name of the table to create.\ndatabase_name : str | None, optional\n    Name of the data source database. If not provided, inferred from the\n    data model's dataset.\nschema_name : str | None, optional\n    Name of the data source schema. If not provided, inferred from the\n    data model's dataset.\ndataset_id : str | None, optional\n    ID of the dataset where the table will be created. If not provided,\n    inferred from the data model (requires a single dataset).\nimport_query : str | None, optional\n    SQL statement used as a custom import query. Defaults to ``None``.\ndescription : str, optional\n    Description for the table. Defaults to an empty string.\ntags : list[str] | None, optional\n    List of tags to apply to the table. Defaults to ``None``.\nbuild_behavior_config : dict[str, Any] | None, optional\n    Build behavior configuration applied for EXTRACT models. Supported fields:\n    ``mode`` (one of ``\"replace\"``, ``\"replace_changes\"``, ``\"append\"``,\n    ``\"increment\"``) and, when ``mode`` is ``\"increment\"``, ``column_name``\n    (the incremental column).\n\nReturns\n-------\ndict[str, Any]\n    The created (or build-behavior-updated) table object on success, or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model where the table will be created."
        },
        "table_name": {
          "type": "string",
          "description": "Name of the table to create."
        },
        "database_name": {
          "type": "string",
          "description": "Name of the data source database. If not provided, inferred from the data model's dataset."
        },
        "schema_name": {
          "type": "string",
          "description": "Name of the data source schema. If not provided, inferred from the data model's dataset."
        },
        "dataset_id": {
          "type": "string",
          "description": "ID of the dataset where the table will be created. If not provided, inferred from the data model (requires a single dataset)."
        },
        "import_query": {
          "type": "string",
          "description": "SQL statement used as a custom import query. Defaults to ``None``."
        },
        "description": {
          "type": "string",
          "description": "Description for the table. Defaults to an empty string."
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of tags to apply to the table. Defaults to ``None``."
        },
        "build_behavior_config": {
          "type": "object",
          "description": "Build behavior configuration applied for EXTRACT models. Supported fields: ``mode`` (one of ``\"replace\"``, ``\"replace_changes\"``, ``\"append\"``, ``\"increment\"``) and, when ``mode`` is ``\"increment\"``, ``column_name`` (the incremental column)."
        }
      },
      "required": [
        "datamodel_name",
        "table_name"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.delete_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "delete_datamodel",
    "description": "Delete a data model by title and server using the GraphQL ECM endpoint.",
    "full_doc": "Delete a data model by title and server using the GraphQL ECM endpoint.\n\nSends a ``POST /api/v2/ecm/`` GraphQL mutation (``removeElasticube``).\n\nParameters\n----------\ntitle : str\n    The exact title of the data model to delete.\nserver : str\n    The server name where the model is hosted (for example\n    ``\"LocalHost\"``).\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True}`` on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The exact title of the data model to delete."
        },
        "server": {
          "type": "string",
          "description": "The server name where the model is hosted (for example ``\"LocalHost\"``)."
        }
      },
      "required": [
        "title",
        "server"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.delete_datasecurity",
    "module": "datamodel",
    "sub_module": "datamodel.security",
    "class": "DataModel",
    "method": "delete_datasecurity",
    "description": "Delete all datasecurity rules for one table/column of a datamodel.",
    "full_doc": "Delete all datasecurity rules for one table/column of a datamodel.\n\nSends ``DELETE {datasecurity_endpoint}/{table}/{column}`` using the\nendpoint flavor for the model's type (EXTRACT or LIVE). Combined with\n``update_datasecurity`` / ``set_live_datasecurity_add_many`` (which\nadd rules), this enables replace semantics: delete the column's rules,\nthen add the new ones.\n\nParameters\n----------\ndatamodel_name : str\n    Title of the datamodel.\ntable : str\n    Table name the rules apply to.\ncolumn : str\n    Column name the rules apply to.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True}`` on success (the API answers 200 or 204), or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Title of the datamodel."
        },
        "table": {
          "type": "string",
          "description": "Table name the rules apply to."
        },
        "column": {
          "type": "string",
          "description": "Column name the rules apply to."
        }
      },
      "required": [
        "datamodel_name",
        "table",
        "column"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.deploy_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.build",
    "class": "DataModel",
    "method": "deploy_datamodel",
    "description": "Deploy (build or publish) the specified data model based on its type.",
    "full_doc": "Deploy (build or publish) the specified data model based on its type.\n\nSupports both Elasticube (EXTRACT) and Live models. For EXTRACT models a\nbuild is triggered using ``build_type``, ``row_limit``, and ``schema_origin``.\nFor LIVE models a publish is triggered and ``row_limit`` and\n``schema_origin`` are ignored.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model to deploy.\nbuild_type : Literal[\"full\", \"by_table\", \"schema_changes\"], optional\n    Type of deployment for EXTRACT models. One of ``\"schema_changes\"``\n    (build only schema changes), ``\"by_table\"`` (build per each table's\n    config, e.g. incremental/accumulative), or ``\"full\"`` (rebuild the\n    entire model, the default). For LIVE models this is overridden to\n    ``\"publish\"``.\nrow_limit : int, optional\n    Maximum number of rows to process for EXTRACT builds. Defaults to ``0``\n    (no limit). Ignored for LIVE models.\nschema_origin : Literal[\"latest\", \"running\"], optional\n    Schema source for EXTRACT builds. One of ``\"latest\"`` (schema as seen\n    in the Data page, the default) or ``\"running\"`` (last successfully built\n    version). Ignored for LIVE models.\n\nReturns\n-------\ndict[str, Any]\n    Deployment result including status on success, or ``{\"error\": \"...\"}``\n    on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model to deploy."
        },
        "build_type": {
          "type": "string",
          "enum": [
            "full",
            "by_table"
          ],
          "description": "Build strategy for extract models. Omit for live/publish.",
          "x-aliases": {
            "full": [
              "build",
              "rebuild",
              "start",
              "run",
              "execute",
              "refresh"
            ],
            "by_table": [
              "by-table",
              "table-wise",
              "incremental-tables"
            ]
          }
        },
        "row_limit": {
          "type": "integer",
          "description": "Maximum number of rows to process for EXTRACT builds. Defaults to ``0`` (no limit). Ignored for LIVE models.",
          "minimum": 1
        },
        "schema_origin": {
          "type": "string",
          "enum": [
            "latest",
            "schema_changes"
          ],
          "description": "Schema source for EXTRACT builds. One of ``\"latest\"`` (schema as seen in the Data page, the default) or ``\"running\"`` (last successfully built version). Ignored for LIVE models."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.describe_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "describe_datamodel",
    "description": "Retrieve data model structure in a flat, row-based format.",
    "full_doc": "Retrieve data model structure in a flat, row-based format.\n\nResolves the data model by name and flattens its datasets and tables\ninto one row per table, suitable for DataFrame or CSV export.\n\nParameters\n----------\ndatamodel_name : str\n    Name (title) of the data model to describe.\n\nReturns\n-------\nlist[dict[str, Any]]\n    List of row dicts, each representing a single table with model,\n    dataset, connection, and table context. Returns an empty list if\n    the data model is not found.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name (title) of the data model to describe."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.describe_datamodel_raw",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "describe_datamodel_raw",
    "description": "Retrieve detailed information about a specific data model.",
    "full_doc": "Retrieve detailed information about a specific data model.\n\nResolves the data model by name, then builds a nested structure\ncontaining model metadata along with its datasets, connections, and\ntables.\n\nParameters\n----------\ndatamodel_name : str\n    Name (title) of the data model to describe.\n\nReturns\n-------\ndict[str, Any]\n    A nested dict with keys ``name``, ``id``, ``type``,\n    ``datamodel_last_build_publish``, ``datamodel_last_updated``, and\n    ``datasets`` on success, or ``{\"error\": \"...\"}`` if not found.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name (title) of the data model to describe."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.export_datamodel_schema",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "export_datamodel_schema",
    "description": "Export a data model's full schema definition for re-import elsewhere.",
    "full_doc": "Export a data model's full schema definition for re-import elsewhere.\n\nSends ``GET /api/v2/datamodel-exports/schema`` (or, on Windows\ndeployments, the legacy streaming export endpoint) and returns the\nexported schema JSON as-is, ready to be passed to\n``import_datamodel_schema`` — typically against a different Sisense\nenvironment.\n\nParameters\n----------\ndatamodel_id : str\n    OID of the data model to export.\ndependencies : list[str] or None, optional\n    API dependency identifiers to include in the export (for example\n    ``\"dataContext\"``, ``\"scopeConfiguration\"``,\n    ``\"formulaManagement\"``, ``\"drillHierarchies\"``,\n    ``\"perspectives\"``). Windows: has no effect — the export endpoint\n    used there accepts no dependencies parameter.\n\nReturns\n-------\ndict[str, Any]\n    The exported schema object on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_id": {
          "type": "string",
          "description": "OID of the data model to export."
        },
        "dependencies": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "API dependency identifiers to include in the export (for example ``\"dataContext\"``, ``\"scopeConfiguration\"``, ``\"formulaManagement\"``, ``\"drillHierarchies\"``, ``\"perspectives\"``). Windows: has no effect — the export endpoint used there accepts no dependencies parameter."
        }
      },
      "required": [
        "datamodel_id"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.generate_connections_payload",
    "module": "datamodel",
    "sub_module": "datamodel.connections",
    "class": "DataModel",
    "method": "generate_connections_payload",
    "description": "Generate a connection payload for a given data source type.",
    "full_doc": "Generate a connection payload for a given data source type.\n\nBuilds the provider-specific request body consumed by\n``create_connections``. The ``datasource_type`` is matched\ncase-insensitively. Supported types are ``\"Athena\"``, ``\"RedShift\"``,\n``\"BigQuery\"``, and ``\"DataBricks\"``.\n\nParameters\n----------\ndatasource_type : Literal[\"Athena\", \"RedShift\", \"BigQuery\", \"DataBricks\"]\n    Type of data source (matched case-insensitively).\nconnection_params : AthenaConnectionParams | RedShiftConnectionParams | BigQueryConnectionParams | DataBricksConnectionParams\n    Connection details. Supported keys depend on ``datasource_type``:\n\n    - Athena: ``name`` (required), ``region`` (required),\n      ``s3_output_location`` (required), ``aws_access_key`` (required),\n      ``aws_secret_key`` (required), ``description``, ``schema``,\n      ``additional_parameters``.\n    - DataBricks: ``name`` (required), ``connection_string`` (required),\n      ``token`` (required), ``description``, ``use_dynamic_schema``,\n      ``schema``.\n    - BigQuery: ``name`` (required), ``service_account_key_path``\n      (required), ``description``, ``use_service_account``,\n      ``use_proxy_server``, ``use_dynamic_schema``,\n      ``record_field_flattening_level``, ``unnest_arrays``,\n      ``allow_large_results``, ``use_storage_api``,\n      ``additional_parameters``, ``database``.\n    - RedShift: ``server`` (required), ``username`` (required),\n      ``password`` (required), ``name``, ``description``,\n      ``default_database``, ``additional_parameters``.\n\nReturns\n-------\ndict[str, Any]\n    The provider-specific connection payload.\n\nRaises\n------\nKeyError\n    If a required connection parameter is missing.\nValueError\n    If ``datasource_type`` is not supported.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource_type": {
          "type": "string",
          "enum": [
            "Athena",
            "RedShift",
            "BigQuery",
            "DataBricks"
          ],
          "description": "Type of data source (matched case-insensitively)."
        },
        "connection_params": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "region": {
              "type": "string"
            },
            "s3_output_location": {
              "type": "string"
            },
            "aws_access_key": {
              "type": "string"
            },
            "aws_secret_key": {
              "type": "string"
            },
            "description": {
              "type": "string"
            },
            "schema": {
              "type": "string"
            },
            "additional_parameters": {
              "type": "string"
            },
            "server": {
              "type": "string"
            },
            "username": {
              "type": "string"
            },
            "password": {
              "type": "string"
            },
            "default_database": {
              "type": "string"
            },
            "service_account_key_path": {
              "type": "string"
            },
            "use_service_account": {
              "type": "boolean"
            },
            "use_proxy_server": {
              "type": "boolean"
            },
            "use_dynamic_schema": {
              "type": "boolean"
            },
            "record_field_flattening_level": {
              "type": "string"
            },
            "unnest_arrays": {
              "type": "boolean"
            },
            "allow_large_results": {
              "type": "boolean"
            },
            "use_storage_api": {
              "type": "boolean"
            },
            "database": {
              "type": "string"
            },
            "connection_string": {
              "type": "string"
            },
            "token": {
              "type": "string"
            }
          },
          "required": [],
          "additionalProperties": true,
          "description": "Connection details. Supported keys depend on ``datasource_type``: - Athena: ``name`` (required), ``region`` (required), ``s3_output_location`` (required), ``aws_access_key`` (required), ``aws_secret_key`` (required), ``description``, ``schema``, ``additional_parameters``. - DataBricks: ``name`` (required), ``connection_string`` (required), ``token`` (required), ``description``, ``use_dynamic_schema``, ``schema``. - BigQuery: ``name`` (required), ``service_account_key_path`` (required), ``description``, ``use_service_account``, ``use_proxy_server``, ``use_dynamic_schema``, ``record_field_flattening_level``, ``unnest_arrays``, ``allow_large_results``, ``use_storage_api``, ``additional_parameters``, ``database``. - RedShift: ``server`` (required), ``username`` (required), ``password`` (required), ``name``, ``description``, ``default_database``, ``additional_parameters``."
        }
      },
      "required": [
        "datasource_type",
        "connection_params"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_all_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "get_all_datamodel",
    "description": "Retrieve metadata for all data models using an internal API.",
    "full_doc": "Retrieve metadata for all data models using an internal API.\n\nSends a ``POST /api/v2/ecm/`` GraphQL query (``elasticubesMetadata``).\nThis includes additional fields such as build status, size, and\ntimestamps that may not be available through the standard public\nendpoints.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of data model metadata objects (each with ``oid``, ``title``,\n    ``type``, ``status``, ``sizeInMb``) on success, or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_connection",
    "module": "datamodel",
    "sub_module": "datamodel.connections",
    "class": "DataModel",
    "method": "get_connection",
    "description": "Retrieve connections matching a name.",
    "full_doc": "Retrieve connections matching a name.\n\nSends ``GET /api/v2/connections?name=<connection_name>`` and returns the\nmatching connection list.\n\nParameters\n----------\nconnection_name : str\n    Name of the connection to filter by.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of matching connection objects if found, or\n    ``{\"error\": \"...\"}`` on failure or when no match is found.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_name": {
          "type": "string",
          "description": "Name of the connection to filter by."
        }
      },
      "required": [
        "connection_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_connections_all",
    "module": "datamodel",
    "sub_module": "datamodel.connections",
    "class": "DataModel",
    "method": "get_connections_all",
    "description": "Retrieve all connections.",
    "full_doc": "Retrieve all connections.\n\nSends ``GET /api/v2/connections`` and returns the full connection list.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of connection objects on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_data",
    "module": "datamodel",
    "sub_module": "datamodel.data",
    "class": "DataModel",
    "method": "get_data",
    "description": "Retrieve data from a specific table in a data model.",
    "full_doc": "Retrieve data from a specific table in a data model.\n\nRuns a SQL query against the data model and returns the rows in a\nrow-based format (a list of dicts) compatible with ``to_dataframe``.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model.\ntable_name : str\n    Name of the table to retrieve data from.\nquery : str | None, optional\n    Optional SQL query to filter the data. When omitted, all rows of the\n    table are selected.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of dictionaries where each dict represents a row — an empty\n    list means the query genuinely returned no data. On failure,\n    returns the standard ``{\"ok\": False, \"error\": \"...\", ...}`` dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model."
        },
        "table_name": {
          "type": "string",
          "description": "Name of the table to retrieve data from."
        },
        "query": {
          "type": "string",
          "description": "Optional SQL query to filter the data. When omitted, all rows of the table are selected."
        }
      },
      "required": [
        "datamodel_name",
        "table_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "get_datamodel",
    "description": "Retrieve a data model by its title.",
    "full_doc": "Retrieve a data model by its title.\n\nSends ``GET /api/v2/datamodels/schema?title=<name>`` and returns the\nmatching data model schema object.\n\nParameters\n----------\ndatamodel_name : str\n    Name (title) of the data model to retrieve.\n\nReturns\n-------\ndict[str, Any]\n    The data model schema object if found, or ``{\"error\": \"...\"}`` on\n    failure or when no match is found.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name (title) of the data model to retrieve."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_datamodel_permissions_extract",
    "module": "datamodel",
    "sub_module": "datamodel.shares",
    "class": "DataModel",
    "method": "get_datamodel_permissions_extract",
    "description": "Retrieve raw share entries for an EXTRACT (Elasticube) data model.",
    "full_doc": "Retrieve raw share entries for an EXTRACT (Elasticube) data model.\n\nSends ``GET /api/elasticubes/localhost/{datamodel_title}/permissions``\nand returns the raw ``shares`` list — each entry keyed by\n``partyId`` and not resolved to a user/group name. Intended for\ncallers that need to round-trip shares as-is (for example, migrating\nthem between environments). Use ``get_datamodel_shares`` instead for\na resolved, human-readable view.\n\nParameters\n----------\ndatamodel_title : str\n    Title of the EXTRACT data model.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    The raw list of share objects from the API, or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_title": {
          "type": "string",
          "description": "Title of the EXTRACT data model."
        }
      },
      "required": [
        "datamodel_title"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_datamodel_permissions_live",
    "module": "datamodel",
    "sub_module": "datamodel.shares",
    "class": "DataModel",
    "method": "get_datamodel_permissions_live",
    "description": "Retrieve raw share entries for a LIVE data model.",
    "full_doc": "Retrieve raw share entries for a LIVE data model.\n\nSends ``GET /api/v1/elasticubes/live/{datamodel_id}/permissions`` and\nreturns the raw share list — each entry keyed by ``partyId`` and not\nresolved to a user/group name. Intended for callers that need to\nround-trip shares as-is (for example, migrating them between\nenvironments).\n\nParameters\n----------\ndatamodel_id : str\n    OID of the LIVE data model.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    The raw list of share objects from the API, or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_id": {
          "type": "string",
          "description": "OID of the LIVE data model."
        }
      },
      "required": [
        "datamodel_id"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_datamodel_shares",
    "module": "datamodel",
    "sub_module": "datamodel.shares",
    "class": "DataModel",
    "method": "get_datamodel_shares",
    "description": "Retrieve all share entries (users and groups) for a given data model.",
    "full_doc": "Retrieve all share entries (users and groups) for a given data model.\n\nResolves user and group identifiers to names/emails and returns the shares\nin a flat row format. Permission codes are mapped to ``\"EDIT\"``, ``\"READ\"``,\nor ``\"USE\"``.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model to retrieve shares for.\n\nReturns\n-------\nlist[dict[str, Any]]\n    List of dicts, each with ``\"datamodel_name\"``, ``\"datamodel_id\"``,\n    ``\"party_name\"``, ``\"party_type\"``, and ``\"permission\"`` — an empty\n    list means the model genuinely has no shares. On failure, returns\n    the standard ``{\"ok\": False, \"error\": \"...\", ...}`` dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model to retrieve shares for."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_datasecurity",
    "module": "datamodel",
    "sub_module": "datamodel.security",
    "class": "DataModel",
    "method": "get_datasecurity",
    "description": "Retrieve datasecurity table and column entries for a given data model.",
    "full_doc": "Retrieve datasecurity table and column entries for a given data model.\n\nResolves the data model, fetches its datasecurity rules, and returns the\nunique table/column entries in a flat row format.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model to retrieve datasecurity for.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of dicts, each with ``\"datamodel_name\"``, ``\"table_name\"``,\n    ``\"column_name\"``, and ``\"data_type\"`` — an empty list means the\n    model genuinely has no rules, so the row count always equals the\n    number of secured columns. On failure (model cannot be resolved,\n    or the rules cannot be fetched), returns the standard\n    ``{\"ok\": False, \"error\": \"...\", ...}`` dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model to retrieve datasecurity for."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_datasecurity_detail",
    "module": "datamodel",
    "sub_module": "datamodel.security",
    "class": "DataModel",
    "method": "get_datasecurity_detail",
    "description": "Retrieve detailed datasecurity rules for a data model, including share-level visibility.",
    "full_doc": "Retrieve detailed datasecurity rules for a data model, including share-level visibility.\n\nEach row represents a unique column-level rule and is repeated per share for\nclarity. Special handling is applied to interpret member values:\n\n- If ``members`` is an empty list and ``exclusionary`` is missing/null, it is\n  interpreted as \"Nothing\".\n- If ``members`` is empty and ``exclusionary`` is ``False``, it is interpreted\n  as \"Everything\".\n- If values exist and ``exclusionary`` is ``True``, it is treated as a\n  restricted subset.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model to retrieve datasecurity rules for.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of dicts representing datasecurity rules in flat, share-resolved\n    format, each with ``\"datamodel_name\"``, ``\"table_name\"``,\n    ``\"column_name\"``, ``\"data_type\"``, ``\"value\"``, ``\"exclusionary\"``,\n    ``\"share_type\"``, ``\"share_name\"``, and ``\"rule_description\"`` — an\n    empty list means the model genuinely has no rules, so the row count\n    always equals the number of rule/share combinations. On failure\n    (model cannot be resolved, or the rules cannot be fetched), returns\n    the standard ``{\"ok\": False, \"error\": \"...\", ...}`` dict.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model to retrieve datasecurity rules for."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_datasecurity_raw",
    "module": "datamodel",
    "sub_module": "datamodel.security",
    "class": "DataModel",
    "method": "get_datasecurity_raw",
    "description": "Retrieve the raw, unprocessed datasecurity rules for a data model.",
    "full_doc": "Retrieve the raw, unprocessed datasecurity rules for a data model.\n\nUnlike ``get_datasecurity``/``get_datasecurity_detail``, this returns\neach rule exactly as the API provides it — including ``members``,\n``exclusionary``, and raw ``shares`` — with no flattening,\ndeduplication, or share-name resolution. Intended for callers that\nneed to round-trip a full rule definition, for example migrating\nrules between environments.\n\nParameters\n----------\ndatamodel_name : str\n    Name (title) of the data model to retrieve raw datasecurity\n    rules for.\ndatamodel_type : str or None, optional\n    The data model's type (``\"extract\"`` or ``\"live\"``), if already\n    known. When provided, the datasecurity endpoint is built\n    directly from ``datamodel_name`` and this type, skipping the\n    data model resolve call. When omitted, the data model is\n    resolved by name first (matching ``get_datasecurity``).\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    The raw list of datasecurity rule objects from the API, or\n    ``{\"error\": \"...\"}`` on failure (including when the data model\n    cannot be resolved).",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name (title) of the data model to retrieve raw datasecurity rules for."
        },
        "datamodel_type": {
          "type": "string",
          "description": "The data model's type (``\"extract\"`` or ``\"live\"``), if already known. When provided, the datasecurity endpoint is built directly from ``datamodel_name`` and this type, skipping the data model resolve call. When omitted, the data model is resolved by name first (matching ``get_datasecurity``)."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_elasticubes",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "get_elasticubes",
    "description": "List all ElastiCubes using the legacy v1 endpoint.",
    "full_doc": "List all ElastiCubes using the legacy v1 endpoint.\n\nSends ``GET /api/v1/elasticubes/getElasticubes``. This endpoint is\nsupported on both Linux and Windows Sisense deployments and returns\nbasic ElastiCube metadata including ``title``, ``address``, and\n``fullname``.\n\nPrefer ``get_all_datamodel`` for Linux deployments when richer metadata\n(build status, size, timestamps) is needed. Use ``get_elasticubes``\nwhen targeting Windows environments or when a lightweight list suffices.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of ElastiCube objects on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_model_schema",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "get_model_schema",
    "description": "Retrieve the schema of a data model, including tables and columns.",
    "full_doc": "Retrieve the schema of a data model, including tables and columns.\n\nResolves the data model by name and emits one row per column, mapping\nSisense numeric column type codes to readable type names.\n\nParameters\n----------\ndatamodel_name : str\n    Name (title) of the data model to retrieve the schema for.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of dicts (one per column) with ``datamodel_name``,\n    ``datamodel_type``, ``dataset_name``, ``table_name``,\n    ``column_name``, and ``column_type`` on success, or\n    ``{\"error\": \"...\"}`` if the data model is not found.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name (title) of the data model to retrieve the schema for."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_row_count",
    "module": "datamodel",
    "sub_module": "datamodel.data",
    "class": "DataModel",
    "method": "get_row_count",
    "description": "Retrieve the row count for each table in a specific data model.",
    "full_doc": "Retrieve the row count for each table in a specific data model.\n\nResolves the data model's tables, counts rows per table, and returns the\nresults in a flat row-based structure suitable for tabular representation.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    List of dictionaries, each with ``\"table_name\"`` and ``\"row_count\"``,\n    plus a final entry with the total row count. On failure, returns the\n    standard ``{\"ok\": False, \"error\": \"...\", ...}`` dict; tables whose\n    count query fails are skipped with a logged warning.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model."
        }
      },
      "required": [
        "datamodel_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.get_table_schema",
    "module": "datamodel",
    "sub_module": "datamodel.connections",
    "class": "DataModel",
    "method": "get_table_schema",
    "description": "Retrieve the schema of a table within a connection's data source.",
    "full_doc": "Retrieve the schema of a table within a connection's data source.\n\nResolves the connection by name to obtain its ``oid`` and ``provider``,\nthen sends ``POST /api/v1/connection/{id}/table_schema_details``.\n\nParameters\n----------\nconnection_name : str\n    Name of the connection.\ndatabase_name : str\n    Name of the database (sent as ``Database``).\nschema_name : str\n    Name of the schema (sent as ``schema``).\ntable_name : str\n    Name of the table (sent as ``table``).\n\nReturns\n-------\ndict[str, Any]\n    Table schema details if found, or ``{\"error\": \"...\"}`` on failure or\n    when no schema is found.\n\nNotes\n-----\nThis endpoint is undocumented and may change in future Sisense versions.\nUse with caution.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_name": {
          "type": "string",
          "description": "Name of the connection."
        },
        "database_name": {
          "type": "string",
          "description": "Name of the database (sent as ``Database``)."
        },
        "schema_name": {
          "type": "string",
          "description": "Name of the schema (sent as ``schema``)."
        },
        "table_name": {
          "type": "string",
          "description": "Name of the table (sent as ``table``)."
        }
      },
      "required": [
        "connection_name",
        "database_name",
        "schema_name",
        "table_name"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.import_datamodel_schema",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "import_datamodel_schema",
    "description": "Import a data model schema (as produced by ``export_datamodel_schema``).",
    "full_doc": "Import a data model schema (as produced by ``export_datamodel_schema``).\n\nSends ``POST /api/v2/datamodel-imports/schema``. When ``action`` is\n``\"overwrite\"`` and ``target_datamodel_id`` is provided, the import\ntargets that existing data model via the ``datamodelId`` query\nparameter; if the target is not found (404), automatically retries as\na plain create. When ``action`` is ``\"duplicate\"``, imports as a new\ndata model titled ``new_title`` (or ``\"<title> (Duplicate)\"`` when\nomitted). Any other ``action`` value performs a plain create.\n\nParameters\n----------\nschema : dict[str, Any]\n    Schema object to import, typically produced by\n    ``export_datamodel_schema``.\naction : str or None, optional\n    One of ``\"overwrite\"`` or ``\"duplicate\"``. Any other value\n    (including ``None``) performs a plain create.\ntarget_datamodel_id : str or None, optional\n    OID of the existing data model to overwrite. Required for\n    ``action=\"overwrite\"`` to take effect.\nnew_title : str or None, optional\n    Title for the duplicated data model. Used only when\n    ``action=\"duplicate\"``.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"datamodel_id\": <str or None>, \"already_exists\": False}`` on\n    success, or ``{\"error\": \"...\", \"already_exists\": bool}`` on\n    failure. ``already_exists`` is ``True`` when the import failed\n    because a data model with the same title already exists on the\n    target under a different ID.",
    "parameters": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "object",
          "description": "Schema object to import, typically produced by ``export_datamodel_schema``."
        },
        "action": {
          "type": "string",
          "description": "One of ``\"overwrite\"`` or ``\"duplicate\"``. Any other value (including ``None``) performs a plain create."
        },
        "target_datamodel_id": {
          "type": "string",
          "description": "OID of the existing data model to overwrite. Required for ``action=\"overwrite\"`` to take effect."
        },
        "new_title": {
          "type": "string",
          "description": "Title for the duplicated data model. Used only when ``action=\"duplicate\"``."
        }
      },
      "required": [
        "schema"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.load_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "load_datamodel",
    "description": "Look up a data model's OID by title using the GraphQL ECM endpoint.",
    "full_doc": "Look up a data model's OID by title using the GraphQL ECM endpoint.\n\nSends a ``POST /api/v2/ecm/`` GraphQL query (``elasticubeByTitle``) and\nreturns the matching model's ``oid``. This is the recommended way to\nresolve a model title to its internal identifier when the OID is not\nalready known.\n\nParameters\n----------\ntitle : str\n    The exact title of the data model to look up.\nserver : str, optional\n    The server name where the model is hosted. Defaults to\n    ``\"LocalHost\"``.\n\nReturns\n-------\ndict[str, Any]\n    A dict containing at least ``oid`` and ``__typename`` on success, or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The exact title of the data model to look up."
        },
        "server": {
          "type": "string",
          "description": "The server name where the model is hosted. Defaults to ``\"LocalHost\"``."
        }
      },
      "required": [
        "title"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.resolve_datamodel_reference",
    "module": "datamodel",
    "sub_module": "datamodel.core",
    "class": "DataModel",
    "method": "resolve_datamodel_reference",
    "description": "Resolve a data model reference (ID or title) to a concrete data model ID and title.",
    "full_doc": "Resolve a data model reference (ID or title) to a concrete data model ID and title.\n\nThis helper accepts a single string that may be either:\n- a Sisense data model ID, or\n- a data model title (schema name).\n\nIt first attempts to treat the reference as an ID using\n`/api/v2/datamodels/{id}/schema`. If that fails, it falls back to\ncalling `/api/v2/datamodels/schema` with a `title` query parameter.\n\nParameters\n----------\ndatamodel_ref : str\n    Data model reference to resolve. This can be either an ID or a name.\n\nReturns\n-------\ndict\n    A dictionary with the following keys:\n    - success (bool): True if the reference was resolved to a data model.\n    - status_code (int): 200 if resolved successfully, 404 if not found,\n      or 500 if an unexpected error occurred.\n    - datamodel_id (str or None): Resolved data model ID (oid) if found,\n      otherwise None.\n    - datamodel_title (str or None): Resolved data model title if found,\n      otherwise None.\n    - error (str or None): Error message if success is False, otherwise None.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_ref": {
          "type": "string",
          "description": "Data model reference to resolve. This can be either an ID or a name."
        }
      },
      "required": [
        "datamodel_ref"
      ]
    },
    "mutates": false,
    "tags": [
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.set_live_datasecurity_add_many",
    "module": "datamodel",
    "sub_module": "datamodel.security",
    "class": "DataModel",
    "method": "set_live_datasecurity_add_many",
    "description": "Add multiple datasecurity rules to a LIVE datamodel.",
    "full_doc": "Add multiple datasecurity rules to a LIVE datamodel.\n\nSends ``POST /api/v1/elasticubes/live/{datamodel_name}/datasecurity/addMany``\nwith a bulk rule payload.\n\nParameters\n----------\ndatamodel_name : str\n    Title of the LIVE datamodel to update.\nrules : list[dict[str, Any]]\n    Datasecurity rules to add in Sisense API format. Each rule\n    requires ``table``, ``column``, ``datatype``, ``members`` (list of\n    strings), ``exclusionary``, ``shares``, ``allMembers``, ``live``,\n    and ``fullname`` (``\"live:{title}\"``). ``live`` and ``fullname``\n    are filled in automatically when omitted; server-managed fields\n    (``_id``, ``created``, ``lastModified``, ``importedIdIdentifier``)\n    are stripped automatically.\n\nReturns\n-------\ndict[str, Any]\n    API response on success, or ``{\"error\": \"...\"}`` on failure.\n\nNotes\n-----\nThe LIVE datamodel must be **published** — the API answers\n``\"Elasticube has not been found\"`` for unpublished (draft) live\nmodels.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Title of the LIVE datamodel to update."
        },
        "rules": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Datasecurity rules to add in Sisense API format. Each rule requires ``table``, ``column``, ``datatype``, ``members`` (list of strings), ``exclusionary``, ``shares``, ``allMembers``, ``live``, and ``fullname`` (``\"live:{title}\"``). ``live`` and ``fullname`` are filled in automatically when omitted; server-managed fields (``_id``, ``created``, ``lastModified``, ``importedIdIdentifier``) are stripped automatically."
        }
      },
      "required": [
        "datamodel_name",
        "rules"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.setup_datamodel",
    "module": "datamodel",
    "sub_module": "datamodel.build",
    "class": "DataModel",
    "method": "setup_datamodel",
    "description": "Set up a data model end to end using an existing connection.",
    "full_doc": "Set up a data model end to end using an existing connection.\n\nCreates the data model, a dataset, and the requested tables in sequence,\nreusing the supplied connection.\n\nParameters\n----------\ndatamodel_name : str\n    Name of the data model.\ndatamodel_type : Literal[\"extract\", \"live\"]\n    Type of the data model: ``\"extract\"`` (Elasticube) or ``\"live\"``.\nconnection_name : str\n    Name of the connection to use.\ndatabase_name : str\n    Name of the data source database.\nschema_name : str\n    Name of the data source schema.\ntables : list[dict[str, Any]]\n    List of table definitions to create in the data model. Each table is a\n    dictionary that may include the fields: ``table_name``, ``schema_name``,\n    ``database_name``, ``import_query`` (SQL custom import query),\n    ``description``, ``tags`` (list of tags), and ``build_behavior_config``\n    (build behavior configuration).\ndataset_name : str | None, optional\n    Name of the dataset. Defaults to the schema name if not provided.\n\nReturns\n-------\ndict[str, Any]\n    Dictionary with ``\"datamodel_id\"``, ``\"dataset_id\"``, and ``\"tables\"``\n    (list of created table names) on success, or ``{\"error\": \"...\"}`` on\n    failure. A failure dict also carries any partial state already\n    provisioned before the abort (``\"datamodel_id\"``, ``\"dataset_id\"``,\n    ``\"created_tables\"``) so the caller can clean up or resume.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Name of the data model."
        },
        "datamodel_type": {
          "type": "string",
          "enum": [
            "extract",
            "live"
          ],
          "description": "Type of the data model: ``\"extract\"`` (Elasticube) or ``\"live\"``.",
          "x-aliases": {
            "extract": [
              "ec",
              "elasticube",
              "elastic cube",
              "cube",
              "elastic-cube"
            ],
            "live": [
              "realtime",
              "real-time",
              "live model"
            ]
          }
        },
        "connection_name": {
          "type": "string",
          "description": "Name of the connection to use."
        },
        "database_name": {
          "type": "string",
          "description": "Name of the data source database."
        },
        "schema_name": {
          "type": "string",
          "description": "Name of the data source schema."
        },
        "tables": {
          "type": "array",
          "description": "List of tables to add. For 'live' models, build_behavior_config is ignored. For 'extract' models, set build_behavior_config as needed.",
          "items": {
            "type": "object",
            "properties": {
              "database_name": {
                "type": "string",
                "description": "Optional override of the table's database. Defaults to top-level database_name if omitted."
              },
              "schema_name": {
                "type": "string",
                "description": "Optional override of the table's schema. Defaults to top-level schema_name if omitted."
              },
              "table_name": {
                "type": "string",
                "description": "Physical table name to add, or a logical name when using import_query."
              },
              "import_query": {
                "type": "string",
                "description": "Optional custom SQL (executed as-is). Use fully-qualified tables: schema.table (Databricks: `schema`.`table`)."
              },
              "description": {
                "type": "string",
                "description": "Optional table description."
              },
              "tags": {
                "type": "array",
                "description": "Optional list of tags for the table.",
                "items": {
                  "type": "string"
                }
              },
              "build_behavior_config": {
                "type": "object",
                "description": "Extract models only; omit for 'live'. For 'increment' mode, column_name is required.",
                "properties": {
                  "mode": {
                    "type": "string",
                    "enum": [
                      "replace",
                      "replace_changes",
                      "append",
                      "increment"
                    ],
                    "description": "Table build behavior for extract models."
                  },
                  "column_name": {
                    "type": "string",
                    "description": "Required when mode='increment'; ignored otherwise."
                  }
                }
              }
            },
            "required": [
              "table_name"
            ]
          },
          "minItems": 1
        },
        "dataset_name": {
          "type": "string",
          "description": "Name of the dataset. Defaults to the schema name if not provided."
        }
      },
      "required": [
        "datamodel_name",
        "datamodel_type",
        "connection_name",
        "database_name",
        "schema_name",
        "tables"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.update_connection",
    "module": "datamodel",
    "sub_module": "datamodel.connections",
    "class": "DataModel",
    "method": "update_connection",
    "description": "Update an existing connection.",
    "full_doc": "Update an existing connection.\n\nSends ``PATCH /api/v2/connections/{connection_id}``. Only fields present\nin ``connection_data`` are sent in the request body; omitted fields are\nnot modified on the server. Use for connection remapping during\nmigration.\n\nParameters\n----------\nconnection_id : str\n    Connection ``oid`` to update.\nconnection_data : ConnectionUpdatePayload\n    Fields to update (for example ``name``, ``parameters``,\n    ``provider``). Supported keys depend on the Sisense connection type.\n\nReturns\n-------\ndict[str, Any]\n    Updated connection object on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "connection_id": {
          "type": "string",
          "description": "Connection ``oid`` to update."
        },
        "connection_data": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "description": {
              "type": "string"
            },
            "provider": {
              "type": "string"
            },
            "parameters": {
              "type": "object"
            },
            "enabled": {
              "type": "boolean"
            }
          },
          "required": [],
          "additionalProperties": true,
          "description": "Fields to update (for example ``name``, ``parameters``, ``provider``). Supported keys depend on the Sisense connection type."
        }
      },
      "required": [
        "connection_id",
        "connection_data"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.update_datamodel_permissions_extract",
    "module": "datamodel",
    "sub_module": "datamodel.shares",
    "class": "DataModel",
    "method": "update_datamodel_permissions_extract",
    "description": "Replace share entries for an EXTRACT (Elasticube) data model.",
    "full_doc": "Replace share entries for an EXTRACT (Elasticube) data model.\n\nSends ``PUT /api/elasticubes/localhost/{datamodel_title}/permissions``\nwith the full raw share list (each entry keyed by ``partyId``). Use\n``add_datamodel_shares`` instead for name/email-based share\nmanagement.\n\nUses ``PUT`` because that is what the EXTRACT permissions endpoint\nrequires — the LIVE counterpart, ``update_datamodel_permissions_live``,\nrequires ``PATCH`` instead. This is an API difference between the two\nendpoints, not an inconsistency between the two methods.\n\nParameters\n----------\ndatamodel_title : str\n    Title of the EXTRACT data model.\nshares : list[dict[str, Any]]\n    Raw share objects, each with ``partyId``, ``type`` (``\"user\"`` or\n    ``\"group\"``), and ``permission``.\n\nReturns\n-------\ndict[str, Any]\n    API response on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_title": {
          "type": "string",
          "description": "Title of the EXTRACT data model."
        },
        "shares": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Raw share objects, each with ``partyId``, ``type`` (``\"user\"`` or ``\"group\"``), and ``permission``."
        }
      },
      "required": [
        "datamodel_title",
        "shares"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.update_datamodel_permissions_live",
    "module": "datamodel",
    "sub_module": "datamodel.shares",
    "class": "DataModel",
    "method": "update_datamodel_permissions_live",
    "description": "Replace share entries for a LIVE data model.",
    "full_doc": "Replace share entries for a LIVE data model.\n\nSends ``PATCH /api/v1/elasticubes/live/{datamodel_id}/permissions``\nwith the full raw share list (each entry keyed by ``partyId``). The\nLIVE model must already be published — publish it first with\n``deploy_datamodel`` if it has never been built.\n\nUses ``PATCH`` because that is what the LIVE permissions endpoint\nrequires — the EXTRACT counterpart, ``update_datamodel_permissions_extract``,\nrequires ``PUT`` instead. This is an API difference between the two\nendpoints, not an inconsistency between the two methods.\n\nParameters\n----------\ndatamodel_id : str\n    OID of the LIVE data model.\nshares : list[dict[str, Any]]\n    Raw share objects, each with ``partyId``, ``type`` (``\"user\"`` or\n    ``\"group\"``), and ``permission``.\n\nReturns\n-------\ndict[str, Any]\n    API response on success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_id": {
          "type": "string",
          "description": "OID of the LIVE data model."
        },
        "shares": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Raw share objects, each with ``partyId``, ``type`` (``\"user\"`` or ``\"group\"``), and ``permission``."
        }
      },
      "required": [
        "datamodel_id",
        "shares"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "datamodel.update_datasecurity",
    "module": "datamodel",
    "sub_module": "datamodel.security",
    "class": "DataModel",
    "method": "update_datasecurity",
    "description": "Add datasecurity rules to an EXTRACT (Elasticube) datamodel.",
    "full_doc": "Add datasecurity rules to an EXTRACT (Elasticube) datamodel.\n\nSends ``POST /api/elasticubes/localhost/{datamodel_name}/datasecurity``\nwith the rule list. The API **adds** the given rules (bulk); it does not\nreplace existing ones — to replace a column's rules, remove them first\nwith :meth:`delete_datasecurity`. Use this for a standalone\n``migrate_datasecurity`` phase after the datamodel exists on the target.\n\nServer-managed fields (``_id``, ``created``, ``lastModified``,\n``importedIdIdentifier``) are stripped from each rule automatically, so\nrules read back via ``get_datasecurity_raw`` can be re-submitted as-is.\n\nParameters\n----------\ndatamodel_name : str\n    Title of the EXTRACT datamodel to update.\ndatasecurity : list[dict[str, Any]]\n    Datasecurity rule list in Sisense API format. Each rule includes\n    ``table``, ``column``, ``datatype``, ``members`` (list of strings),\n    ``exclusionary``, ``shares``, and ``allMembers``.\n\nReturns\n-------\ndict[str, Any]\n    API response on success, or ``{\"error\": \"...\"}`` on failure.\n\nNotes\n-----\nThe Elasticube must be **built and running** — datasecurity writes are\nrejected by the API for unbuilt (draft) cubes.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_name": {
          "type": "string",
          "description": "Title of the EXTRACT datamodel to update."
        },
        "datasecurity": {
          "type": "array",
          "items": {
            "type": "object"
          },
          "description": "Datasecurity rule list in Sisense API format. Each rule includes ``table``, ``column``, ``datatype``, ``members`` (list of strings), ``exclusionary``, ``shares``, and ``allMembers``."
        }
      },
      "required": [
        "datamodel_name",
        "datasecurity"
      ]
    },
    "mutates": true,
    "tags": [
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "encryption.decrypt",
    "module": "encryption",
    "sub_module": "encryption.core",
    "class": "Encryption",
    "method": "decrypt",
    "description": "Decrypt a value using the Sisense encryption service.",
    "full_doc": "Decrypt a value using the Sisense encryption service.\n\nSends ``POST /api/v1/encryption/decrypt``. Use when reading encrypted\nconnection parameters from exported datamodels.\n\nParameters\n----------\npayload : dict[str, Any]\n    Decryption request body (typically includes a ``value`` field with\n    the ciphertext to decrypt).\n\nReturns\n-------\ndict[str, Any]\n    Decryption response from the API, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "payload": {
          "type": "object",
          "description": "Decryption request body (typically includes a ``value`` field with the ciphertext to decrypt)."
        }
      },
      "required": [
        "payload"
      ]
    },
    "mutates": true,
    "tags": [
      "encryption",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "encryption.encrypt",
    "module": "encryption",
    "sub_module": "encryption.core",
    "class": "Encryption",
    "method": "encrypt",
    "description": "Encrypt a value using the Sisense encryption service.",
    "full_doc": "Encrypt a value using the Sisense encryption service.\n\nSends ``POST /api/v1/encryption/encrypt``. Use when preparing connection\nparameters or keys for cross-server datamodel import.\n\nParameters\n----------\npayload : dict[str, Any]\n    Encryption request body (typically includes a ``value`` field with\n    the plaintext to encrypt).\n\nReturns\n-------\ndict[str, Any]\n    Encryption response from the API, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "payload": {
          "type": "object",
          "description": "Encryption request body (typically includes a ``value`` field with the plaintext to encrypt)."
        }
      },
      "required": [
        "payload"
      ]
    },
    "mutates": true,
    "tags": [
      "encryption",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.create_folder",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "create_folder",
    "description": "Create a new Sisense folder.",
    "full_doc": "Create a new Sisense folder.\n\nSends ``POST /api/v1/folders`` with the folder name and an optional\nparent folder ID. When ``parent_id`` is omitted, the folder is created\nat the root of the folder tree.\n\nParameters\n----------\nname : str\n    Display name for the new folder.\nparent_id : str, optional\n    OID of the parent folder. When omitted, the folder is created at\n    the root level.\n\nReturns\n-------\ndict[str, Any]\n    The created folder object from the API (includes ``oid``, ``name``,\n    and related fields), or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for the new folder."
        },
        "parent_id": {
          "type": "string",
          "description": "OID of the parent folder. When omitted, the folder is created at the root level."
        }
      },
      "required": [
        "name"
      ]
    },
    "mutates": true,
    "tags": [
      "folder",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.delete_folder",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "delete_folder",
    "description": "Delete a folder by OID.",
    "full_doc": "Delete a folder by OID.\n\nSends ``DELETE /api/v1/folders/{folder_id}``. The folder must be empty\nor otherwise deletable per Sisense server rules.\n\nParameters\n----------\nfolder_id : str\n    OID of the folder to delete.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"message\": \"...\"}`` on success (HTTP 204), or ``{\"error\": \"...\"}``\n    on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "folder_id": {
          "type": "string",
          "description": "OID of the folder to delete."
        }
      },
      "required": [
        "folder_id"
      ]
    },
    "mutates": true,
    "tags": [
      "folder",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.get_all_folders",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "get_all_folders",
    "description": "Retrieve the full folder tree.",
    "full_doc": "Retrieve the full folder tree.\n\nConvenience wrapper for ``get_folders(\"tree\")``. Returns the nested\nfolder hierarchy used by Sisense for organizing dashboards.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A list of root-level folder nodes (each may contain nested\n    ``folders`` and ``dashboards`` keys), or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "folder",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.get_folder_ancestors",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "get_folder_ancestors",
    "description": "Retrieve folders by a caller-supplied structure value.",
    "full_doc": "Retrieve folders by a caller-supplied structure value.\n\nThin alias for ``get_folders(structure)`` — passes the ``structure``\nstring through to ``GET /api/v1/folders?structure={structure}``\nunchanged. Provided for compatibility with win2linux workflows that\npass an ancestors-specific structure string.\n\nParameters\n----------\nstructure : str\n    Sisense folder structure type for the request.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    Folder data from the API, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "structure": {
          "type": "string",
          "description": "Sisense folder structure type for the request."
        }
      },
      "required": [
        "structure"
      ]
    },
    "mutates": false,
    "tags": [
      "folder",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.get_folder_id",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "get_folder_id",
    "description": "Retrieve a single folder by OID.",
    "full_doc": "Retrieve a single folder by OID.\n\nSends ``GET /api/v1/folders/{folder_id}`` and returns the folder\nmetadata object.\n\nParameters\n----------\nfolder_id : str\n    OID of the folder to retrieve.\n\nReturns\n-------\ndict[str, Any]\n    Folder metadata from the API, or ``{\"error\": \"...\"}`` if the\n    request fails or no folder is found.",
    "parameters": {
      "type": "object",
      "properties": {
        "folder_id": {
          "type": "string",
          "description": "OID of the folder to retrieve."
        }
      },
      "required": [
        "folder_id"
      ]
    },
    "mutates": false,
    "tags": [
      "folder",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.get_folders",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "get_folders",
    "description": "Retrieve folders using a configurable ``structure`` query parameter.",
    "full_doc": "Retrieve folders using a configurable ``structure`` query parameter.\n\nSends ``GET /api/v1/folders?structure={structure}``. The default\n``structure`` is ``\"flat\"``, which returns a flat folder list (used\nbefore migration in win2linux workflows).\n\nParameters\n----------\nstructure : str, optional\n    Sisense folder structure type (for example ``\"flat\"`` or ``\"tree\"``).\n    Default is ``\"flat\"``.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    Folder data from the API (typically a list), or ``{\"error\": \"...\"}``\n    on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "structure": {
          "type": "string",
          "description": "Sisense folder structure type (for example ``\"flat\"`` or ``\"tree\"``). Default is ``\"flat\"``."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "folder",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.get_navver",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "get_navver",
    "description": "Retrieve the Sisense navigation tree (navver payload).",
    "full_doc": "Retrieve the Sisense navigation tree (navver payload).\n\nSends ``GET /api/v1/navver``. The response includes a ``folders`` key\nwith the navigation folder hierarchy.\n\nReturns\n-------\ndict[str, Any]\n    The navver response object on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "folder",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "folder.update_folder",
    "module": "folder",
    "sub_module": "folder.core",
    "class": "Folder",
    "method": "update_folder",
    "description": "Update an existing folder.",
    "full_doc": "Update an existing folder.\n\nSends ``PATCH /api/v1/folders/{folder_id}``. Only fields explicitly\nprovided are included in the request body; omitted arguments are not\nmodified on the server.\n\nParameters\n----------\nfolder_id : str\n    OID of the folder to update.\nname : str, optional\n    New display name for the folder.\nparent_id : str, optional\n    OID of the new parent folder (moves the folder in the tree).\nowner : str, optional\n    User OID of the new folder owner.\n\nReturns\n-------\ndict[str, Any]\n    The updated folder object from the API, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "folder_id": {
          "type": "string",
          "description": "OID of the folder to update."
        },
        "name": {
          "type": "string",
          "description": "New display name for the folder."
        },
        "parent_id": {
          "type": "string",
          "description": "OID of the new parent folder (moves the folder in the tree)."
        },
        "owner": {
          "type": "string",
          "description": "User OID of the new folder owner."
        }
      },
      "required": [
        "folder_id"
      ]
    },
    "mutates": true,
    "tags": [
      "folder",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_blox_actions",
    "module": "mergetool",
    "sub_module": "mergetool.blox",
    "class": "MergeTool",
    "method": "migrate_all_blox_actions",
    "description": "Migrate all Blox actions from source to target.",
    "full_doc": "Migrate all Blox actions from source to target.\n\nDelegates to ``migrate_blox_actions`` with no type filter, so every\nBlox action found on the source is migrated.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy applied to every Blox action.\nconcurrency : int, default 1\n    Same as in ``migrate_blox_actions``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_blox_actions``.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy applied to every Blox action."
        },
        "concurrency": {
          "type": "integer",
          "description": "Same as in ``migrate_blox_actions``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_dashboards",
    "module": "mergetool",
    "sub_module": "mergetool.dashboards",
    "class": "MergeTool",
    "method": "migrate_all_dashboards",
    "description": "Migrate all dashboards from source to target.",
    "full_doc": "Migrate all dashboards from source to target.\n\nFetches every dashboard from the source environment and delegates to\n``migrate_dashboards``.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy applied to every dashboard.\nconcurrency : int, default 1\n    Same as in ``migrate_dashboards``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_dashboards``.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy applied to every dashboard."
        },
        "concurrency": {
          "type": "integer",
          "description": "Same as in ``migrate_dashboards``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_datamodels",
    "module": "mergetool",
    "sub_module": "mergetool.datamodels",
    "class": "MergeTool",
    "method": "migrate_all_datamodels",
    "description": "Migrate all data models from source to target.",
    "full_doc": "Migrate all data models from source to target.\n\nFetches every data model from the source environment and delegates\nto ``migrate_datamodels``.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy applied to every data model.\ndependencies : list[str] or str or None, default None\n    Same as in ``migrate_datamodels``.\nprovider_connection_map : dict[str, str] or None, default None\n    Same as in ``migrate_datamodels``.\nshares : bool, default False\n    Same as in ``migrate_datamodels``.\nconcurrency : int, default 1\n    Same as in ``migrate_datamodels``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_datamodels``.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy applied to every data model."
        },
        "dependencies": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Same as in ``migrate_datamodels``."
        },
        "provider_connection_map": {
          "type": "object",
          "description": "Same as in ``migrate_datamodels``."
        },
        "shares": {
          "type": "boolean",
          "description": "Same as in ``migrate_datamodels``."
        },
        "concurrency": {
          "type": "integer",
          "description": "Same as in ``migrate_datamodels``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_datasecurity",
    "module": "mergetool",
    "sub_module": "mergetool.datasecurity",
    "class": "MergeTool",
    "method": "migrate_all_datasecurity",
    "description": "Migrate datasecurity rules for all data models from source to target.",
    "full_doc": "Migrate datasecurity rules for all data models from source to target.\n\nFetches every data model from the source environment and delegates\nto ``migrate_datasecurity``. Data models not yet present on the\ntarget are skipped — run ``migrate_all_datamodels`` first.\n\nParameters\n----------\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_datasecurity``.",
    "parameters": {
      "type": "object",
      "properties": {
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_folders",
    "module": "mergetool",
    "sub_module": "mergetool.folder",
    "class": "MergeTool",
    "method": "migrate_all_folders",
    "description": "Migrate all folders from source to target, preserving full hierarchy.",
    "full_doc": "Migrate all folders from source to target, preserving full hierarchy.\n\nFetches every folder from the source environment and delegates to\n``migrate_folders``.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy applied to every folder.\nconcurrency : int, default 1\n    Same as in ``migrate_folders`` — maximum number of folders to\n    create/delete concurrently within each hierarchy depth level.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_folders``.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy applied to every folder."
        },
        "concurrency": {
          "type": "integer",
          "description": "Same as in ``migrate_folders`` — maximum number of folders to create/delete concurrently within each hierarchy depth level."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_groups",
    "module": "mergetool",
    "sub_module": "mergetool.groups",
    "class": "MergeTool",
    "method": "migrate_all_groups",
    "description": "Migrate all eligible groups from source to target.",
    "full_doc": "Migrate all eligible groups from source to target.\n\nFetches every group from the source environment, excludes the\nbuilt-in ``Admins``, ``All users in system``, and ``Everyone`` groups,\nand — when the source environment exposes tenant information —\nrestricts migration to groups belonging to the system tenant, then\ndelegates to ``migrate_groups``.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy applied to every group.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_groups``.\n\nNotes\n-----\nIf the source environment does not expose tenant information (single\ntenant on-premises deployments), tenant-based filtering is skipped and\nevery non-excluded group is treated as eligible.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy applied to every group."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "groups",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_notebooks",
    "module": "mergetool",
    "sub_module": "mergetool.custom_code",
    "class": "MergeTool",
    "method": "migrate_all_notebooks",
    "description": "Migrate all notebooks from source to target.",
    "full_doc": "Migrate all notebooks from source to target.\n\nFetches every notebook from the source environment and delegates to\n``migrate_notebooks``.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy applied to every notebook.\nconcurrency : int, default 1\n    Same as in ``migrate_notebooks``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_notebooks``.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy applied to every notebook."
        },
        "concurrency": {
          "type": "integer",
          "description": "Same as in ``migrate_notebooks``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_saved_filters",
    "module": "mergetool",
    "sub_module": "mergetool.filters",
    "class": "MergeTool",
    "method": "migrate_all_saved_filters",
    "description": "Migrate saved filter dimensions for all data models from source to target.",
    "full_doc": "Migrate saved filter dimensions for all data models from source to target.\n\nFetches every data model from the source environment and delegates\nto ``migrate_saved_filters``. Data models not yet present on the\ntarget are skipped — run ``migrate_all_datamodels`` first.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Same as in ``migrate_saved_filters``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_saved_filters``.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Same as in ``migrate_saved_filters``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_saved_formulas",
    "module": "mergetool",
    "sub_module": "mergetool.formulas",
    "class": "MergeTool",
    "method": "migrate_all_saved_formulas",
    "description": "Migrate saved formula measures for all data models from source to target.",
    "full_doc": "Migrate saved formula measures for all data models from source to target.\n\nFetches every data model from the source environment and delegates\nto ``migrate_saved_formulas``. Data models not yet present on the\ntarget are skipped — run ``migrate_all_datamodels`` first.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Same as in ``migrate_saved_formulas``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_saved_formulas``.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Same as in ``migrate_saved_formulas``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_all_users",
    "module": "mergetool",
    "sub_module": "mergetool.users",
    "class": "MergeTool",
    "method": "migrate_all_users",
    "description": "Migrate all eligible users from source to target.",
    "full_doc": "Migrate all eligible users from source to target.\n\nFetches every user from the source environment, excludes users with\nthe built-in ``super`` role (the source and target super admin\naccounts are expected to already exist independently on each\nenvironment), and — when the source environment exposes tenant\ninformation — restricts migration to users belonging to the system\ntenant, then delegates to ``migrate_users``.\n\nParameters\n----------\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy applied to every user.\nignore_custom_roles : bool, default False\n    When ``True``, strips a ``custom_`` prefix from source role names\n    before matching them against target roles.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback.\n\nReturns\n-------\ndict[str, Any]\n    Same structure as ``migrate_users``.\n\nNotes\n-----\nIf the source environment does not expose tenant information (single\ntenant on-premises deployments), tenant-based filtering is skipped and\nevery non-``super`` user is treated as eligible.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy applied to every user."
        },
        "ignore_custom_roles": {
          "type": "boolean",
          "description": "When ``True``, strips a ``custom_`` prefix from source role names before matching them against target roles."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_blox_actions",
    "module": "mergetool",
    "sub_module": "mergetool.blox",
    "class": "MergeTool",
    "method": "migrate_blox_actions",
    "description": "Migrate specific Blox actions from source to target.",
    "full_doc": "Migrate specific Blox actions from source to target.\n\nFetches the requested actions from the source environment, transforms\neach into a save-ready payload, and creates or overwrites it on the\ntarget. Conflict detection is based on the action's ``type`` field.\nSaving and deleting Blox actions is Linux-only, so the target\nenvironment must be a Linux deployment.\n\nParameters\n----------\naction_types : list[str] or None, default None\n    The ``type`` identifiers of the Blox actions to migrate. If\n    ``None``, every Blox action on the source is migrated.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy when an action with the same ``type`` already\n    exists on the target.\n\n    - ``\"skip\"`` — leave the existing action unchanged.\n    - ``\"overwrite\"`` — delete the existing action then recreate from\n      source.\n    - ``\"duplicate\"`` — always create, regardless of conflicts.\nconcurrency : int, default 1\n    Maximum number of Blox actions to migrate concurrently, run via a\n    background thread pool (``asyncio.to_thread``) since the\n    underlying HTTP client is synchronous. Blox actions are\n    independent of each other, so any value is safe. Values <= 1\n    (the default) process actions one at a time.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys. When\n    ``concurrency`` is greater than 1, this callback may be invoked\n    from multiple worker threads concurrently.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``type``\n    - ``skipped`` : list[dict] — each has ``type`` and ``reason``\n    - ``failed`` : list[dict] — each has ``type`` and ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nNotes\n-----\nIf called from code that is already running an asyncio event loop,\n``concurrency`` greater than 1 falls back to sequential processing (a\nnested event loop cannot be started) and logs a warning.",
    "parameters": {
      "type": "object",
      "properties": {
        "action_types": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The ``type`` identifiers of the Blox actions to migrate. If ``None``, every Blox action on the source is migrated."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy when an action with the same ``type`` already exists on the target. - ``\"skip\"`` — leave the existing action unchanged. - ``\"overwrite\"`` — delete the existing action then recreate from source. - ``\"duplicate\"`` — always create, regardless of conflicts."
        },
        "concurrency": {
          "type": "integer",
          "description": "Maximum number of Blox actions to migrate concurrently, run via a background thread pool (``asyncio.to_thread``) since the underlying HTTP client is synchronous. Blox actions are independent of each other, so any value is safe. Values <= 1 (the default) process actions one at a time."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys. When ``concurrency`` is greater than 1, this callback may be invoked from multiple worker threads concurrently."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_dashboards",
    "module": "mergetool",
    "sub_module": "mergetool.dashboards",
    "class": "MergeTool",
    "method": "migrate_dashboards",
    "description": "Migrate specific dashboards from source to target.",
    "full_doc": "Migrate specific dashboards from source to target.\n\nExports each dashboard from the source environment, repoints its\nembedded datasource references to the target's local Elasticube, and\nimports it into the target via the bulk import endpoint, which\nmatches dashboards by ``oid`` and applies ``action`` natively.\nAfter a successful import, the dashboard's owner and shares are\nremapped from source to target users/groups (matched by email/name),\nand the dashboard is moved into the target folder whose path matches\nits source parent folder's path — if that folder has already been\nmigrated with ``migrate_folders``. Conflict detection for the\n``action`` parameter itself is based on the dashboard's ``oid``.\n\nParameters\n----------\ndashboard_ids : list[str] or None, default None\n    Dashboard OIDs to migrate. Provide either this or\n    ``dashboard_names``.\ndashboard_names : list[str] or None, default None\n    Dashboard titles to migrate. Provide either this or\n    ``dashboard_ids``.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy when a dashboard with the same ``oid`` already\n    exists on the target.\n\n    - ``\"skip\"`` — leave the existing dashboard unchanged.\n    - ``\"overwrite\"`` — replace the existing dashboard with the\n      source version.\n    - ``\"duplicate\"`` — always create, regardless of conflicts.\nconcurrency : int, default 1\n    Maximum number of dashboards to migrate concurrently, run via a\n    background thread pool (``asyncio.to_thread``) since the\n    underlying HTTP client is synchronous. Dashboards are independent\n    of each other, so any value is safe. Values <= 1 (the default)\n    process dashboards one at a time.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys. When\n    ``concurrency`` is greater than 1, this callback may be invoked\n    from multiple worker threads concurrently.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``title``, ``oid``, and\n      ``source_oid``\n    - ``skipped`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``reason``\n    - ``failed`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nRaises\n------\nValueError\n    If both or neither of ``dashboard_ids`` and ``dashboard_names``\n    are provided.\n\nNotes\n-----\nIf called from code that is already running an asyncio event loop,\n``concurrency`` greater than 1 falls back to sequential processing (a\nnested event loop cannot be started) and logs a warning.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Dashboard OIDs to migrate. Provide either this or ``dashboard_names``."
        },
        "dashboard_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Dashboard titles to migrate. Provide either this or ``dashboard_ids``."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy when a dashboard with the same ``oid`` already exists on the target. - ``\"skip\"`` — leave the existing dashboard unchanged. - ``\"overwrite\"`` — replace the existing dashboard with the source version. - ``\"duplicate\"`` — always create, regardless of conflicts."
        },
        "concurrency": {
          "type": "integer",
          "description": "Maximum number of dashboards to migrate concurrently, run via a background thread pool (``asyncio.to_thread``) since the underlying HTTP client is synchronous. Dashboards are independent of each other, so any value is safe. Values <= 1 (the default) process dashboards one at a time."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys. When ``concurrency`` is greater than 1, this callback may be invoked from multiple worker threads concurrently."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_datamodels",
    "module": "mergetool",
    "sub_module": "mergetool.datamodels",
    "class": "MergeTool",
    "method": "migrate_datamodels",
    "description": "Migrate specific data models from source to target.",
    "full_doc": "Migrate specific data models from source to target.\n\nExports each data model's schema from the source environment and\nimports it into the target via the schema import endpoint. Conflict\ndetection is based on the data model's ``title``. Embedded connection\ncredentials are repointed using ``provider_connection_map`` when a\nmatching provider entry is supplied, and stripped otherwise — they\nmust be re-entered (or reconnected) on the target after migration.\n\nParameters\n----------\ndatamodel_ids : list[str] or None, default None\n    Data model OIDs to migrate. Provide either this or\n    ``datamodel_names``.\ndatamodel_names : list[str] or None, default None\n    Data model titles to migrate. Provide either this or\n    ``datamodel_ids``.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy when a data model with the same ``title``\n    already exists on the target.\n\n    - ``\"skip\"`` — leave the existing data model unchanged.\n    - ``\"overwrite\"`` — replace the existing data model's schema with\n      the source version, matched by the target model's own OID.\n    - ``\"duplicate\"`` — always create a new data model titled\n      ``\"<title> (Duplicate)\"``, regardless of conflicts.\ndependencies : list[str] or str or None, default None\n    Dependencies to include in the export. One or more of\n    ``\"dataSecurity\"``, ``\"formulas\"``, ``\"hierarchies\"``,\n    ``\"perspectives\"``. Defaults to all of them when ``None`` or\n    ``\"all\"``. Windows: has no effect when the source is a Windows\n    deployment — its export endpoint accepts no dependencies parameter.\nprovider_connection_map : dict[str, str] or None, default None\n    Maps a connection provider name (for example ``\"Athena\"``) to a\n    target-environment connection OID. Datasets whose provider is not\n    present in this map have their connection parameters cleared on\n    import.\nshares : bool, default False\n    Whether to also migrate each data model's shares after a\n    successful import, remapping users and groups by email/name.\nconcurrency : int, default 1\n    Maximum number of data models to migrate concurrently, run via a\n    background thread pool (``asyncio.to_thread``) since the\n    underlying HTTP client is synchronous. Data models are\n    independent of each other, so any value is safe. Values <= 1\n    (the default) process data models one at a time.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys. When\n    ``concurrency`` is greater than 1, this callback may be invoked\n    from multiple worker threads concurrently.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``target_id``\n    - ``skipped`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``reason``\n    - ``failed`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nRaises\n------\nValueError\n    If both or neither of ``datamodel_ids`` and ``datamodel_names``\n    are provided.\n\nNotes\n-----\nIf called from code that is already running an asyncio event loop,\n``concurrency`` greater than 1 falls back to sequential processing (a\nnested event loop cannot be started) and logs a warning.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model OIDs to migrate. Provide either this or ``datamodel_names``."
        },
        "datamodel_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model titles to migrate. Provide either this or ``datamodel_ids``."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy when a data model with the same ``title`` already exists on the target. - ``\"skip\"`` — leave the existing data model unchanged. - ``\"overwrite\"`` — replace the existing data model's schema with the source version, matched by the target model's own OID. - ``\"duplicate\"`` — always create a new data model titled ``\"<title> (Duplicate)\"``, regardless of conflicts."
        },
        "dependencies": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Dependencies to include in the export. One or more of ``\"dataSecurity\"``, ``\"formulas\"``, ``\"hierarchies\"``, ``\"perspectives\"``. Defaults to all of them when ``None`` or ``\"all\"``. Windows: has no effect when the source is a Windows deployment — its export endpoint accepts no dependencies parameter."
        },
        "provider_connection_map": {
          "type": "object",
          "description": "Maps a connection provider name (for example ``\"Athena\"``) to a target-environment connection OID. Datasets whose provider is not present in this map have their connection parameters cleared on import."
        },
        "shares": {
          "type": "boolean",
          "description": "Whether to also migrate each data model's shares after a successful import, remapping users and groups by email/name."
        },
        "concurrency": {
          "type": "integer",
          "description": "Maximum number of data models to migrate concurrently, run via a background thread pool (``asyncio.to_thread``) since the underlying HTTP client is synchronous. Data models are independent of each other, so any value is safe. Values <= 1 (the default) process data models one at a time."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys. When ``concurrency`` is greater than 1, this callback may be invoked from multiple worker threads concurrently."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_datasecurity",
    "module": "mergetool",
    "sub_module": "mergetool.datasecurity",
    "class": "MergeTool",
    "method": "migrate_datasecurity",
    "description": "Migrate row-level datasecurity rules for specific data models from source to target.",
    "full_doc": "Migrate row-level datasecurity rules for specific data models from source to target.\n\nFetches each data model's raw datasecurity rules from the source,\nremaps the user/group shares on every rule to their target-environment\nids (matched by email for users and by name for groups), and writes\nthe resolved rules onto the already-existing target data model.\nRequires the target data model to already exist — run\n``migrate_datamodels``/``migrate_all_datamodels`` first.\n\nParameters\n----------\ndatamodel_ids : list[str] or None, default None\n    Data model OIDs to migrate. Provide either this or\n    ``datamodel_names``.\ndatamodel_names : list[str] or None, default None\n    Data model titles to migrate. Provide either this or\n    ``datamodel_ids``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``rule_count``\n    - ``skipped`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``reason``\n    - ``failed`` : list[dict] — each has ``title``, ``source_oid``,\n      and ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nRaises\n------\nValueError\n    If both or neither of ``datamodel_ids`` and ``datamodel_names``\n    are provided.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model OIDs to migrate. Provide either this or ``datamodel_names``."
        },
        "datamodel_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model titles to migrate. Provide either this or ``datamodel_ids``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_folders",
    "module": "mergetool",
    "sub_module": "mergetool.folder",
    "class": "MergeTool",
    "method": "migrate_folders",
    "description": "Migrate specific folders and their full subtrees from source to target.",
    "full_doc": "Migrate specific folders and their full subtrees from source to target.\n\nResolves the requested root folders by ID or display name, expands\neach to its complete subtree, then recreates the hierarchy on the\ntarget in depth-first order. Conflict detection is path-based (full\n``parent/child`` path), so identically-named folders in different\nbranches are treated independently.\n\nFolders whose parent is not part of the migration (e.g. when migrating\na sub-folder in isolation) are created at the root level on the target.\n\nParameters\n----------\nfolder_ids : list[str] or None, default None\n    Folder OIDs to migrate. Provide either this or ``folder_names``.\nfolder_names : list[str] or None, default None\n    Folder display names to migrate. Provide either this or\n    ``folder_ids``.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy when a folder at the same path already exists on\n    the target.\n\n    - ``\"skip\"`` — leave the existing folder unchanged and map its OID\n      so child folders can still be placed under it correctly.\n    - ``\"overwrite\"`` — delete the existing folder then recreate from\n      source. **Warning:** deleting a folder on Sisense also removes\n      all dashboards inside it.\n    - ``\"duplicate\"`` — always create, regardless of conflicts.\nconcurrency : int, default 1\n    Maximum number of folders to create/delete concurrently within\n    each hierarchy depth level, run via a background thread pool\n    (``asyncio.to_thread``) since the underlying HTTP client is\n    synchronous. Folders at the same depth are independent — their\n    parent was already migrated in an earlier, fully-completed depth\n    level — so they're safe to run in parallel; different depths are\n    still processed strictly in order, since a child's target parent\n    OID must exist before it can be created. Values <= 1 (the\n    default) process folders one at a time.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys. When\n    ``concurrency`` is greater than 1, this callback may be invoked\n    from multiple worker threads concurrently for folders in the same\n    depth level.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``name``, ``path``, and\n      ``source_oid``\n    - ``skipped`` : list[dict] — each has ``name``, ``path``,\n      ``source_oid``, and ``reason``\n    - ``failed`` : list[dict] — each has ``name``, ``path``,\n      ``source_oid``, and ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nRaises\n------\nValueError\n    If both or neither of ``folder_ids`` and ``folder_names`` are\n    provided.\n\nNotes\n-----\nIf called from code that is already running an asyncio event loop,\n``concurrency`` greater than 1 falls back to sequential processing for\nthe affected depth level (a nested event loop cannot be started) and\nlogs a warning.",
    "parameters": {
      "type": "object",
      "properties": {
        "folder_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Folder OIDs to migrate. Provide either this or ``folder_names``."
        },
        "folder_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Folder display names to migrate. Provide either this or ``folder_ids``."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy when a folder at the same path already exists on the target. - ``\"skip\"`` — leave the existing folder unchanged and map its OID so child folders can still be placed under it correctly. - ``\"overwrite\"`` — delete the existing folder then recreate from source. **Warning:** deleting a folder on Sisense also removes all dashboards inside it. - ``\"duplicate\"`` — always create, regardless of conflicts."
        },
        "concurrency": {
          "type": "integer",
          "description": "Maximum number of folders to create/delete concurrently within each hierarchy depth level, run via a background thread pool (``asyncio.to_thread``) since the underlying HTTP client is synchronous. Folders at the same depth are independent — their parent was already migrated in an earlier, fully-completed depth level — so they're safe to run in parallel; different depths are still processed strictly in order, since a child's target parent OID must exist before it can be created. Values <= 1 (the default) process folders one at a time."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys. When ``concurrency`` is greater than 1, this callback may be invoked from multiple worker threads concurrently for folders in the same depth level."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_groups",
    "module": "mergetool",
    "sub_module": "mergetool.groups",
    "class": "MergeTool",
    "method": "migrate_groups",
    "description": "Migrate specific groups from source to target.",
    "full_doc": "Migrate specific groups from source to target.\n\nFetches the requested groups from the source environment and creates\n(or replaces) them on the target via the bulk group endpoint. Conflict\ndetection is based on the group's ``name`` field.\n\nParameters\n----------\ngroup_names : list[str] or None, default None\n    Group names to migrate. If ``None``, every group on the source is\n    migrated.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy when a group with the same ``name`` already\n    exists on the target.\n\n    - ``\"skip\"`` — leave the existing group unchanged.\n    - ``\"overwrite\"`` — delete the existing group then recreate from\n      source. **Warning:** this can disrupt user/group associations\n      still referencing the deleted group on the target.\n    - ``\"duplicate\"`` — always create, regardless of conflicts.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``name``\n    - ``skipped`` : list[dict] — each has ``name`` and ``reason``\n    - ``failed`` : list[dict] — each has ``name`` and ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int",
    "parameters": {
      "type": "object",
      "properties": {
        "group_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Group names to migrate. If ``None``, every group on the source is migrated."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy when a group with the same ``name`` already exists on the target. - ``\"skip\"`` — leave the existing group unchanged. - ``\"overwrite\"`` — delete the existing group then recreate from source. **Warning:** this can disrupt user/group associations still referencing the deleted group on the target. - ``\"duplicate\"`` — always create, regardless of conflicts."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "groups",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_notebooks",
    "module": "mergetool",
    "sub_module": "mergetool.custom_code",
    "class": "MergeTool",
    "method": "migrate_notebooks",
    "description": "Migrate specific notebooks from source to target.",
    "full_doc": "Migrate specific notebooks from source to target.\n\nExports each notebook from the source environment and creates or\nupdates it on the target. Conflict detection is based on ``displayName``.\n\nParameters\n----------\nnotebook_ids : list[str] or None, default None\n    Notebook IDs to migrate. Provide either this or ``notebook_names``.\nnotebook_names : list[str] or None, default None\n    Notebook display names to migrate. Provide either this or\n    ``notebook_ids``.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy when a notebook with the same ``displayName``\n    already exists on the target.\n\n    - ``\"skip\"`` — leave the existing notebook unchanged.\n    - ``\"overwrite\"`` — delete the existing notebook then recreate from\n      source.\n    - ``\"duplicate\"`` — always create, regardless of conflicts.\nconcurrency : int, default 1\n    Maximum number of notebooks to migrate concurrently, run via a\n    background thread pool (``asyncio.to_thread``) since the\n    underlying HTTP client is synchronous. Notebooks are independent\n    of each other, so any value is safe. Values <= 1 (the default)\n    process notebooks one at a time.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys. When\n    ``concurrency`` is greater than 1, this callback may be invoked\n    from multiple worker threads concurrently.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``name`` and ``source_id``\n    - ``skipped`` : list[dict] — each has ``name``, ``source_id``, and\n      ``reason``\n    - ``failed`` : list[dict] — each has ``name``, ``source_id``, and\n      ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nRaises\n------\nValueError\n    If both or neither of ``notebook_ids`` and ``notebook_names`` are\n    provided.\n\nNotes\n-----\nIf called from code that is already running an asyncio event loop,\n``concurrency`` greater than 1 falls back to sequential processing (a\nnested event loop cannot be started) and logs a warning.",
    "parameters": {
      "type": "object",
      "properties": {
        "notebook_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Notebook IDs to migrate. Provide either this or ``notebook_names``."
        },
        "notebook_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Notebook display names to migrate. Provide either this or ``notebook_ids``."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy when a notebook with the same ``displayName`` already exists on the target. - ``\"skip\"`` — leave the existing notebook unchanged. - ``\"overwrite\"`` — delete the existing notebook then recreate from source. - ``\"duplicate\"`` — always create, regardless of conflicts."
        },
        "concurrency": {
          "type": "integer",
          "description": "Maximum number of notebooks to migrate concurrently, run via a background thread pool (``asyncio.to_thread``) since the underlying HTTP client is synchronous. Notebooks are independent of each other, so any value is safe. Values <= 1 (the default) process notebooks one at a time."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys. When ``concurrency`` is greater than 1, this callback may be invoked from multiple worker threads concurrently."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_saved_filters",
    "module": "mergetool",
    "sub_module": "mergetool.filters",
    "class": "MergeTool",
    "method": "migrate_saved_filters",
    "description": "Migrate saved filter dimensions for specific data models from source to target.",
    "full_doc": "Migrate saved filter dimensions for specific data models from source to target.\n\nFetches each data model's dimensions from the source, keeps only the\nones that carry a saved filter definition (a truthy ``filter`` key),\nand recreates them on the target via a raw metadata query. Conflict\ndetection is based on a filter's ``title``. Requires the target data\nmodel to already exist — run\n``migrate_datamodels``/``migrate_all_datamodels`` first.\n\nParameters\n----------\ndatamodel_ids : list[str] or None, default None\n    Data model OIDs to migrate. Provide either this or\n    ``datamodel_names``.\ndatamodel_names : list[str] or None, default None\n    Data model titles to migrate. Provide either this or\n    ``datamodel_ids``.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy for filters whose ``title`` already exists on\n    the target datasource.\n\n    - ``\"skip\"`` — leave the existing filter unchanged (default).\n    - ``\"overwrite\"`` or ``\"duplicate\"`` — always create, regardless\n      of conflicts. The Sisense metadata API exposes no update or\n      delete endpoint for saved filters, so ``\"overwrite\"`` cannot\n      replace the existing filter in place and behaves identically to\n      ``\"duplicate\"``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``datamodel`` and\n      ``filter`` (the filter's title)\n    - ``skipped`` : list[dict] — each has ``datamodel``, ``filter``,\n      and ``reason``\n    - ``failed`` : list[dict] — each has ``datamodel``, ``filter``,\n      and ``reason``\n    - ``source_count`` : int — total saved filters found across the\n      resolved data models\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nRaises\n------\nValueError\n    If both or neither of ``datamodel_ids`` and ``datamodel_names``\n    are provided.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model OIDs to migrate. Provide either this or ``datamodel_names``."
        },
        "datamodel_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model titles to migrate. Provide either this or ``datamodel_ids``."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy for filters whose ``title`` already exists on the target datasource. - ``\"skip\"`` — leave the existing filter unchanged (default). - ``\"overwrite\"`` or ``\"duplicate\"`` — always create, regardless of conflicts. The Sisense metadata API exposes no update or delete endpoint for saved filters, so ``\"overwrite\"`` cannot replace the existing filter in place and behaves identically to ``\"duplicate\"``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_saved_formulas",
    "module": "mergetool",
    "sub_module": "mergetool.formulas",
    "class": "MergeTool",
    "method": "migrate_saved_formulas",
    "description": "Migrate saved formula measures for specific data models from source to target.",
    "full_doc": "Migrate saved formula measures for specific data models from source to target.\n\nFetches each data model's saved formula measures from the source and\ncreates them on the target via the metadata measure endpoint.\nConflict detection is based on a formula's ``title``. Requires the\ntarget data model to already exist — run\n``migrate_datamodels``/``migrate_all_datamodels`` first.\n\nParameters\n----------\ndatamodel_ids : list[str] or None, default None\n    Data model OIDs to migrate. Provide either this or\n    ``datamodel_names``.\ndatamodel_names : list[str] or None, default None\n    Data model titles to migrate. Provide either this or\n    ``datamodel_ids``.\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy for formulas whose ``title`` already exists on\n    the target datasource.\n\n    - ``\"skip\"`` — leave the existing formula unchanged (default).\n    - ``\"overwrite\"`` or ``\"duplicate\"`` — always create, regardless\n      of conflicts. The Sisense metadata API exposes no update or\n      delete endpoint for saved formulas, so ``\"overwrite\"`` cannot\n      replace the existing formula in place and behaves identically\n      to ``\"duplicate\"``.\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``datamodel`` and\n      ``formula`` (the formula's title)\n    - ``skipped`` : list[dict] — each has ``datamodel``, ``formula``,\n      and ``reason``\n    - ``failed`` : list[dict] — each has ``datamodel``, ``formula``,\n      and ``reason``\n    - ``source_count`` : int — total saved formulas found across the\n      resolved data models\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int\n\nRaises\n------\nValueError\n    If both or neither of ``datamodel_ids`` and ``datamodel_names``\n    are provided.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model OIDs to migrate. Provide either this or ``datamodel_names``."
        },
        "datamodel_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Data model titles to migrate. Provide either this or ``datamodel_ids``."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy for formulas whose ``title`` already exists on the target datasource. - ``\"skip\"`` — leave the existing formula unchanged (default). - ``\"overwrite\"`` or ``\"duplicate\"`` — always create, regardless of conflicts. The Sisense metadata API exposes no update or delete endpoint for saved formulas, so ``\"overwrite\"`` cannot replace the existing formula in place and behaves identically to ``\"duplicate\"``."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "mergetool.migrate_users",
    "module": "mergetool",
    "sub_module": "mergetool.users",
    "class": "MergeTool",
    "method": "migrate_users",
    "description": "Migrate specific users from source to target.",
    "full_doc": "Migrate specific users from source to target.\n\nFetches the requested users from the source environment, resolves\ntheir role and group assignments to target environment IDs, and\ncreates (or replaces) them on the target via the bulk user endpoint.\nConflict detection is based on the user's ``email`` field.\n\nRole resolution mirrors the legacy Win2Linux merge tool: when the\ntarget environment is multi-tenant (its role list includes\n``tenantAdmin``), source ``super``/``admin`` roles are mapped to\n``tenantAdmin``; when the target is a Windows deployment, source\n``tenantAdmin`` is mapped back to ``admin``.\n\nParameters\n----------\nuser_emails : list[str] or None, default None\n    Email addresses of the users to migrate. If ``None``, every user\n    on the source is migrated. (format: email)\naction : {\"skip\", \"overwrite\", \"duplicate\"}, default \"skip\"\n    Conflict strategy when a user with the same ``email`` already\n    exists on the target.\n\n    - ``\"skip\"`` — leave the existing user unchanged.\n    - ``\"overwrite\"`` — delete the existing user then recreate from\n      source.\n    - ``\"duplicate\"`` — always create, regardless of conflicts.\nignore_custom_roles : bool, default False\n    When ``True``, strips a ``custom_`` prefix from source role names\n    before matching them against target roles (and matches target\n    roles with the same prefix stripped).\nemit : Callable[[dict[str, Any]], None], optional\n    Optional progress callback. Each invocation receives a dict with at\n    least ``type``, ``step``, and ``message`` keys.\n\nReturns\n-------\ndict[str, Any]\n    - ``ok`` : bool\n    - ``status`` : \"success\" | \"failed\" | \"noop\"\n    - ``succeeded`` : list[dict] — each has ``email``\n    - ``skipped`` : list[dict] — each has ``email`` and ``reason``\n    - ``failed`` : list[dict] — each has ``email`` and ``reason``\n    - ``source_count`` : int\n    - ``succeeded_count`` : int\n    - ``skipped_count`` : int\n    - ``failed_count`` : int",
    "parameters": {
      "type": "object",
      "properties": {
        "user_emails": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "email"
          },
          "description": "Email addresses of the users to migrate. If ``None``, every user on the source is migrated."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Conflict strategy when a user with the same ``email`` already exists on the target. - ``\"skip\"`` — leave the existing user unchanged. - ``\"overwrite\"`` — delete the existing user then recreate from source. - ``\"duplicate\"`` — always create, regardless of conflicts."
        },
        "ignore_custom_roles": {
          "type": "boolean",
          "description": "When ``True``, strips a ``custom_`` prefix from source role names before matching them against target roles (and matches target roles with the same prefix stripped)."
        },
        "emit": {
          "type": "object",
          "description": "Optional progress callback. Each invocation receives a dict with at least ``type``, ``step``, and ``message`` keys."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "mergetool",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "metadata.add_datasource_measure",
    "module": "metadata",
    "sub_module": "metadata.core",
    "class": "Metadata",
    "method": "add_datasource_measure",
    "description": "Create a saved formula measure in Sisense metadata.",
    "full_doc": "Create a saved formula measure in Sisense metadata.\n\nSends ``POST /api/metadata/`` with the measure definition payload.\n\nParameters\n----------\nmeasure : MeasurePayload\n    Measure object in Sisense metadata format. ``title`` and\n    ``datasource`` (``{\"title\": ..., \"fullname\": ...}``) are required;\n    additional Sisense metadata fields (expression, context,\n    table/column references) may be included and are passed through\n    unchanged.\n\nReturns\n-------\ndict[str, Any]\n    Created measure object on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "measure": {
          "type": "object",
          "properties": {
            "title": {
              "type": "string"
            },
            "datasource": {
              "type": "object",
              "properties": {
                "title": {
                  "type": "string"
                },
                "fullname": {
                  "type": "string"
                }
              },
              "required": [
                "fullname",
                "title"
              ],
              "additionalProperties": true
            }
          },
          "required": [
            "datasource",
            "title"
          ],
          "additionalProperties": true,
          "description": "Measure object in Sisense metadata format. ``title`` and ``datasource`` (``{\"title\": ..., \"fullname\": ...}``) are required; additional Sisense metadata fields (expression, context, table/column references) may be included and are passed through unchanged."
        }
      },
      "required": [
        "measure"
      ]
    },
    "mutates": true,
    "tags": [
      "metadata",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "metadata.get_datasource_dimensions",
    "module": "metadata",
    "sub_module": "metadata.core",
    "class": "Metadata",
    "method": "get_datasource_dimensions",
    "description": "Retrieve saved filter dimensions for a datasource.",
    "full_doc": "Retrieve saved filter dimensions for a datasource.\n\nSends ``GET /api/metadata/dimensions`` with optional ``datasource`` and\n``dsFullName`` query parameters.\n\nParameters\n----------\ndatasource : str, optional\n    Datasource identifier (for example datamodel title).\nds_full_name : str, optional\n    Full datasource name (for example ``localhost/MyModel``).\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    Dimensions payload from the API (typically a list), or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource": {
          "type": "string",
          "description": "Datasource identifier (for example datamodel title)."
        },
        "ds_full_name": {
          "type": "string",
          "description": "Full datasource name (for example ``localhost/MyModel``)."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "metadata",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "metadata.get_datasource_measures",
    "module": "metadata",
    "sub_module": "metadata.core",
    "class": "Metadata",
    "method": "get_datasource_measures",
    "description": "Retrieve saved formula measures for a datasource.",
    "full_doc": "Retrieve saved formula measures for a datasource.\n\nSends ``GET /api/metadata/measures`` with optional ``datasource`` and\n``dsFullName`` query parameters.\n\nParameters\n----------\ndatasource : str, optional\n    Datasource identifier (for example datamodel title).\nds_full_name : str, optional\n    Full datasource name (for example ``localhost/MyModel``).\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    Measures payload from the API (typically a list), or\n    ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource": {
          "type": "string",
          "description": "Datasource identifier (for example datamodel title)."
        },
        "ds_full_name": {
          "type": "string",
          "description": "Full datasource name (for example ``localhost/MyModel``)."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "metadata",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "metadata.get_datasources",
    "module": "metadata",
    "sub_module": "metadata.core",
    "class": "Metadata",
    "method": "get_datasources",
    "description": "Retrieve all datasources visible to the authenticated user.",
    "full_doc": "Retrieve all datasources visible to the authenticated user.\n\nSends ``GET /api/datasources``.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    Datasource list from the API, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "metadata",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "metadata.post_metadata_query",
    "module": "metadata",
    "sub_module": "metadata.core",
    "class": "Metadata",
    "method": "post_metadata_query",
    "description": "Execute a metadata query against Sisense.",
    "full_doc": "Execute a metadata query against Sisense.\n\nSends ``POST /api/metadata`` with the query payload.\n\nParameters\n----------\nquery_payload : dict[str, Any]\n    Metadata query body as required by the Sisense API.\n\nReturns\n-------\ndict[str, Any]\n    Query result from the API, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "query_payload": {
          "type": "object",
          "description": "Metadata query body as required by the Sisense API."
        }
      },
      "required": [
        "query_payload"
      ]
    },
    "mutates": true,
    "tags": [
      "metadata",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_all_dashboards",
    "module": "migration",
    "sub_module": "migration.dashboards",
    "class": "Migration",
    "method": "migrate_all_dashboards",
    "description": "Migrates all dashboards from the source to the target environment in batches.",
    "full_doc": "Migrates all dashboards from the source to the target environment in batches.\n\nParameters\n----------\naction : str, optional\n    Determines how to handle existing dashboards in the target environment.\n    Options:\n    - 'skip': Skip existing dashboards in the target; new dashboards are processed\n    normally, including shares and ownership.\n    - 'overwrite': Overwrite existing dashboards; shares and ownership will not be\n    migrated. If the dashboard already exists, shares will be retained, but the API\n    user will be set as the new owner.\n    - 'duplicate': Create a duplicate of existing dashboards without migrating shares\n    or ownership.\n    Default: None. Existing dashboards are skipped, and only new ones are migrated.\n    Unless existing dashboards are different owners, shares will be migrated.\n    **Note:** If an existing dashboard in the target environment has a different owner\n    than the user's token running the SDK, the dashboard will be migrated with a new\n    ID, and its shares and ownership will be migrated from the original source\n    dashboard.\nrepublish : bool, optional\n    Whether to republish dashboards after migration. Default: False.\nmigrate_share : bool, optional\n    Whether to migrate shares for the dashboards. If `True`, shares will be\n    migrated, and ownership migration will be controlled by the `change_ownership` parameter.\n    If `False`, both shares and ownership migration will be skipped. Default: False.\nchange_ownership : bool, optional\n    Whether to change ownership of the target dashboards.\n    Effective only if `migrate_share` is True. Default: False.\nbatch_size : int, optional\n    Number of dashboards to process in each batch. Default: 10.\nsleep_time : int, optional\n    Time (in seconds) to sleep between batches. Default: 10 seconds.\nemit : Callable[[Dict[str, Any]], None], optional\n    Optional callback invoked with structured progress events. If not provided, the method\n    emits no events and only returns a final result.\n\n    Event payloads follow a consistent shape:\n    - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\")\n    - ``step``: str logical step identifier\n    - ``message``: str human-readable message\n    - Additional fields depending on the step (counts, status_code, etc.)\n\nReturns\n-------\ndict\n    A summary of the migration results for all batches, containing lists of succeeded, skipped,\n    and failed dashboards.\n\nNotes\n-----\n- **Batch Processing**: Dashboards are processed in batches to avoid overloading the system.\n- **Best Use Case**: This method is suitable when migrating all dashboards from a source to a\ntarget environment.\n- **Overwrite Action**: When using `overwrite`, shares and ownership will not be migrated.\nIf a dashboard already exists, the target dashboard will be overwritten, retaining its existing shares\nbut setting the API user as the new owner. Subsequent adjustments to shares and ownership will not be\nsupported in this mode.\n- **Duplicate Action**: Creates duplicate dashboards without shares and ownership migration.\n- **Skip Action**: Skips migration for existing dashboards, but new ones are processed normally.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "description": "Determines how to handle existing dashboards in the target environment. Options: - 'skip': Skip existing dashboards in the target; new dashboards are processed normally, including shares and ownership. - 'overwrite': Overwrite existing dashboards; shares and ownership will not be migrated. If the dashboard already exists, shares will be retained, but the API user will be set as the new owner. - 'duplicate': Create a duplicate of existing dashboards without migrating shares or ownership. Default: None. Existing dashboards are skipped, and only new ones are migrated. Unless existing dashboards are different owners, shares will be migrated. **Note:** If an existing dashboard in the target environment has a different owner than the user's token running the SDK, the dashboard will be migrated with a new ID, and its shares and ownership will be migrated from the original source dashboard.",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ]
        },
        "republish": {
          "type": "boolean",
          "description": "Whether to republish dashboards after migration. Default: False."
        },
        "migrate_share": {
          "type": "boolean",
          "description": "Whether to migrate shares for the dashboards. If `True`, shares will be migrated, and ownership migration will be controlled by the `change_ownership` parameter. If `False`, both shares and ownership migration will be skipped. Default: False."
        },
        "change_ownership": {
          "type": "boolean",
          "description": "Whether to change ownership of the target dashboards. Effective only if `migrate_share` is True. Default: False."
        },
        "batch_size": {
          "type": "integer",
          "description": "Number of dashboards to process in each batch. Default: 10."
        },
        "sleep_time": {
          "type": "integer",
          "description": "Time (in seconds) to sleep between batches. Default: 10 seconds."
        },
        "emit": {
          "type": "object",
          "description": "Optional callback invoked with structured progress events. If not provided, the method emits no events and only returns a final result. Event payloads follow a consistent shape: - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\") - ``step``: str logical step identifier - ``message``: str human-readable message - Additional fields depending on the step (counts, status_code, etc.)"
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "migration",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_all_datamodels",
    "module": "migration",
    "sub_module": "migration.datamodels",
    "class": "Migration",
    "method": "migrate_all_datamodels",
    "description": "Migrates all data models from the source environment to the target environment in batches.",
    "full_doc": "Migrates all data models from the source environment to the target environment in batches.\n\nParameters\n----------\ndependencies : list[str] or str or None, default None\n    Dependencies to include in the migration. If None or \"all\", all supported dependencies\n    are included by default.\n\n    Supported values:\n    - \"dataSecurity\" (includes both Data Security and Scope Configuration)\n    - \"formulas\" (for Formulas)\n    - \"hierarchies\" (for Drill Hierarchies)\n    - \"perspectives\" (for Perspectives)\n\nshares : bool, default False\n    Whether to also migrate data model shares after the schema import.\n\nbatch_size : int, default 10\n    Number of data models to migrate per batch.\n\nsleep_time : int, default 5\n    Time (in seconds) to sleep between batches.\n\naction : str or None, default None\n    Strategy to handle existing data models in the target environment.\n\n    - \"overwrite\": Attempts to overwrite an existing model using its original ID via the\n    ``datamodelId`` parameter. If the model is not found in the target environment, it\n    falls back to creating the model.\n    - \"duplicate\": Creates a new model by appending \" (Duplicate)\" to the original name.\n\nemit : Callable[[dict], None] or None, default None\n    Optional callback invoked with structured progress events. If not provided, the method\n    emits no events and only returns a final result.\n\n    Event payloads follow a consistent shape:\n    - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\")\n    - ``step``: str logical step identifier\n    - ``message``: str human-readable message\n    - Additional fields depending on the step (counts, status_code, etc.)\n\nReturns\n-------\ndict\n    A migration summary containing:\n    - ``succeeded``: list\n    - ``skipped``: list\n    - ``failed``: list\n    - Counts/metadata fields (for example: ``total_count``, ``batches_total``, etc.)",
    "parameters": {
      "type": "object",
      "properties": {
        "dependencies": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "dataSecurity",
              "formulas",
              "hierarchies",
              "perspectives"
            ]
          },
          "description": "Dependencies to include in the migration. If None or \"all\", all supported dependencies are included by default. Supported values: - \"dataSecurity\" (includes both Data Security and Scope Configuration) - \"formulas\" (for Formulas) - \"hierarchies\" (for Drill Hierarchies) - \"perspectives\" (for Perspectives)"
        },
        "shares": {
          "type": "boolean",
          "description": "Whether to also migrate data model shares after the schema import."
        },
        "batch_size": {
          "type": "integer",
          "description": "Number of data models to migrate per batch."
        },
        "sleep_time": {
          "type": "integer",
          "description": "Time (in seconds) to sleep between batches."
        },
        "action": {
          "type": "string",
          "description": "Strategy to handle existing data models in the target environment. - \"overwrite\": Attempts to overwrite an existing model using its original ID via the ``datamodelId`` parameter. If the model is not found in the target environment, it falls back to creating the model. - \"duplicate\": Creates a new model by appending \" (Duplicate)\" to the original name.",
          "enum": [
            "overwrite",
            "duplicate"
          ]
        },
        "emit": {
          "type": "object",
          "description": "Optional callback invoked with structured progress events. If not provided, the method emits no events and only returns a final result. Event payloads follow a consistent shape: - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\") - ``step``: str logical step identifier - ``message``: str human-readable message - Additional fields depending on the step (counts, status_code, etc.)"
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "migration",
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_all_groups",
    "module": "migration",
    "sub_module": "migration.groups",
    "class": "Migration",
    "method": "migrate_all_groups",
    "description": "Migrate groups from the source environment to the target environment using the bulk endpoint.",
    "full_doc": "Migrate groups from the source environment to the target environment using the bulk endpoint.\n\nThis method supports optional progress emission via the ``emit`` callback. If provided,\nthe method will publish structured progress events at key milestones so a caller can\nstream updates to a user interface while the migration is running.\n\nParameters\n----------\nemit : Callable[[Dict[str, Any]], None], optional\n    Optional callback invoked with structured progress events. If not provided, the\n    method behaves like a standard synchronous call and only returns a final result.\n\n    Each emitted event is a dictionary that typically includes:\n    - ``type``: str, event type (e.g., \"started\", \"progress\", \"completed\", \"error\")\n    - ``step``: str, logical step name\n    - ``message``: str, human-readable message\n    - Additional fields depending on the event (counts, status_code, etc.)\n\nReturns\n-------\nDict[str, Any]\n    Structured result payload with:\n    - ``ok``: bool\n    - ``status``: str (\"success\" | \"failed\" | \"noop\")\n    - ``results``: List[Dict[str, str]] per-group statuses\n    - ``source_count``: int\n    - ``eligible_count``: int\n    - ``success_count``: int\n    - ``failed_count``: int\n    - ``raw_error``: Any\n    - ``warnings``: List[str]",
    "parameters": {
      "type": "object",
      "properties": {
        "emit": {
          "type": "object",
          "description": "Optional callback invoked with structured progress events. If not provided, the method behaves like a standard synchronous call and only returns a final result. Each emitted event is a dictionary that typically includes: - ``type``: str, event type (e.g., \"started\", \"progress\", \"completed\", \"error\") - ``step``: str, logical step name - ``message``: str, human-readable message - Additional fields depending on the event (counts, status_code, etc.)"
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "migration",
      "groups",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_all_users",
    "module": "migration",
    "sub_module": "migration.users",
    "class": "Migration",
    "method": "migrate_all_users",
    "description": "Migrate all eligible users from the source environment to the target environment using the bulk endpoint.",
    "full_doc": "Migrate all eligible users from the source environment to the target environment using the bulk endpoint.\n\nThis method supports streaming progress by optionally emitting structured\nprogress events via the ``emit`` callback. It remains synchronous, but callers can run it\nin a worker thread and forward events to an event stream.\n\nThe migration performs the following steps:\n1) Fetch users from the source environment (expanded with groups and role).\n2) Fetch roles and groups from the target environment for ID mapping.\n3) Build a bulk payload by mapping role names to role IDs and group names to group IDs.\n4) Submit the payload to the target bulk endpoint.\n5) Return a structured summary and per-user status list.\n\nParameters\n----------\nemit : Callable[[Dict[str, Any]], None], optional\n    Optional callback invoked with structured progress events. If not provided, the method\n    emits no events and only returns a final result.\n\n    Event payloads follow a consistent shape:\n    - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\")\n    - ``step``: str logical step identifier\n    - ``message``: str human-readable message\n    - Additional fields depending on the step (counts, status_code, etc.)\n\nReturns\n-------\nDict[str, Any]\n    Structured result payload:\n    - ``ok``: bool\n    - ``status``: str (\"success\" | \"failed\" | \"noop\")\n    - ``results``: List[Dict[str, str]] per-user statuses (name=email, status)\n    - ``source_count``: int number of users retrieved from source\n    - ``eligible_count``: int number of users included in the bulk payload\n    - ``skipped_super_count``: int number of sysadmin users skipped\n    - ``missing_role_mappings_count``: int number of users with unresolved role mapping\n    - ``missing_group_mappings_count``: int number of group memberships not found in target\n    - ``success_count``: int\n    - ``failed_count``: int\n    - ``raw_error``: Any error payload if the bulk request fails, else None\n    - ``warnings``: List[str]",
    "parameters": {
      "type": "object",
      "properties": {
        "emit": {
          "type": "object",
          "description": "Optional callback invoked with structured progress events. If not provided, the method emits no events and only returns a final result. Event payloads follow a consistent shape: - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\") - ``step``: str logical step identifier - ``message``: str human-readable message - Additional fields depending on the step (counts, status_code, etc.)"
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "migration",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_dashboard_shares",
    "module": "migration",
    "sub_module": "migration.dashboards",
    "class": "Migration",
    "method": "migrate_dashboard_shares",
    "description": "Migrate shares for specific dashboards from the source to the target environment.",
    "full_doc": "Migrate shares for specific dashboards from the source to the target environment.\n\nPairs source and target dashboard IDs positionally, maps source users\nand groups to their target equivalents, and applies the resulting\nshares to each target dashboard. Optionally transfers ownership.\n\nParameters\n----------\nsource_dashboard_ids : list[str]\n    Dashboard IDs from the source environment to fetch shares from.\ntarget_dashboard_ids : list[str]\n    Dashboard IDs from the target environment to apply shares to,\n    paired positionally with ``source_dashboard_ids``.\nchange_ownership : bool, optional\n    Whether to change ownership of the target dashboard. Defaults to\n    ``False``.\n\nReturns\n-------\ndict[str, Any]\n    A summary of the share migration with a ``summary`` block (counts of\n    succeeded and failed shares) and ``dashboard_results`` (per-dashboard\n    outcomes).\n\nRaises\n------\nValueError\n    If ``source_dashboard_ids`` or ``target_dashboard_ids`` are not\n    provided, or if their lengths do not match.",
    "parameters": {
      "type": "object",
      "properties": {
        "source_dashboard_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Dashboard IDs from the source environment to fetch shares from."
        },
        "target_dashboard_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Dashboard IDs from the target environment to apply shares to, paired positionally with ``source_dashboard_ids``."
        },
        "change_ownership": {
          "type": "boolean",
          "description": "Whether to change ownership of the target dashboard. Defaults to ``False``."
        }
      },
      "required": [
        "source_dashboard_ids",
        "target_dashboard_ids"
      ]
    },
    "mutates": true,
    "tags": [
      "migration",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_dashboards",
    "module": "migration",
    "sub_module": "migration.dashboards",
    "class": "Migration",
    "method": "migrate_dashboards",
    "description": "Migrate dashboards from the source to the target environment using Sisense bulk import.",
    "full_doc": "Migrate dashboards from the source to the target environment using Sisense bulk import.\n\nThis method exports dashboards from the source and imports them into the target in a single\nbulk request. It then parses the bulk import response to determine which dashboards were\nsucceeded, skipped, and failed. Finally, it optionally migrates shares/ownership for\ndashboards that were actually created in the target (depending on `migrate_share`, `action`).\n\nParameters\n----------\ndashboard_ids : list[str] or None, default None\n    List of dashboard OIDs to migrate. Provide either `dashboard_ids` or `dashboard_names`.\ndashboard_names : list[str] or None, default None\n    List of dashboard titles to migrate. Provide either `dashboard_ids` or `dashboard_names`.\naction : {\"skip\", \"overwrite\", \"duplicate\"} or None, default None\n    Determines how the target handles conflicts.\n    - None: default Sisense behavior (typically skip existing).\n    - \"skip\": skip if exists.\n    - \"overwrite\": overwrite if exists (shares/ownership migration is skipped).\n    - \"duplicate\": create duplicate (shares/ownership migration is skipped).\nrepublish : bool, default False\n    Whether to republish dashboards after import.\nmigrate_share : bool, default False\n    If True, attempts to migrate shares (and optionally ownership) after dashboards are created.\nchange_ownership : bool, default False\n    If True and `migrate_share=True`, attempts to change ownership on the target dashboards.\nemit : Callable[[dict[str, Any]], None] or None, default None\n    Optional callback invoked with structured progress events. If not provided, no events\n    are emitted and only the final result is returned. Event payloads carry a ``type``\n    (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\"), a ``step`` identifier,\n    a human-readable ``message``, and additional step-specific fields.\n\nReturns\n-------\ndict\n    Migration summary with the following keys:\n    - \"succeeded\": list[dict]\n        Each item includes: {\"title\": str, \"source_id\": str | None, \"target_id\": str | None}\n    - \"skipped\": list[dict]\n        Each item includes: {\"title\": str, \"source_id\": str | None, \"target_id\": str | None, \"reason\": str | None}\n    - \"failed\": list[dict]\n        Each item includes: {\"title\": str | None, \"source_id\": str | None, \"reason\": str}\n    - \"meta\": dict\n        Helpful metadata about request-level status.\n\nNotes\n-----\nResponse parsing strategy:\n1) Primary (source of truth): when bulk import returns a success status (typically 201),\nparse the structured response fields:\n- \"succeded\" / \"succeeded\" (Sisense sometimes uses the misspelling)\n- \"skipped\"\n- \"failed\" (often grouped by error category)\n2) Fallbacks (old failsafes): if the response is missing expected fields, not JSON,\nor indicates a request-level error via keys like \"message\" / \"error\", we treat the\nentire batch as failed and attach the best-available reason from:\n- response JSON \"message\" / \"error\" / \"error.message\"\n- response.text (truncated)",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboard_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of dashboard OIDs to migrate. Provide either `dashboard_ids` or `dashboard_names`."
        },
        "dashboard_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of dashboard titles to migrate. Provide either `dashboard_ids` or `dashboard_names`."
        },
        "action": {
          "type": "string",
          "enum": [
            "skip",
            "overwrite",
            "duplicate"
          ],
          "description": "Determines how the target handles conflicts. - None: default Sisense behavior (typically skip existing). - \"skip\": skip if exists. - \"overwrite\": overwrite if exists (shares/ownership migration is skipped). - \"duplicate\": create duplicate (shares/ownership migration is skipped)."
        },
        "republish": {
          "type": "boolean",
          "description": "Whether to republish dashboards after import."
        },
        "migrate_share": {
          "type": "boolean",
          "description": "If True, attempts to migrate shares (and optionally ownership) after dashboards are created."
        },
        "change_ownership": {
          "type": "boolean",
          "description": "If True and `migrate_share=True`, attempts to change ownership on the target dashboards."
        },
        "emit": {
          "type": "object",
          "description": "Optional callback invoked with structured progress events. If not provided, no events are emitted and only the final result is returned. Event payloads carry a ``type`` (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\"), a ``step`` identifier, a human-readable ``message``, and additional step-specific fields."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "migration",
      "dashboards",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_datamodels",
    "module": "migration",
    "sub_module": "migration.datamodels",
    "class": "Migration",
    "method": "migrate_datamodels",
    "description": "Migrates specific data models from the source environment to the target environment.",
    "full_doc": "Migrates specific data models from the source environment to the target environment.\n\nParameters\n----------\ndatamodel_ids : list[str] or None, default None\n    A list of data model IDs to migrate. Either `datamodel_ids` or `datamodel_names` must be provided.\ndatamodel_names : list[str] or None, default None\n    A list of data model names to migrate. Either `datamodel_ids` or `datamodel_names` must be provided.\nprovider_connection_map : dict[str, str] or None, default None\n    A dictionary mapping provider names to connection IDs. This allows specifying different connections\n    per provider.\n    Example:\n    {\n        \"Databricks\": \"Connection ID\",\n        \"GoogleBigQuery\": \"Connection ID\"\n    }\ndependencies : list[str] or str or None, default None\n    A list of dependencies to include in the migration. If not provided or if \"all\" is passed, all\n    dependencies are selected by default.\n\n    Possible values:\n    - \"dataSecurity\" (includes both Data Security and Scope Configuration)\n    - \"formulas\" (for Formulas)\n    - \"hierarchies\" (for Drill Hierarchies)\n    - \"perspectives\" (for Perspectives)\n\n    If left blank or set to \"all\", all dependencies are included by default.\n\n    Windows: the export endpoint used on Windows sources has no\n    dependencies parameter, so this argument has no effect there.\nshares : bool, default False\n    Whether to also migrate the data model's shares.\naction : str or None, default None\n    Strategy to handle existing data models in the target environment.\n\n        - \"overwrite\": Attempts to overwrite existing model using its original ID via the datamodelId parameter.\n        If the model is not found in target environment, it will automatically fall back and create the model.\n        - \"duplicate\": Creates a new model by passing a `new_title` to the `newTitle` parameter of the import API.\n        If `new_title` is not provided, the original title will be used with \" (Duplicate)\" appended.\nnew_title : str or None, default None\n    New name for the duplicated data model. Used only when `action='duplicate'`.\nemit : Callable[[dict], None] or None, default None\n    Optional callback invoked with structured progress events. If not provided, the method emits no events\n    and only returns a final result.\n\n    Event payloads follow a consistent shape:\n    - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\")\n    - ``step``: str logical step identifier\n    - ``message``: str human-readable message\n    - Additional fields depending on the step (counts, status_code, etc.)\n\nReturns\n-------\ndict\n    A summary of the migration results containing lists of succeeded, skipped, and failed data models,\n    plus counts/metadata in a dashboard-consistent format.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodel_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of data model IDs to migrate. Either `datamodel_ids` or `datamodel_names` must be provided."
        },
        "datamodel_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of data model names to migrate. Either `datamodel_ids` or `datamodel_names` must be provided."
        },
        "provider_connection_map": {
          "type": "object",
          "description": "A dictionary mapping provider names to connection IDs. This allows specifying different connections per provider. Example: { \"Databricks\": \"Connection ID\", \"GoogleBigQuery\": \"Connection ID\" }"
        },
        "dependencies": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "dataSecurity",
              "formulas",
              "hierarchies",
              "perspectives"
            ]
          },
          "description": "A list of dependencies to include in the migration. If not provided or if \"all\" is passed, all dependencies are selected by default. Possible values: - \"dataSecurity\" (includes both Data Security and Scope Configuration) - \"formulas\" (for Formulas) - \"hierarchies\" (for Drill Hierarchies) - \"perspectives\" (for Perspectives) If left blank or set to \"all\", all dependencies are included by default. Windows: the export endpoint used on Windows sources has no dependencies parameter, so this argument has no effect there."
        },
        "shares": {
          "type": "boolean",
          "description": "Whether to also migrate the data model's shares."
        },
        "action": {
          "type": "string",
          "description": "Strategy to handle existing data models in the target environment. - \"overwrite\": Attempts to overwrite existing model using its original ID via the datamodelId parameter. If the model is not found in target environment, it will automatically fall back and create the model. - \"duplicate\": Creates a new model by passing a `new_title` to the `newTitle` parameter of the import API. If `new_title` is not provided, the original title will be used with \" (Duplicate)\" appended.",
          "enum": [
            "overwrite",
            "duplicate"
          ]
        },
        "new_title": {
          "type": "string",
          "description": "New name for the duplicated data model. Used only when `action='duplicate'`."
        },
        "emit": {
          "type": "object",
          "description": "Optional callback invoked with structured progress events. If not provided, the method emits no events and only returns a final result. Event payloads follow a consistent shape: - ``type``: str (\"started\" | \"progress\" | \"warning\" | \"error\" | \"completed\") - ``step``: str logical step identifier - ``message``: str human-readable message - Additional fields depending on the step (counts, status_code, etc.)"
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "migration",
      "datamodel",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_groups",
    "module": "migration",
    "sub_module": "migration.groups",
    "class": "Migration",
    "method": "migrate_groups",
    "description": "Migrate specific groups from the source environment to the target environment.",
    "full_doc": "Migrate specific groups from the source environment to the target environment.\n\nFetches groups from the source environment, filters to the requested\nnames, and submits them to the target environment's bulk endpoint.\n\nParameters\n----------\ngroup_name_list : list[str]\n    The group names to migrate.\n\nReturns\n-------\ndict[str, Any] | list[dict[str, Any]]\n    A result payload with per-group migration statuses and any raw error\n    encountered, or a list with an informational message when no groups\n    match the requested names.",
    "parameters": {
      "type": "object",
      "properties": {
        "group_name_list": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The group names to migrate."
        }
      },
      "required": [
        "group_name_list"
      ]
    },
    "mutates": true,
    "tags": [
      "migration",
      "groups",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "migration.migrate_users",
    "module": "migration",
    "sub_module": "migration.users",
    "class": "Migration",
    "method": "migrate_users",
    "description": "Migrate specific users from the source environment to the target environment.",
    "full_doc": "Migrate specific users from the source environment to the target environment.\n\nFetches users from the source environment, maps their role and group\nnames to target environment IDs, and creates them in the target.\n\nParameters\n----------\nuser_name_list : list[str]\n    The user identifiers (email addresses) to migrate. (format: email)\n\nReturns\n-------\ndict[str, Any] | list[dict[str, Any]]\n    A result payload with per-user migration statuses and any raw error\n    encountered, or a list with an informational message when there are\n    no users to migrate.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_name_list": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "email"
          },
          "description": "The user identifiers (email addresses) to migrate."
        }
      },
      "required": [
        "user_name_list"
      ]
    },
    "mutates": true,
    "tags": [
      "migration",
      "users",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.disable_plugin",
    "module": "plugins",
    "sub_module": "plugins.core",
    "class": "Plugins",
    "method": "disable_plugin",
    "description": "Disable a single plugin by name or folder name.",
    "full_doc": "Disable a single plugin by name or folder name.\n\nLooks up the plugin from the live plugin list then sends a PATCH\nrequest to disable it. If the plugin is already disabled, no API call\nis made.\n\nParameters\n----------\nplugin : str\n    Plugin name or ``folderName`` to disable. The ``\"plugin-\"`` prefix\n    is optional.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"folderName\": str, \"isEnabled\": False, \"changed\": bool}`` on\n    success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "plugin": {
          "type": "string",
          "description": "Plugin name or ``folderName`` to disable. The ``\"plugin-\"`` prefix is optional."
        }
      },
      "required": [
        "plugin"
      ]
    },
    "mutates": true,
    "tags": [
      "plugins",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.disable_plugins",
    "module": "plugins",
    "sub_module": "plugins.core",
    "class": "Plugins",
    "method": "disable_plugins",
    "description": "Disable one or more plugins by name or folder name.",
    "full_doc": "Disable one or more plugins by name or folder name.\n\nFetches the current plugin list, identifies which of the requested\nplugins need to be disabled, and applies the changes. Plugins already\ndisabled are skipped without making an API call.\n\nParameters\n----------\nplugins : str or list[str]\n    One or more plugin names or folder names. The ``\"plugin-\"`` prefix\n    is optional and is stripped before matching.\nbulk : bool, optional\n    When ``True`` (default), all updates are sent in a single PATCH\n    request. When ``False``, one PATCH request is made per plugin.\n\nReturns\n-------\ndict[str, Any]\n    A summary dict with keys:\n\n    - ``changed`` (list[str]): ``folderName`` values that were disabled.\n    - ``already_disabled`` (list[str]): ``folderName`` values already disabled.\n    - ``not_found`` (list[str]): Requested names that matched no plugin.\n    - ``errors`` (list[str]): ``folderName`` values where the PATCH failed\n      (non-bulk mode only).",
    "parameters": {
      "type": "object",
      "properties": {
        "plugins": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more plugin names or folder names. The ``\"plugin-\"`` prefix is optional and is stripped before matching."
        },
        "bulk": {
          "type": "boolean",
          "description": "When ``True`` (default), all updates are sent in a single PATCH request. When ``False``, one PATCH request is made per plugin."
        }
      },
      "required": [
        "plugins"
      ]
    },
    "mutates": true,
    "tags": [
      "plugins",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.enable_plugin",
    "module": "plugins",
    "sub_module": "plugins.core",
    "class": "Plugins",
    "method": "enable_plugin",
    "description": "Enable a single plugin by name or folder name.",
    "full_doc": "Enable a single plugin by name or folder name.\n\nLooks up the plugin from the live plugin list then sends a PATCH\nrequest to enable it. If the plugin is already enabled, no API call\nis made.\n\nParameters\n----------\nplugin : str\n    Plugin name or ``folderName`` to enable. The ``\"plugin-\"`` prefix\n    is optional.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"folderName\": str, \"isEnabled\": True, \"changed\": bool}`` on\n    success, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "plugin": {
          "type": "string",
          "description": "Plugin name or ``folderName`` to enable. The ``\"plugin-\"`` prefix is optional."
        }
      },
      "required": [
        "plugin"
      ]
    },
    "mutates": true,
    "tags": [
      "plugins",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.enable_plugins",
    "module": "plugins",
    "sub_module": "plugins.core",
    "class": "Plugins",
    "method": "enable_plugins",
    "description": "Enable one or more plugins by name or folder name.",
    "full_doc": "Enable one or more plugins by name or folder name.\n\nFetches the current plugin list, identifies which of the requested\nplugins need to be enabled, and applies the changes. Plugins already\nenabled are skipped without making an API call.\n\nParameters\n----------\nplugins : str or list[str]\n    One or more plugin names or folder names. The ``\"plugin-\"`` prefix\n    is optional and is stripped before matching.\nbulk : bool, optional\n    When ``True`` (default), all updates are sent in a single PATCH\n    request. When ``False``, one PATCH request is made per plugin.\n\nReturns\n-------\ndict[str, Any]\n    A summary dict with keys:\n\n    - ``changed`` (list[str]): ``folderName`` values that were enabled.\n    - ``already_enabled`` (list[str]): ``folderName`` values already enabled.\n    - ``not_found`` (list[str]): Requested names that matched no plugin.\n    - ``errors`` (list[str]): ``folderName`` values where the PATCH failed\n      (non-bulk mode only).",
    "parameters": {
      "type": "object",
      "properties": {
        "plugins": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more plugin names or folder names. The ``\"plugin-\"`` prefix is optional and is stripped before matching."
        },
        "bulk": {
          "type": "boolean",
          "description": "When ``True`` (default), all updates are sent in a single PATCH request. When ``False``, one PATCH request is made per plugin."
        }
      },
      "required": [
        "plugins"
      ]
    },
    "mutates": true,
    "tags": [
      "plugins",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.get_all_plugins",
    "module": "plugins",
    "sub_module": "plugins.core",
    "class": "Plugins",
    "method": "get_all_plugins",
    "description": "Retrieve all plugins installed on the Sisense instance.",
    "full_doc": "Retrieve all plugins installed on the Sisense instance.\n\nFetches the complete plugin list using paginated requests to\n``GET /api/v1/plugins``. All pages are collected and returned as a\nsingle flat list.\n\nReturns\n-------\nlist[dict[str, Any]]\n    A list of plugin objects, each containing at minimum:\n\n    - ``name`` (str): API identifier for the plugin.\n    - ``folderName`` (str): Filesystem folder name (e.g. ``\"plugin-MyPlugin\"``).\n    - ``isEnabled`` (bool): Whether the plugin is currently active.\n\n    Returns ``[{\"error\": \"...\"}]`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": false,
    "tags": [
      "plugins",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.get_plugin",
    "module": "plugins",
    "sub_module": "plugins.core",
    "class": "Plugins",
    "method": "get_plugin",
    "description": "Get a single plugin by its name or folder name.",
    "full_doc": "Get a single plugin by its name or folder name.\n\nFetches all plugins and returns the first one whose ``name`` or\n``folderName`` matches the given string (case-insensitive, with or\nwithout the ``\"plugin-\"`` prefix).\n\nParameters\n----------\nplugin : str\n    Plugin name or ``folderName`` to look up. The ``\"plugin-\"`` prefix\n    is optional and is stripped before matching.\n\nReturns\n-------\ndict[str, Any]\n    The plugin object on success, or ``{\"error\": \"...\"}`` if the plugin\n    was not found or the request failed.",
    "parameters": {
      "type": "object",
      "properties": {
        "plugin": {
          "type": "string",
          "description": "Plugin name or ``folderName`` to look up. The ``\"plugin-\"`` prefix is optional and is stripped before matching."
        }
      },
      "required": [
        "plugin"
      ]
    },
    "mutates": false,
    "tags": [
      "plugins",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.restore_snapshot",
    "module": "plugins",
    "sub_module": "plugins.snapshots",
    "class": "Plugins",
    "method": "restore_snapshot",
    "description": "Restore plugin states to exactly match a previously saved snapshot.",
    "full_doc": "Restore plugin states to exactly match a previously saved snapshot.\n\nCompares the snapshot's plugin list against the live instance state and\ncomputes the minimal set of changes needed — enabling plugins that should\nbe on, disabling plugins that should be off. Plugins already in the\ncorrect state are skipped.\n\nParameters\n----------\nsnapshot : PluginSnapshot\n    A snapshot dict as returned by :meth:`save_snapshot`, containing at\n    minimum a ``\"plugins\"`` key with a list of ``folderName`` values for\n    the plugins that should be enabled. All other plugins will be disabled.\nbulk : bool, optional\n    When ``True`` (default), all enable and disable changes are sent in a\n    single PATCH request. When ``False``, one PATCH request is made per\n    plugin change.\n\nReturns\n-------\ndict[str, Any]\n    A summary dict with keys:\n\n    - ``enabled`` (list[str]): ``folderName`` values that were enabled.\n    - ``disabled`` (list[str]): ``folderName`` values that were disabled.\n    - ``already_set`` (int): Count of plugins already in the correct state.\n    - ``not_in_instance`` (list[str]): Snapshot entries with no matching plugin.\n    - ``errors`` (list[str]): ``folderName`` values where the PATCH failed\n      (non-bulk mode only).\n\n    Returns ``{\"error\": \"...\"}`` if the plugin list could not be fetched or\n    the snapshot is missing the ``\"plugins\"`` key.",
    "parameters": {
      "type": "object",
      "properties": {
        "snapshot": {
          "type": "object",
          "properties": {
            "plugins": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "created": {
              "type": "string"
            }
          },
          "required": [
            "plugins"
          ],
          "additionalProperties": true,
          "description": "A snapshot dict as returned by :meth:`save_snapshot`, containing at minimum a ``\"plugins\"`` key with a list of ``folderName`` values for the plugins that should be enabled. All other plugins will be disabled."
        },
        "bulk": {
          "type": "boolean",
          "description": "When ``True`` (default), all enable and disable changes are sent in a single PATCH request. When ``False``, one PATCH request is made per plugin change."
        }
      },
      "required": [
        "snapshot"
      ]
    },
    "mutates": true,
    "tags": [
      "plugins",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "plugins.save_snapshot",
    "module": "plugins",
    "sub_module": "plugins.snapshots",
    "class": "Plugins",
    "method": "save_snapshot",
    "description": "Capture the current plugin enable/disable state as a snapshot.",
    "full_doc": "Capture the current plugin enable/disable state as a snapshot.\n\nFetches all plugins and records which ones are currently enabled.\nThe returned dict can be stored by the caller and later passed to\n:meth:`restore_snapshot` to bring the instance back to this state.\n\nReturns\n-------\ndict[str, Any]\n    A snapshot dict with keys:\n\n    - ``created`` (str): ISO 8601 UTC timestamp of when the snapshot was taken.\n    - ``plugins`` (list[str]): Sorted list of ``folderName`` values for all\n      currently enabled plugins.\n\n    Returns ``{\"error\": \"...\"}`` if the plugin list could not be fetched.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "mutates": true,
    "tags": [
      "plugins",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "queries.elasticube_run_jaql_query",
    "module": "queries",
    "sub_module": "queries.core",
    "class": "Queries",
    "method": "elasticube_run_jaql_query",
    "description": "Run a JAQL query against a datasource (elasticube).",
    "full_doc": "Run a JAQL query against a datasource (elasticube).\n\nSends ``POST /api/datasources/{datasource_name}/jaql``. Use for\nvalidation-tab style queries in Sisense.\n\nParameters\n----------\ndatasource_name : str\n    Datasource / elasticube name.\njaql_payload : dict[str, Any]\n    JAQL query body (for example ``metadata``, ``datasource``, and\n    related fields as required by the API).\n\nReturns\n-------\ndict[str, Any]\n    Query result from the API, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource_name": {
          "type": "string",
          "description": "Datasource / elasticube name."
        },
        "jaql_payload": {
          "type": "object",
          "description": "JAQL query body (for example ``metadata``, ``datasource``, and related fields as required by the API)."
        }
      },
      "required": [
        "datasource_name",
        "jaql_payload"
      ]
    },
    "mutates": true,
    "tags": [
      "queries",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "queries.elasticubes_run_jaql_csv",
    "module": "queries",
    "sub_module": "queries.core",
    "class": "Queries",
    "method": "elasticubes_run_jaql_csv",
    "description": "Run a JAQL query and return CSV output.",
    "full_doc": "Run a JAQL query and return CSV output.\n\nSends ``POST /api/datasources/{datasource_name}/jaql/csv``.\n\nParameters\n----------\ndatasource_name : str\n    Datasource / elasticube name.\njaql_payload : dict[str, Any]\n    JAQL query body.\n\nReturns\n-------\ndict[str, Any] | str\n    Parsed JSON if the response is JSON, raw CSV text if the response\n    is not JSON, or ``{\"error\": \"...\"}`` on failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "datasource_name": {
          "type": "string",
          "description": "Datasource / elasticube name."
        },
        "jaql_payload": {
          "type": "object",
          "description": "JAQL query body."
        }
      },
      "required": [
        "datasource_name",
        "jaql_payload"
      ]
    },
    "mutates": true,
    "tags": [
      "queries",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "report_manager.create_report",
    "module": "report_manager",
    "sub_module": "report_manager.core",
    "class": "ReportManager",
    "method": "create_report",
    "description": "Create a new Report Manager report.",
    "full_doc": "Create a new Report Manager report.\n\nSends ``POST /api/v1/report_manager/reports``. Supported fields (canonical\nSisense payload names): ``name`` (required), ``reportType``\n(optional — defaults to all formats disabled when omitted; if\nprovided, must include the ``PDF``, ``CSV``, and ``URL`` boolean\nflags), ``runOnFinish`` (optional — defaults to\n``{\"fileShare\": {\"overwriteExisting\": False}}`` when omitted; if\nprovided, must include ``fileShare.overwriteExisting``), ``events``\n(defaults to ``[]`` when omitted), ``enabled``, ``schedule``,\n``dashboards``, ``recipients``, ``templateId``, ``priority``,\n``errorEmails``.\n\nParameters\n----------\nreport : dict[str, Any]\n    The report definition to create.\n\nReturns\n-------\ndict[str, Any]\n    The API response body on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "report": {
          "type": "object",
          "description": "The report definition to create."
        }
      },
      "required": [
        "report"
      ]
    },
    "mutates": true,
    "tags": [
      "report_manager",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "report_manager.delete_report",
    "module": "report_manager",
    "sub_module": "report_manager.core",
    "class": "ReportManager",
    "method": "delete_report",
    "description": "Delete a Report Manager report by id.",
    "full_doc": "Delete a Report Manager report by id.\n\nSends ``DELETE /api/v1/report_manager/reports/{id}``.\n\nParameters\n----------\nreport_id : str\n    The report id to delete.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True}`` on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "report_id": {
          "type": "string",
          "description": "The report id to delete."
        }
      },
      "required": [
        "report_id"
      ]
    },
    "mutates": true,
    "tags": [
      "report_manager",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "report_manager.get_report",
    "module": "report_manager",
    "sub_module": "report_manager.core",
    "class": "ReportManager",
    "method": "get_report",
    "description": "Retrieve a single Report Manager report by id.",
    "full_doc": "Retrieve a single Report Manager report by id.\n\nSends ``GET /api/v1/report_manager/reports/{id}``.\n\nParameters\n----------\nreport_id : str\n    The report id.\nowner_info : bool, optional\n    When ``True``, include the report owner's information. Default\n    is ``False``.\nrecipients_info : bool, optional\n    When ``True``, include recipient data. Default is ``False``.\ndashboards_info : bool, optional\n    When ``True``, include dashboard names. Default is ``False``.\nfields : str, optional\n    Whitelist of fields to return. Fields can also be excluded by\n    prefixing their name with ``-``.\n\nReturns\n-------\ndict[str, Any]\n    The report object, or ``{\"error\": \"...\"}`` on failure or when the\n    report is not found.",
    "parameters": {
      "type": "object",
      "properties": {
        "report_id": {
          "type": "string",
          "description": "The report id."
        },
        "owner_info": {
          "type": "boolean",
          "description": "When ``True``, include the report owner's information. Default is ``False``."
        },
        "recipients_info": {
          "type": "boolean",
          "description": "When ``True``, include recipient data. Default is ``False``."
        },
        "dashboards_info": {
          "type": "boolean",
          "description": "When ``True``, include dashboard names. Default is ``False``."
        },
        "fields": {
          "type": "string",
          "description": "Whitelist of fields to return. Fields can also be excluded by prefixing their name with ``-``."
        }
      },
      "required": [
        "report_id"
      ]
    },
    "mutates": false,
    "tags": [
      "report_manager",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "report_manager.get_reports",
    "module": "report_manager",
    "sub_module": "report_manager.core",
    "class": "ReportManager",
    "method": "get_reports",
    "description": "Retrieve all reports configured in Report Manager.",
    "full_doc": "Retrieve all reports configured in Report Manager.\n\nSends paginated requests to ``GET /api/v1/report_manager/reports`` and\ncollects every page into a single flat list. Filters are applied\nserver-side.\n\nParameters\n----------\nname : str, optional\n    Search reports by name.\nids : list[str] or str, optional\n    One or more report ids to filter by. A bare string is treated as\n    a single id.\nenabled : bool, optional\n    When provided, filter to only enabled (``True``) or disabled\n    (``False``) reports.\nstatuses : list[str] or str, optional\n    One or more running statuses to filter by. A bare string is\n    treated as a single status.\npriority : str, optional\n    Filter reports by priority. One of ``\"high\"``, ``\"normal\"``.\nowner_ids : list[str] or str, optional\n    One or more owner user ids to filter by. A bare string is\n    treated as a single id.\nfields : str, optional\n    Whitelist of fields to return for each report. Fields can also\n    be excluded by prefixing their name with ``-``.\nsort : str, optional\n    Field to sort results by. Ascending by default, descending if\n    prefixed with ``-``.\nlimit : int, optional\n    Page size used for the underlying paginated requests. Default is\n    ``100``.\n\nReturns\n-------\nlist[dict[str, Any]] | dict[str, Any]\n    A flat list of report objects, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Search reports by name."
        },
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more report ids to filter by. A bare string is treated as a single id."
        },
        "enabled": {
          "type": "boolean",
          "description": "When provided, filter to only enabled (``True``) or disabled (``False``) reports."
        },
        "statuses": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more running statuses to filter by. A bare string is treated as a single status."
        },
        "priority": {
          "type": "string",
          "description": "Filter reports by priority. One of ``\"high\"``, ``\"normal\"``."
        },
        "owner_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more owner user ids to filter by. A bare string is treated as a single id."
        },
        "fields": {
          "type": "string",
          "description": "Whitelist of fields to return for each report. Fields can also be excluded by prefixing their name with ``-``."
        },
        "sort": {
          "type": "string",
          "description": "Field to sort results by. Ascending by default, descending if prefixed with ``-``."
        },
        "limit": {
          "type": "integer",
          "description": "Page size used for the underlying paginated requests. Default is ``100``."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "report_manager",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "report_manager.run_report",
    "module": "report_manager",
    "sub_module": "report_manager.core",
    "class": "ReportManager",
    "method": "run_report",
    "description": "Trigger an immediate run of a Report Manager report.",
    "full_doc": "Trigger an immediate run of a Report Manager report.\n\nSends ``POST /api/v1/report_manager/reports/{id}/run``. If the maximum\nnumber of concurrently running reports has been reached, the report\nis queued by the server instead of running immediately.\n\nParameters\n----------\nreport_id : str\n    The report id to run.\n\nReturns\n-------\ndict[str, Any]\n    ``{\"success\": True}`` on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "report_id": {
          "type": "string",
          "description": "The report id to run."
        }
      },
      "required": [
        "report_id"
      ]
    },
    "mutates": true,
    "tags": [
      "report_manager",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "report_manager.update_report",
    "module": "report_manager",
    "sub_module": "report_manager.core",
    "class": "ReportManager",
    "method": "update_report",
    "description": "Update an existing Report Manager report.",
    "full_doc": "Update an existing Report Manager report.\n\nUpdates must be provided inside the ``report`` payload. Only fields\nprovided are updated; omitted fields are not modified. Sends\n``PATCH /api/v1/report_manager/reports/{id}``. Supported fields (canonical\nSisense payload names): ``name``, ``enabled``, ``schedule``,\n``events``, ``dashboards``, ``recipients``, ``templateId``,\n``priority``, ``errorEmails``, ``reportType`` (if provided, must\ninclude the ``PDF``, ``CSV``, and ``URL`` boolean flags),\n``runOnFinish`` (if provided, must include\n``fileShare.overwriteExisting``).\n\nParameters\n----------\nreport_id : str\n    The report id to update.\nreport : dict[str, Any]\n    The fields to update.\n\nReturns\n-------\ndict[str, Any]\n    The API response body on success, or ``{\"error\": \"...\"}`` on\n    failure.",
    "parameters": {
      "type": "object",
      "properties": {
        "report_id": {
          "type": "string",
          "description": "The report id to update."
        },
        "report": {
          "type": "object",
          "description": "The fields to update."
        }
      },
      "required": [
        "report_id",
        "report"
      ]
    },
    "mutates": true,
    "tags": [
      "report_manager",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_dashboard_structure",
    "module": "wellcheck",
    "sub_module": "wellcheck.dashboard_checks",
    "class": "WellCheck",
    "method": "check_dashboard_structure",
    "description": "Analyze the structure of one or more dashboards.",
    "full_doc": "Analyze the structure of one or more dashboards.\n\nThis method:\n  - Counts pivot widgets (pivot / pivot2)\n  - Counts tabber widgets (WidgetsTabber)\n  - Counts accordion widgets via accordionConfig on widgets\n  - Counts jump-to-dashboard (JTD) instances, treated as child dashboards\n\nA \"child dashboard\" is treated as any dashboard referenced as a\njump-to-dashboard target from the parent dashboard, either via\nwidget options (drillTarget) or via dashboard script\n(prism.jumpToDashboard calls).\n\nThis method is intended to back wellcheck tasks and agentic tools that\nrespond to prompts such as:\n  - \"check child dashboards for this dashboard\"\n  - \"check jump-to dashboards on XYZ\"\n  - \"analyze dashboard structure / complexity\"\n\nParameters\n----------\ndashboards : list of str, optional\n    One or more dashboard references to analyze. Each reference can be:\n      - a Sisense dashboard ID, or\n      - a dashboard title (name).\n    At least one dashboard reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\n\nReturns\n-------\nlist of dict\n    A list with one entry per successfully processed dashboard. Each\n    entry has the keys:\n      - dashboard_id (str): Resolved dashboard ID.\n      - dashboard_title (str): Resolved dashboard title.\n      - pivot_count (int): Number of pivot/pivot2 widgets.\n      - tabber_count (int): Number of WidgetsTabber widgets.\n      - accordion_count (int): Number of accordion widgets detected\n        via accordionConfig on widgets.\n      - jtd_count (int): Number of jump-to-dashboard (JTD) instances\n        (child dashboards) detected in widget options and scripts.\n    If no dashboards are successfully processed, an empty list is\n    returned and details are available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboards": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more dashboard references to analyze. Each reference can be: - a Sisense dashboard ID, or - a dashboard title (name). At least one dashboard reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_dashboard_widget_counts",
    "module": "wellcheck",
    "sub_module": "wellcheck.dashboard_checks",
    "class": "WellCheck",
    "method": "check_dashboard_widget_counts",
    "description": "Compute widget counts for one or more dashboards.",
    "full_doc": "Compute widget counts for one or more dashboards.\n\nThis method retrieves each specified dashboard definition, counts the\nnumber of widgets on that dashboard, and returns a per-dashboard\nsummary.\n\nParameters\n----------\ndashboards : list of str, optional\n    One or more dashboard references to analyze. Each reference can be:\n      - a Sisense dashboard ID, or\n      - a dashboard title (name).\n    At least one dashboard reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\n\nReturns\n-------\nlist of dict\n    A list with one entry per successfully processed dashboard. Each\n    entry contains:\n      - dashboard_id (str): Resolved dashboard ID.\n      - dashboard_title (str): Resolved dashboard title.\n      - widget_count (int): Number of widgets on the dashboard.\n\n    If no dashboards are successfully processed, an empty list is\n    returned and details are available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboards": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more dashboard references to analyze. Each reference can be: - a Sisense dashboard ID, or - a dashboard title (name). At least one dashboard reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "dashboards",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_datamodel_custom_tables",
    "module": "wellcheck",
    "sub_module": "wellcheck.datamodel_checks",
    "class": "WellCheck",
    "method": "check_datamodel_custom_tables",
    "description": "Inspect custom tables in one or more data models and flag the use of UNION.",
    "full_doc": "Inspect custom tables in one or more data models and flag the use of UNION.\n\nThis method resolves each data model reference (ID or title), retrieves\nits schema from the Sisense API, iterates through all datasets/tables,\nand returns one row per custom table with a flag indicating whether its\nSQL expression contains the word \"union\" (case-insensitive).\n\nParameters\n----------\ndatamodels : list of str, optional\n    One or more data model references to analyze. Each reference can be:\n      - a Sisense data model ID, or\n      - a data model title (name).\n    At least one data model reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\n\nReturns\n-------\nlist of dict\n    A list with one entry per custom table. Each entry contains:\n      - data_model (str): Data model title.\n      - table (str): Table name.\n      - has_union (str): \"yes\" if the custom table expression contains\n        \"union\" (case-insensitive), otherwise \"no\".\n\n    If no data models are successfully processed, an empty list is\n    returned and details are available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more data model references to analyze. Each reference can be: - a Sisense data model ID, or - a data model title (name). At least one data model reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_datamodel_import_queries",
    "module": "wellcheck",
    "sub_module": "wellcheck.datamodel_checks",
    "class": "WellCheck",
    "method": "check_datamodel_import_queries",
    "description": "Inspect tables in one or more data models for import queries.",
    "full_doc": "Inspect tables in one or more data models for import queries.\n\nThis method resolves each data model reference (ID or title), loads its\nschema, and checks every table for a ``configOptions.importQuery``\nconfiguration. For each table, it returns a row indicating whether an\nimport query is configured.\n\nParameters\n----------\ndatamodels : list of str, optional\n    One or more data model references to analyze. Each reference can be:\n      - a Sisense data model ID, or\n      - a data model title (name).\n    At least one data model reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\n\nReturns\n-------\nlist of dict\n    A list with one entry per table across all successfully processed\n    data models. Each entry contains:\n      - data_model (str): Resolved data model title.\n      - table (str): Table name.\n      - has_import_query (str): \"yes\" if an importQuery is present in\n        the table config options, otherwise \"no\".\n\n    If no data models are successfully processed, an empty list is\n    returned and details are available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more data model references to analyze. Each reference can be: - a Sisense data model ID, or - a data model title (name). At least one data model reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_datamodel_island_tables",
    "module": "wellcheck",
    "sub_module": "wellcheck.datamodel_checks",
    "class": "WellCheck",
    "method": "check_datamodel_island_tables",
    "description": "Identify island tables (tables with no relationships) in one or more data models.",
    "full_doc": "Identify island tables (tables with no relationships) in one or more data models.\n\nThis method retrieves the schema for each specified data model, inspects\nits relations and tables, and returns information about tables that do\nnot participate in any relationship (often called \"island tables\").\n\nParameters\n----------\ndatamodels : list of str, optional\n    One or more data model references to analyze. Each reference can be:\n      - a Sisense data model ID, or\n      - a data model title (name).\n    At least one data model reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\n\nReturns\n-------\nlist of dict\n    A list with one entry per island table. Each entry contains:\n      - datamodel (str): Data model title.\n      - datamodel_oid (str): Data model ID.\n      - table (str): Table name.\n      - table_oid (str): Table ID.\n      - type (str): Table type (e.g., 'live', 'custom').\n      - relation (str): Always \"no\" for island tables.\n\n    If no data models are successfully processed, an empty list is\n    returned and details are available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more data model references to analyze. Each reference can be: - a Sisense data model ID, or - a data model title (name). At least one data model reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_datamodel_m2m_relationships",
    "module": "wellcheck",
    "sub_module": "wellcheck.datamodel_checks",
    "class": "WellCheck",
    "method": "check_datamodel_m2m_relationships",
    "description": "Check for potential many-to-many (M2M) relationships between tables",
    "full_doc": "Check for potential many-to-many (M2M) relationships between tables\nin one or more data models.\n\nFor each data model, this method inspects the relation graph, builds\ntable/column pairs from the relations, and runs aggregate SQL queries\nagainst the data source to detect whether both sides of the relation\ncontain duplicate keys. Pairs where each side has more than one\noccurrence of its key are flagged as many-to-many.\n\nParameters\n----------\ndatamodels : list of str, optional\n    One or more data model references to analyze. Each reference can be:\n      - a Sisense data model ID, or\n      - a data model title (name).\n    At least one data model reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\n\nReturns\n-------\nlist of dict\n    A list with one entry per relation field pair checked. Each entry\n    contains:\n      - data_model (str): Data model title.\n      - left_table (str): Name of the left table.\n      - left_column (str): Name of the left column.\n      - right_table (str): Name of the right table.\n      - right_column (str): Name of the right column.\n      - is_m2m (bool): True when both sides have more than one\n        occurrence of their key, False otherwise.\n\n    If no data models are successfully processed, an empty list is\n    returned and details are available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more data model references to analyze. Each reference can be: - a Sisense data model ID, or - a data model title (name). At least one data model reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_datamodel_rls_datatypes",
    "module": "wellcheck",
    "sub_module": "wellcheck.datamodel_checks",
    "class": "WellCheck",
    "method": "check_datamodel_rls_datatypes",
    "description": "Inspect row-level security (RLS) rules for one or more data models and",
    "full_doc": "Inspect row-level security (RLS) rules for one or more data models and\nreport the datatype of the columns used in those rules.\n\nThis method resolves each data model reference, fetches its RLS (data\nsecurity) rules from the appropriate API endpoint based on the data\nmodel type (extract or live), and returns one row per unique\n(datamodel, table, column, datatype) combination.\n\nParameters\n----------\ndatamodels : list of str, optional\n    One or more data model references to analyze. Each reference can be:\n      - a Sisense data model ID, or\n      - a data model title (name).\n    At least one data model reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\n\nReturns\n-------\nlist of dict\n    A list with one entry per unique RLS column. Each entry contains:\n      - datamodel (str): Data model title.\n      - table (str): Table name where RLS is applied.\n      - column (str): Column name used in the RLS rule.\n      - datatype (str): Datatype reported by Sisense for that column.\n\n    If no data models are successfully processed, an empty list is\n    returned and details are available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "datamodels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more data model references to analyze. Each reference can be: - a Sisense data model ID, or - a data model title (name). At least one data model reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "datamodel",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.check_pivot_widget_fields",
    "module": "wellcheck",
    "sub_module": "wellcheck.dashboard_checks",
    "class": "WellCheck",
    "method": "check_pivot_widget_fields",
    "description": "Analyze pivot widgets on one or more dashboards and report those with many fields.",
    "full_doc": "Analyze pivot widgets on one or more dashboards and report those with many fields.\n\nThis method retrieves each specified dashboard, scans all pivot widgets\n(types containing ``\"pivot\"`` or ``\"pivot2\"``), counts how many fields\n(items) are attached to those widgets, and returns a per-widget summary\nfor any pivot with more than ``max_fields`` fields.\n\nParameters\n----------\ndashboards : list of str, optional\n    One or more dashboard references to analyze. Each reference can be:\n      - a Sisense dashboard ID, or\n      - a dashboard title (name).\n    At least one dashboard reference is required. At runtime this\n    method is tolerant of a single string being passed instead of a\n    list, and will normalize it to a one-element list.\nmax_fields : int, optional\n    Threshold for the number of fields on a pivot widget. Only pivot\n    widgets with more than this number of fields are included in the\n    returned data. Defaults to 20.\n\nReturns\n-------\nlist of dict\n    A list of dictionaries, each describing a pivot widget that exceeds\n    the configured field threshold. Each entry contains:\n      - dashboard_id (str): Resolved dashboard ID.\n      - dashboard_title (str): Resolved dashboard title.\n      - widget_id (str): Pivot widget ID.\n      - has_more_fields (bool): Always True for returned rows, since\n        only widgets above the threshold are included.\n      - field_count (int): Total number of fields (items) in the widget.\n\n    If no dashboards are successfully processed, or no pivot widgets\n    exceed the threshold, an empty list is returned and details are\n    available in the logs.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboards": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more dashboard references to analyze. Each reference can be: - a Sisense dashboard ID, or - a dashboard title (name). At least one dashboard reference is required. At runtime this method is tolerant of a single string being passed instead of a list, and will normalize it to a one-element list."
        },
        "max_fields": {
          "type": "integer",
          "description": "Threshold for the number of fields on a pivot widget. Only pivot widgets with more than this number of fields are included in the returned data. Defaults to 20."
        }
      },
      "required": []
    },
    "mutates": false,
    "tags": [
      "wellcheck",
      "read"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  },
  {
    "tool_id": "wellcheck.run_full_wellcheck",
    "module": "wellcheck",
    "sub_module": "wellcheck",
    "class": "WellCheck",
    "method": "run_full_wellcheck",
    "description": "Run a composite \"full\" wellcheck across dashboards and data models.",
    "full_doc": "Run a composite \"full\" wellcheck across dashboards and data models.\n\nThis method is a convenience wrapper that orchestrates multiple\ndashboard-level and data-model-level checks and returns a structured\nreport that groups their results. It additionally delegates\nunused-column analysis to ``AccessManagement.get_unused_columns_bulk``\nwhen an ``AccessManagement`` instance is configured on this WellCheck\n(the default constructor configures one); otherwise the\n``unused_columns`` section is an empty list and a warning is logged.\n\nParameters\n----------\ndashboards : str or list of str, optional\n    One or more dashboard references to analyze. Each reference can be:\n      - a Sisense dashboard ID, or\n      - a dashboard title (name).\n    At runtime this parameter is tolerant of a single string and will\n    normalize it to a one-element list.\ndatamodels : str or list of str, optional\n    One or more data model references to analyze. Each reference can be:\n      - a data model ID, or\n      - a data model title (name).\n    At runtime this parameter is tolerant of a single string and will\n    normalize it to a one-element list.\nmax_pivot_fields : int, optional\n    Threshold used by the pivot-fields check. Any pivot widget with\n    more than this number of fields is flagged.\n\nReturns\n-------\ndict\n    A dictionary with two top-level sections:\n\n    - \"dashboards\": {\n          \"structure\": [...],\n          \"widget_counts\": [...],\n          \"pivot_widget_fields\": [...],\n      }\n\n    - \"datamodels\": {\n          \"custom_tables\": [...],\n          \"island_tables\": [...],\n          \"rls_datatypes\": [...],\n          \"import_queries\": [...],\n          \"m2m_relationships\": [...],\n          \"unused_columns\": [...],\n      }\n\n    Each subsection contains the list of rows returned by the\n    corresponding check method. If a given set of references is not\n    provided or no assets are successfully processed, that subsection\n    will be an empty list.",
    "parameters": {
      "type": "object",
      "properties": {
        "dashboards": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more dashboard references to analyze. Each reference can be: - a Sisense dashboard ID, or - a dashboard title (name). At runtime this parameter is tolerant of a single string and will normalize it to a one-element list."
        },
        "datamodels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "One or more data model references to analyze. Each reference can be: - a data model ID, or - a data model title (name). At runtime this parameter is tolerant of a single string and will normalize it to a one-element list."
        },
        "max_pivot_fields": {
          "type": "integer",
          "description": "Threshold used by the pivot-fields check. Any pivot widget with more than this number of fields is flagged."
        }
      },
      "required": []
    },
    "mutates": true,
    "tags": [
      "wellcheck",
      "write"
    ],
    "sdk_version": "2.0.1",
    "updated_at": "2026-09-01T17:29:06Z",
    "examples": []
  }
]